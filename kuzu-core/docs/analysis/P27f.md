# P27f: #[inline] analysis

<task id="ses_08f9c4b2effeckVlPN63FADzwD" state="completed">
<task_result>
Now I have a comprehensive understanding of the entire codebase. Let me compile my thorough analysis.

---

## Comprehensive Analysis: Functions Needing `#[inline]` or `#[inline(always)]`

### Finding: The codebase has only 2 existing `#[inline]` annotations total (in `kuzu-processor/src/physical/common.rs`), meaning nearly all hot-path functions lack inline hints.

Below is my complete report organized by module.

---

## 1. kuzu-common/src/types.rs — Type System (EXTREMELY HOT)

These functions are called on every single value access in the query engine.

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 277 | `pub const fn physical_type_from_logical(logical: LogicalTypeID) -> PhysicalTypeID` | 28 lines (match) | `#[inline]` | Called per-value in expression evaluation, hash join probe, aggregate, sort. Big match on small enum — LLVM can optimize to a lookup table if inlined. |
| 310 | `pub fn from_days_since_epoch(days: i32) -> Self` | 1 line | `#[inline(always)]` | Trivial newtype constructor. |
| 315 | `pub fn days_since_epoch(&self) -> i32` | 1 line | `#[inline(always)]` | Trivial accessor. |
| 322 | `pub fn from_micros_since_epoch(micros: i64) -> Self` | 1 line | `#[inline(always)]` | Trivial constructor. |
| 327 | `pub fn micros_since_epoch(&self) -> i64` | 1 line | `#[inline(always)]` | Trivial accessor. |
| 333 | `pub fn new(months: i32, days: i32, micros: i64) -> Self` | 1 line | `#[inline(always)]` | Trivial constructor. |
| 137-231 | `impl From<X> for Value` (all 17 impls) | 1-2 lines each | `#[inline(always)]` | Each is a thin wrapper. `From` conversions are used pervasively in tight loops. |
| 235 | `pub fn logical_type(&self) -> LogicalTypeID` | 31 lines (match) | `#[inline]` | Big match but called per-value. Inlining lets the caller specialize. |
| 271 | `pub fn physical_type(&self) -> PhysicalTypeID` | 1 line | `#[inline(always)]` | Delegates to `physical_type_from_logical`. This appears in many hot paths. |

---

## 2. kuzu-common/src/vector.rs — LegacyValueVector (EXTREMELY HOT)

These getters/setters are called millions of times per query.

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 32 | `pub fn physical_type(&self) -> PhysicalTypeID` | 1 line | `#[inline(always)]` | Trivial field accessor. |
| 36 | `pub fn size(&self) -> usize` | 1 line | `#[inline(always)]` | Trivial field accessor. Called per-row in all loops. |
| 40 | `pub fn capacity(&self) -> usize` | 1 line | `#[inline(always)]` | Trivial field accessor. |
| 44 | `pub fn is_null(&self, idx: usize) -> bool` | 1 line | `#[inline(always)]` | Called per-element in filter, join, aggregate. |
| 48 | `pub fn set_null(&mut self, idx: usize, is_null: bool)` | 1 line | `#[inline(always)]` | Called per-element. |
| 52 | `pub fn resize(&mut self, new_size: usize)` | 2 lines | `#[inline]` | Tiny but has an assert. |
| 58 | `pub fn data(&self) -> &[u8]` | 1 line | `#[inline(always)]` | Raw data accessor — hot in scan write. |
| 62 | `pub fn data_mut(&mut self) -> &mut [u8]` | 2 lines | `#[inline]` | Raw data accessor. |
| 69 | `pub const fn physical_type_size(t: PhysicalTypeID) -> usize` | 12 lines (match) | `#[inline]` | Called per-value-access in getters/setters. Const fn already hints but explicit inline helps. |
| 89 | `pub fn get_i64(&self, idx: usize) -> Option<i64>` | 9 lines | `#[inline]` | Hot getter, called per-row in query execution. |
| 101 | `pub fn set_i64(&mut self, idx: usize, val: i64)` | 8 lines | `#[inline]` | Hot setter. |
| 112 | `pub fn get_i32(&self, idx: usize) -> Option<i32>` | 9 lines | `#[inline]` | Hot getter. |
| 124 | `pub fn set_i32(&mut self, idx: usize, val: i32)` | 8 lines | `#[inline]` | Hot setter. |
| 135 | `pub fn get_double(&self, idx: usize) -> Option<f64>` | 9 lines | `#[inline]` | Hot getter. |
| 147 | `pub fn set_double(&mut self, idx: usize, val: f64)` | 8 lines | `#[inline]` | Hot setter. |
| 464 | `pub fn get_bool(&self, idx: usize) -> Option<bool>` | 4 lines | `#[inline]` | Hot getter. |
| 455 | `pub fn push_bool(&mut self, val: bool)` | 5 lines | `#[inline]` | Hot push operation. |
| 472 | `pub fn push_string(&mut self, val: &str)` | 8 lines | `#[inline]` | Hot push operation. |

---

## 3. kuzu-common/src/data_chunk.rs — DataChunk (EXTREMELY HOT)

These are called per-row in every operator's main loop.

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 61 | `pub fn field(&self, idx: usize) -> &ArrayRef` | 1 line | `#[inline(always)]` | Trivial accessor called per-field per-row. |
| 65 | `pub fn num_fields(&self) -> usize` | 1 line | `#[inline(always)]` | Trivial accessor. |
| 69 | `pub fn resize(&mut self, new_size: usize)` | 2 lines | `#[inline(always)]` | Small delegator. |
| 74 | `pub fn active_rows(&self) -> usize` | 1 line | `#[inline(always)]` | Called in every operator loop core. Tiny but critical. |
| 110 | `pub fn is_null(&self, field_idx: usize, row_idx: usize) -> bool` | 1 line | `#[inline(always)]` | Delegates to Arrow. Called per-element. |
| 114 | `pub fn get_i64(&self, field_idx: usize, row_idx: usize) -> Option<i64>` | 4 lines | `#[inline]` | Hot typed accessor. |
| 120 | `pub fn get_i32(&self, field_idx: usize, row_idx: usize) -> Option<i32>` | 4 lines | `#[inline]` | Hot typed accessor. |
| 126 | `pub fn get_f64(&self, field_idx: usize, row_idx: usize) -> Option<f64>` | 4 lines | `#[inline]` | Hot typed accessor. |
| 132 | `pub fn get_bool(&self, field_idx: usize, row_idx: usize) -> Option<bool>` | 4 lines | `#[inline]` | Hot typed accessor. |
| 138 | `pub fn get_string(&self, field_idx: usize, row_idx: usize) -> Option<&str>` | 4 lines | `#[inline]` | Hot typed accessor. |

---

## 4. kuzu-common/src/selection.rs — SelectionVector (HOT)

Selection vectors are used in filter, hash join probe, and every operator with null/selection tracking.

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 32 | `pub fn is_empty(&self) -> bool` | 1 line | `#[inline(always)]` | Called in filter/materialize paths. |
| 36 | `pub fn clear(&mut self)` | 2 lines | `#[inline(always)]` | Small reset. |
| 41 | `pub fn push(&mut self, idx: u32)` | 6 lines | `#[inline]` | Hot in filter mask building. |
| 57 | `pub fn get(&self, pos: usize) -> Option<u32>` | 4 lines | `#[inline]` | Used in materialize loops. |
| 65 | `pub fn slice(&self, range: Range<usize>) -> &[u32]` | 2 lines | `#[inline]` | Small utility. |

---

## 5. kuzu-common/src/arrow_vector.rs — ArrowVector, Vector, VectorAccess (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 146 | `pub fn data_type(&self) -> DataType` | 1 line | `#[inline(always)]` | Trivial. |
| 152 | `fn size(&self) -> usize` | 1 line | `#[inline(always)]` | Trivial accessor via trait. |
| 156 | `fn physical_type(&self) -> PhysicalTypeID` | 1 line | `#[inline(always)]` | Trivial accessor via trait. |
| 160 | `fn is_null(&self, row: usize) -> bool` | 4 lines | `#[inline]` | Called per-element. |
| 167 | `fn get_i64(&self, row: usize) -> Option<i64>` | 9 lines | `#[inline]` | Hot typed accessor via trait. |
| 179 | `fn get_i32(&self, row: usize) -> Option<i32>` | 9 lines | `#[inline]` | Hot typed accessor. |
| 191 | `fn get_f64(&self, row: usize) -> Option<f64>` | 9 lines | `#[inline]` | Hot typed accessor. |
| 203 | `fn get_f32(&self, row: usize) -> Option<f32>` | 9 lines | `#[inline]` | Hot typed accessor. |
| 215 | `fn get_bool(&self, row: usize) -> Option<bool>` | 9 lines | `#[inline]` | Hot typed accessor. |
| 227 | `fn get_value(&self, row: usize) -> Option<Value>` | 35 lines (match) | `#[inline]` | Big match but called per-element in all operators. Inlining lets LLVM specialize. |
| 332 | `pub fn as_arrow(&self) -> Option<&ArrowVector>` | 4 lines | `#[inline]` | Hot dispatch. |
| 339 | `pub fn as_legacy(&self) -> Option<&ValueVector>` | 4 lines | `#[inline]` | Hot dispatch. |
| 346 | `pub fn as_legacy_mut(&mut self) -> Option<&mut ValueVector>` | 4 lines | `#[inline]` | Hot dispatch. |

---

## 6. kuzu-processor/src/physical/common.rs — Processor Utilities (HOT)

Two functions already have `#[inline]`. Many more need it.

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 6 | `pub(crate) fn store_value_in_vector(v: &mut ValueVector, row: usize, val: &Value)` | 57 lines (match) | `#[inline]` | **Extremely hot.** Called per-value in join probe, order by output, aggregate output, limit, top-k. Inlining lets the caller elide the match. |
| 60 | `pub(crate) fn value_cmp(a: &Value, b: &Value) -> Ordering` | 18 lines (match) | `#[inline]` | Hot in sort/comparison. Called per-row-pair in order by, block merge sort, top-k. |
| 81 | `pub(crate) fn value_hash(val: &Value) -> u64` | 5 lines | `#[inline]` | Tiny delegator, hot in hash join build/probe, aggregate hash table. |
| 89 | `pub(crate) fn hash_value_into(val: &Value, hasher: &mut impl Hasher)` | 41 lines (match) | `#[inline]` | Called by both `value_hash` and `value_hash_fast`. Hot path. |
| 134 | `pub(crate) fn value_hash_fast(val: &Value) -> u64` | 5 lines | `#[inline]` | Tiny delegator (like `value_hash` but with ahash). Hot in hash join. |
| 142 | `pub(crate) fn raw_key_hash_i64(val: i64) -> u64` | 6 lines | Already `#[inline]` | Good. |
| 151 | `pub(crate) fn raw_key_hash_bytes(val: &[u8]) -> u64` | 6 lines | Already `#[inline]` | Good. |

---

## 7. kuzu-processor/src/expression_evaluator.rs — Expression Evaluator (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 76 | `pub fn evaluate_to_arrow(&self, expr: &Expression, chunk: &DataChunk) -> Result<ArrowVector, String>` | 2 lines | `#[inline(always)]` | Thin delegator to `evaluate_arrow`. |

---

## 8. kuzu-processor/src/physical/order_aggregate/aggregatehashtable.rs — Aggregate (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 292 | `pub fn build_group_key(chunk: &DataChunk, group_cols: &[u32], row: usize) -> Value` | 23 lines (match) | `#[inline]` | Hot — called per-row in aggregate. |
| 319 | `fn resolve_name_to_col(name: &str, field_names: &[String]) -> Option<usize>` | 14 lines | `#[inline]` | Small utility, called per-aggregate-function. |
| 337 | `pub fn resolve_agg_col_indices(...) -> Vec<Option<usize>>` | 29 lines | `#[inline]` | Called once per aggregate but hot when inlined into caller. |
| 394 | `pub fn update_states_row(...)` | 17 lines | `#[inline]` | **Extremely hot** — called per-row in aggregate hash table. |

---

## 9. kuzu-processor/src/physical/order_aggregate/radixsort.rs — Radix Sort (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 61 | `pub fn is_radix_eligible(values: &[(Value, bool)]) -> bool` | 1 line | `#[inline(always)]` | Called per-block in sort path. |

---

## 10. kuzu-processor/src/physical/order_aggregate/blockmergesort.rs — Block Merge Sort (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 173 | `fn compare_rows(&self, a: usize, b: usize, ...) -> Ordering` | 11 lines | `#[inline]` | Called O(n log n) times during k-way merge. |

---

## 11. kuzu-processor/src/physical/scan_filter/scan.rs — Scan (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 106 | `fn write_value_to_vector(v: &mut ValueVector, row: usize, val: &Value)` | 57 lines | `#[inline]` | Essentially duplicates `store_value_in_vector`. Hot per-row in scan. |
| 166 | `pub(crate) fn value_to_physical_type(val: &Value) -> PhysicalTypeID` | 24 lines (match) | `#[inline]` | Duplicates `Value::physical_type()`. Called per-column in scan. |
| 194 | `pub(crate) fn logical_to_physical(logical: &LogicalTypeID) -> PhysicalTypeID` | 33 lines (match) | `#[inline]` | Duplicates `physical_type_from_logical` from types.rs. |

---

## 12. kuzu-processor/src/physical/scan_filter/filter.rs — Filter (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 12 | `fn boolean_array_to_selection(bool_arr: &BooleanArray) -> SelectionVector` | 10 lines | `#[inline]` | Hot — built per chunk in filter. |
| 79 | `pub fn mask_to_selection(mask: &[bool]) -> SelectionVector` | 8 lines | `#[inline]` | Hot — built per chunk in filter. |
| 189 | `fn evaluate_binary_op_legacy(op: &BinaryOp, left: &[bool], right: &[bool], size: usize) -> Result<Vec<bool>, String>` | 13 lines | `#[inline]` | Called per binary op in filter evaluation. |

---

## 13. kuzu-storage/src/page.rs — Page/Frame (HOT — storage path)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 38 | `pub fn pin(&mut self)` | 1 line | `#[inline(always)]` | Called on every page access in buffer manager. |
| 43 | `pub fn unpin(&mut self)` | 3 lines | `#[inline]` | Called on every page unpin. |
| 50 | `pub fn is_pinned(&self) -> bool` | 1 line | `#[inline(always)]` | Called during eviction. |
| 55 | `pub fn mark_dirty(&mut self)` | 1 line | `#[inline(always)]` | Called on every page write. |
| 95 | `pub fn page_offset(&self, page_num: PageNum) -> u64` | 1 line | `#[inline(always)]` | Called per page I/O. |

---

## 14. kuzu-storage/src/buffer_manager.rs — Buffer Manager (HOT — storage path)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 102 | `pub fn stats(&self) -> &BufferManagerStats` | 1 line | `#[inline(always)]` | Trivial getter. |
| 106 | `pub fn page_size(&self) -> usize` | 1 line | `#[inline(always)]` | Trivial getter. |
| 110 | `pub fn num_frames(&self) -> usize` | 1 line | `#[inline(always)]` | Trivial getter. |
| 115 | `pub fn is_file_registered(&self, name: &str) -> bool` | 1 line | `#[inline(always)]` | Trivial getter. |

---

## 15. kuzu-storage/src/compression.rs — Compression (HOT — storage path)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 69 | `fn compress_integer_impl(value_bytes: &[u8]) -> Vec<u8>` | 9 lines | `#[inline]` | Hot per-value in compression. |
| 84 | `fn decompress_integer_impl(data: &[u8], original_size: usize) -> Vec<u8>` | 11 lines | `#[inline]` | Hot per-value in decompression. |
| 451 | `pub fn serialized_value_size(physical_type: PhysicalTypeID) -> usize` | 8 lines (match) | `#[inline]` | Called per-column-create and per-compression. |
| 378 | `pub fn compress_serialized_value(compression: CompressionType, raw: &[u8], value_size: usize) -> Vec<u8>` | 31 lines (match) | `#[inline]` | Hot per-value in column append. |
| 415 | `pub fn decompress_serialized_value(compression: CompressionType, compressed: &[u8], value_size: usize) -> Vec<u8>` | 31 lines (match) | `#[inline]` | Hot per-value in column read. |

---

## 16. kuzu-storage/src/column.rs — Columnar Storage (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 669 | `fn locate_row(&self, row_idx: u64) -> (usize, u64)` | 10 lines | `#[inline]` | Called per-row in `get_value`, `read_value_bytes`. Binary search is small. |
| 683 | `fn ensure_page_for_write(&mut self) -> io::Result<u64>` | 7 lines | `#[inline]` | Small helper, called per append. |

---

## 17. kuzu-function/src/scalar/mod.rs — Function Dispatcher (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 74 | `pub fn evaluate_scalar(func: &ScalarFunction, args: &[Value]) -> Result<Value, String>` | 26 lines (match) | `#[inline]` | **Extremely hot.** Called per-row per-function-call in expression evaluation. The match is on an enum discriminant — inlining lets the call site optimize out the dispatch. |

---

## 18. kuzu-function/src/scalar/comparison.rs (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 23 | `pub(crate) fn compare_values(a: &Value, b: &Value) -> Result<Ordering, String>` | 8 lines (match) | `#[inline]` | Called per-row-pair in sort, and per-value-pair in comparison evaluation. |

---

## 19. kuzu-function/src/scalar/hash.rs — Hash Functions (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 11 | `fn hash_value(v: &Value) -> u64` | 27 lines (match) | `#[inline]` | Hot — called per-row for hash joins, hash aggregates. |
| 39 | `fn murmur64(mut x: u64) -> u64` | 7 lines | `#[inline(always)]` | Hot inner loop function. |
| 48 | `fn combine_hash(a: u64, b: u64) -> u64` | 1 line | `#[inline(always)]` | Hot inner loop function. |
| 52 | `fn hash_string(s: &str) -> u64` | 11 lines | `#[inline]` | Hot for string hash keys. |

---

## 20. kuzu-function/src/scalar/arithmetic.rs (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 299 | `pub(crate) fn numeric_to_f64(v: &Value) -> Result<f64, String>` | 7 lines (match) | `#[inline]` | Hot utility called by many arithmetic ops. |
| 309 | `fn add_values(a: Value, b: Value) -> Result<Value, String>` | 9 lines (match) | `#[inline]` | Hot — called per-row for `+`. |
| 320 | `fn sub_values(a: Value, b: Value) -> Result<Value, String>` | 5 lines | `#[inline]` | Hot — called per-row for `-`. |
| 328 | `fn mul_values(a: Value, b: Value) -> Result<Value, String>` | 5 lines | `#[inline]` | Hot — called per-row for `*`. |
| 336 | `fn div_values(a: Value, b: Value) -> Result<Value, String>` | 16 lines | `#[inline]` | Hot — called per-row for `/`. |

---

## 21. kuzu-function/src/aggregate/mod.rs — Aggregation (HOT)

| Line | Function | Body Size | Recommendation | Rationale |
|------|----------|-----------|----------------|-----------|
| 55 | `pub fn new(func: &AggregateFunction) -> Self` | 35 lines (match) | `#[inline]` | Called per-group in aggregate hash table. |
| 93 | `pub fn update(&mut self, val: &Value)` | 69 lines (match) | `#[inline]` | **Extremely hot** — called per-row per-aggregate. |
| 166 | `pub fn finalize(&self) -> Value` | 54 lines (match) | `#[inline]` | Called per-group at finalize. |
| 222 | `pub fn merge(&mut self, other: &Self)` | 87 lines (match) | `#[inline]` | Hot in parallel aggregate merge. |
| 312 | `fn add_values_for_agg(a: Value, b: Value) -> Value` | 7 lines (match) | `#[inline]` | Tiny — called per-aggregate-merge. |
| 484 | `pub fn evaluate_aggregate(func: &AggregateFunction, args: &[Value]) -> Result<Value, String>` | 21 lines | `#[inline]` | Entry point for aggregate evaluation. |

---

## Summary of Recommendations by Priority

### P0 — MUST HAVE `#[inline(always)]` (trivial 1-liner getters/setters)
These are called millions of times per query and are 1-2 lines:

- `types.rs`: `Date::from_days_since_epoch`, `Date::days_since_epoch`, `Timestamp::from_micros_since_epoch`, `Timestamp::micros_since_epoch`, `Interval::new`, all 17 `From<primitive> for Value` impls, `Value::physical_type`
- `vector.rs`: `physical_type()`, `size()`, `capacity()`, `is_null()`, `set_null()`, `data()`, `data_mut()`
- `data_chunk.rs`: `field()`, `num_fields()`, `active_rows()`, `resize()`, `is_null()`
- `selection.rs`: `is_empty()`, `clear()`, `slice()`, `get()`
- `arrow_vector.rs`: `data_type()`, `size()`, `physical_type()`, `is_null()`
- `page.rs`: `pin()`, `is_pinned()`, `mark_dirty()`, `page_offset()`
- `buffer_manager.rs`: `stats()`, `page_size()`, `num_frames()`, `is_file_registered()`

### P1 — MUST HAVE `#[inline]` (small hot functions, match-on-enum)

- `types.rs`: `physical_type_from_logical`, `Value::logical_type`
- `vector.rs`: `physical_type_size`, all typed getters/setters (`get_i64`, `set_i64`, `get_i32`, `set_i32`, `get_double`, `set_double`, `get_bool`, `push_bool`, `push_string`)
- `data_chunk.rs`: All typed getters (`get_i64`, `get_i32`, `get_f64`, `get_bool`, `get_string`)
- `arrow_vector.rs`: All trait getters (`get_i64`, `get_i32`, `get_f64`, `get_f32`, `get_bool`, `get_value`), `as_arrow()`, `as_legacy()`, `as_legacy_mut()`
- `common.rs` (processor): `store_value_in_vector`, `value_cmp`, `value_hash`, `value_hash_fast`, `hash_value_into`
- `aggregatehashtable.rs`: `build_group_key`, `update_states_row`
- `blockmergesort.rs`: `compare_rows`
- `scan.rs`: `write_value_to_vector`, `value_to_physical_type`, `logical_to_physical`
- `filter.rs`: `boolean_array_to_selection`, `mask_to_selection`, `evaluate_binary_op_legacy`
- `compression.rs`: `compress_integer_impl`, `decompress_integer_impl`, `serialized_value_size`, `compress_serialized_value`, `decompress_serialized_value`
- `column.rs`: `locate_row`
- `mod.rs` (function): `evaluate_scalar`
- `comparison.rs`: `compare_values`
- `hash.rs`: `hash_value`, `murmur64`, `combine_hash`, `hash_string`
- `arithmetic.rs`: `numeric_to_f64`, `add_values`, `sub_values`, `mul_values`, `div_values`
- `aggregate/mod.rs`: `new`, `update`, `finalize`, `merge`, `add_values_for_agg`, `evaluate_aggregate`

### P2 — NICE TO HAVE `#[inline]`

- `expression_evaluator.rs`: `evaluate_to_arrow`
- `radixsort.rs`: `is_radix_eligible`
- `resolve_name_to_col`, `resolve_agg_col_indices` in aggregatehashtable.rs
- `DataChunk::get_value`, `DataChunk::iter_rows`, `RowIter::next`
</task_result>
</task>