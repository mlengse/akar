# Port feature session notes: nextval/currval (2026-07-01)

- C++ reference: src/function/sequence/sequence_functions.cpp + src/include/function/sequence/sequence_functions.h.
- C++ semantics:
  - CURRVAL(name): returns current value without increment.
  - NEXTVAL(name): returns current value, then advances sequence (isReadOnly=false).
  - Input arg type is STRING, output INT64.
- Rust status found:
  - Registry has ScalarFunction::SequenceOp and registrations for nextval/currval.
  - Execution path implemented via kuzu-processor ExpressionEvaluator sequence callback + Connection::create_processor callback to catalog get_sequence/get_sequence_mut + next_k_val(1)/curr_val().
  - array_value already registered as alias to ListOp::Creation.
- Gap encountered during testing:
  - Binder currently rejects RETURN nextval('x') with "Variable 'nextval' not in scope" in connection integration tests.
  - So sequence behavior is validated at evaluator/processor layer, not parser/binder integration layer.
- Changes made this session:
  - Removed failing connection integration tests that used unsupported RETURN nextval(...) syntax.
  - Added processor tests in kuzu-processor/src/expression_evaluator.rs:
    - test_sequence_nextval_currval_with_callback
    - test_sequence_requires_callback
    - test_sequence_requires_string_arg
- Verification:
  - cargo check --workspace: pass (existing warnings only)
  - cargo clippy --workspace: pass with existing warnings
  - cargo test --workspace: pass (no failures)
  - cargo test -p kuzu-processor test_sequence_: 3/3 pass

- Continuation update:
  - Root cause of query-level failure was parser: primary rule ignored function_args and returned only Variable.
  - Fix applied in parser: build FunctionCall for variable + function_args.
  - Added parser regression test: RETURN nextval('my_seq'), currval('my_seq') has FunctionCall AST nodes.
  - Binder enhanced to infer Int64 return type for NEXTVAL/CURRVAL and added binder regression test.
  - Attempted connection e2e sequence tests, then removed because current projection/planner path still does not execute computed function-return expressions.
  - Final verification after parser+binder fixes: cargo check/clippy/test workspace all completed successfully.
