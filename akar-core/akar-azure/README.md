# Kuzu Azure Blob Storage Extension

Azure Blob Storage integration via DuckDB delegation using `DuckDbAttachHelper`.

**URI support:** `az://` and `abfss://` schemes

**Functions:** `azure_scan` (table)

**Tests:** 1
