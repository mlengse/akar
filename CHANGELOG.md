# Changelog — Akar

> Riwayat revisi Sprint 19 (P51/P52), dipisah dari `implementation plan.md` agar plan hanya berisi pekerjaan yang belum dikerjakan. Fase selesai P1–P50 + AUDIT → [`SPEC.md`](SPEC.md) & git history.
> Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) · Versi: [SemVer 2.0.0](https://semver.org/). Gate = `test [akar-core]`.

## [Unreleased]

### Added

- **feat(ml) — P90.1: reusable SBYO offline-load helper `SbyoModel` + `SbyoLoad::from_dir` (2026-09-06)** — new `pub(crate)` module `sbyo` in `akar-ml/src/sbyo.rs` (gated behind `onnx-embedding`): `SbyoModel { onnx: Vec<u8>, tokenizer: TokenizerFiles }` + `SbyoLoad::from_dir(dir)` reading `tokenizer.json`/`config.json`/`special_tokens_map.json`/`tokenizer_config.json` + ONNX; `find_onnx_file` pulled to `pub(crate)` (prefers plain `.onnx` over `*_int8.onnx`) for cross-provider reuse (basis P90.2–P90.5). 4 new unit tests. Gate `test [akar-core]` unchanged **1,964 total / 0 ignored / 1,964 passed / 0 failed**; fmt + clippy `-D warnings` clean. — `a166cc6`

- **feat(ml) — P90.3: SBYO offline-load untuk `Bgem3Provider` — `try_from_user_defined` + `new_from_dir` (2026-09-06)** — meniru jalur offline dense (P90.1) untuk BGE-M3: `Bgem3Provider::try_from_user_defined(onnx_bytes, tokenizer_files)` (dari bytes) + `Bgem3Provider::new_from_dir(dir)` (via builder `SbyoLoad::from_dir`) → `fastembed::UserDefinedBgem3Model::new(...)` + `Bgem3Embedding::try_new_from_user_defined(model, Default::default())`; session di-pre-warm (`Some(embedding)` — lazy-init offline tidak diperlukan), `dense_dimensions` tetap 1024, `model_name` dari nama direktori (fallback "user-defined"). 2 unit tests baru (feature-gated, tidak mengubah gate default): `test_bgem3_provider_offline_load` (jalur missing-onnx / missing-tokenizer / garbage-bytes lokal tanpa network download) + `test_bgem3_provider_try_from_user_defined_invalid_bytes`. Gate `test [akar-core]` (default features) tetap **1,964 total / 0 ignored / 0 failed**; jalur feature `onnx-embedding` akar-ml `--lib` **35/35 passed**; fmt + clippy `-D warnings` clean. — `18d7911`

- **feat(ml) — P90.4: SBYO offline-load untuk `RerankProvider` — `try_from_user_defined` + `new_from_dir` (2026-09-06)** — meniru jalur offline BGE-M3 (P90.3) untuk cross-encoder reranker: `RerankProvider::try_from_user_defined(onnx_bytes, tokenizer_files)` (dari bytes) + `RerankProvider::new_from_dir(dir)` (via builder `SbyoLoad::from_dir`) → `fastembed::UserDefinedRerankingModel::new(...)` + `TextRerank::try_new_from_user_defined(model, Default::default())`; session di-pre-warm (`Some(TextRerank)` — lazy-init offline tidak diperlukan), `model_name` dari nama direktori (fallback "user-defined"). `OnnxSource::Memory` dipakai (bytes model dibaca `SbyoLoad`); varian `File` ditangani upstream di dalam `try_new_from_user_defined` (`reranking/impl.rs:114-117`). 2 unit tests baru (feature-gated, tidak mengubah gate default): `test_rerank_provider_offline_load` (jalur missing-onnx / missing-tokenizer / garbage-bytes lokal tanpa network download) + `test_rerank_provider_try_from_user_defined_invalid_bytes`. Gate `test [akar-core]` (default features) tetap **1,964 total / 0 ignored / 0 failed**; jalur feature `onnx-embedding` akar-ml `--lib` **37/37 passed**; fmt + clippy `-D warnings` clean. — `a3f0cb4`

- **feat(ml) — P90.5: proxy `batch_size` ke provider Sparse/BGE-M3/Rerank (parity dense) (2026-09-06)** — tambah field `pub batch_size: usize` (`0` → `DEFAULT_BATCH_SIZE` 256, normalisasi di `try_new`, `DEFAULT_BATCH_SIZE` di konstruktor offline) pada `SparseProviderConfig`, `Bgem3ProviderConfig`, dan `RerankProviderConfig` (yang terakhir kini pakai `impl Default` eksplisit menggantikan derive, default model tetap `RerankerModel::default()`); field disimpan di inner structs + Debug field. Sejalan dengan dense `FastEmbedProvider`: `embed_texts` → `embed_texts_batched(texts, inner.batch_size)`; method baru `embed_texts_batched(texts, batch_size)` (sparse + bgem3) dengan fallback `0` → default, memanggil fastembed `embed(texts, Some(bs))`; `embed_text` tetap via `embed_texts`; `rerank`/`rerank_with_documents` kini pass `Some(inner.batch_size)` (sebelumnya `None`) — parity eksplisit "pasang `Some(batch)`, `None`→default 256" (fastembed `unwrap_or(DEFAULT_BATCH_SIZE)`). 2 unit tests baru (feature-gated): `test_sparse_batched_matches_default` + `test_bgem3_batched_matches_default` (output batched == default); asersi `batch_size == DEFAULT_BATCH_SIZE` ditambah di ketiga config-default test yang ada. Gate `test [akar-core]` (default features) tetap **1,964 total / 0 ignored / 0 failed**; jalur feature `onnx-embedding` akar-ml `--lib` **39/39 passed**; fmt + clippy `-D warnings` clean. — `29927bf`

- **feat(ml) — P90.6: SBYO offline tes per provider — bytes user-defined dense + HF cache re-use lintas instance (2026-09-06)** — melengkapi cakupan SBYO offline: (1) `test_dense_provider_try_from_user_defined_invalid_bytes` — parity jalur bytes offline dense dengan bgem3/rerank (garbage ONNX bytes → `InitFailed` dari bytes lokal tanpa download); (2) 4 tes "jalur cache" membuktikan provider kedua yang di-re-hidrasi dari HF cache yang sama menghasilkan output identik untuk SETIAP provider: `test_dense_cache_reuse_matches` (embeddings approx 1e-4), `test_sparse_cache_reuse_matches` (indices + values identik), `test_bgem3_cache_reuse_matches` (dense approx + shapes dense/sparse/colbert), `test_rerank_provider_cache_reuse_stable_ranking` (first-run mengisi cache `BAAI/bge-reranker-base` ke `.fastembed_cache`, re-init menghasilkan ranking + score identik dan urutan desc). 5 unit tests baru (feature-gated, tidak mengubah gate default). Gate `test [akar-core]` (default features) tetap **1,964 total / 0 ignored / 0 failed**; jalur feature `onnx-embedding` akar-ml `--lib` **44/44 passed** (was 39); fmt + clippy `-D warnings` clean. — `7586a0c`

- **feat(ml) — P97.1: varian model ter-quantize `*Q` via `EmbeddingModelChoice` (2026-09-06)** — tambah varian `DenseQ(EmbeddingModel)` pada enum `EmbeddingModelChoice` (tetap `#[non_exhaustive]`, non-breaking) sehingga selector model dapat mengekspresikan model dense ter-quantize — `EmbeddingModel` fastembed sudah memuat varian `*Q` (`BGEBaseENV15Q`/`BGESmallENV15Q`/`BGELargeENV15Q` → repo `Qdrant/*-onnx-Q` + `model_optimized.onnx`; `EmbeddingGemma300MQ4` 4-bit), jadi cukup memperluas permukaan selector; `test_model_choice_parse` diperluas dengan kasus `DenseQ(BGESmallENV15Q)`. 1 unit test diperluas (bukan baru; feature-gated, tidak mengubah gate default). Gate `test [akar-core]` (default features) tetap **1,964 total / 0 ignored / 0 failed**; jalur feature `onnx-embedding` akar-ml `--lib` **35/35 passed** (filter `embed`); fmt + clippy `-D warnings` clean. — `7586a0c`

### Changed

- **refactor(ml) — P90.1: dense `FastEmbedProvider::new_from_dir` delegates to `SbyoLoad::from_dir` (2026-09-06)** — load logic moved from `embed.rs` (`new_from_dir`, `find_onnx_file`, `read_required_token_file`) into the SBYO helper with identical read order + error messages (no behavior change); `test_new_from_dir_prefers_plain_onnx` updated to use `crate::sbyo::find_onnx_file`. — `a166cc6`

## [0.1.20] - 2026-09-04

### Added

- **feat(ml) — P89.7: integrasi end-to-end — offline weights, batch/parallel, errors, docs di akar-ml (2026-09-04)** — final polish task menutup serial P89.1–P89.7 fastembed integration. `FastEmbedProvider::new_from_dir(model_dir, dims)` baru: memuat `.onnx` + tokenizer (`tokenizer.json`, `config.json`, `special_tokens_map.json`, `tokenizer_config.json`) dari local path tanpa HF Hub download, meneruskan ke `TextEmbedding::try_new_from_user_defined` (via `UserDefinedEmbeddingModel` + `TokenizerFiles`); helper `find_onnx_file` (prefer plain `.onnx` atas `*_int8.onnx`) dan `read_required_token_file`. Batch: `EmbedProviderConfig` + `FastEmbedInner` menyimpan `batch_size` (default `DEFAULT_BATCH_SIZE = 256`, `0` → default); `embed_texts()` meneruskan `Some(batch_size)` ke ONNX; `embed_texts_batched(&self, texts, batch_size)` baru (override eksplisit); `par_embed_texts(&self, texts)` baru — paralelisasi sound via rayon `par_iter()`: karena `TextEmbedding` session tidak `Sync`, tiap worker membangun session sendiri dari init-options yang sama (weights di-cache disk oleh shared session yang di-pre-warm), hasil digabung urutan input identik dengan jalur sequential. Error handling: semua ONNX init/compute error dibungkus `Result<_, EmbeddingError>` (`InitFailed`/`ComputeFailed`). Forward-compat: `#[non_exhaustive]` pada `EmbeddingError`, `SparseEmbedding`, `EmbeddingModelChoice`, `EmbedProviderConfig`, `RerankProviderConfig`, `RerankProvider`, `FastEmbedProvider`. `// SAFETY:` tidak ditambahkan (tidak ada blok `unsafe` di module; `par_embed_texts` dikomentari penuh tentang soundness non-Sync session). Doc comments `# Errors`/`# Panics` ditambahkan pada public API. `akar-ml/Cargo.toml`: optional `rayon.workspace = true` digate ke `onnx-embedding` feature. 6 tests baru (feature-gated, tidak mengubah gate count): `test_offline_weights_load`, `test_new_from_dir_prefers_plain_onnx`, `test_batch_embed_parallel`, `test_batched_matches_default`, `test_error_conversion`, `test_non_exhaustive_struct` — semuanya lulus (termasuk jalur real-model ter-cache). Gate default `test [akar-core]` tetap **1,964 / 0 failed** (module `embed` + tests digate `onnx-embedding`, off di gate default); fmt + clippy `-D warnings` clean.

- **feat(server/dream) — P89.5: provider-aware `find_bridges` — DreamBackend terintegrasi di akar-server (GraphBackend) (2026-09-04)** — implements the P89.5 integration (per user decision "Option A": extend `GraphBackend` in `akar-server` rather than a new `AkarDreamBackend` in `akar-main`). `akar-ml/src/extension.rs` gains public `shared_embedding_provider() -> Option<Arc<dyn EmbeddingProvider>>` (delegates to the UDF's shared `OnceLock` provider); `akar-main/src/ml.rs` (new, module `ml` behind `ml-extension`) re-exposes it; `akar-server/Cargo.toml` gains `embed` (already) + new `ml-extension` feature (`embed` + `akar-main/ml-extension`) and an `akar-ml` dep. `GraphBackend` gains an optional `#[cfg(feature = "embed")]` `provider: Option<Arc<dyn akar_dream::EmbeddingProvider>>` field + `with_embedding()` constructor; the previously-stubbed `find_bridges` now (1) uses the provider when `embeddings` is empty to fetch each community's `Memory.content`, embed dense, compute centroids, and return the closest distinct-community pair via new helpers `fetch_contents`/`find_bridges_with_provider`/`bridges_from_embeddings`/`cosine` + free fn `keeper_distance_bridge`, and (2) still honors caller-provided embeddings (graceful when provider is `None` — REM returns empty `Vec::new()`). `DreamControl` gains `with_db_and_provider(db, Option<Arc<dyn EmbeddingProvider>>)` (embed-gated); `with_db` forwards `None`; `akar-server::accept_loop` wires `with_db_and_provider(db.clone(), akar_main::ml::shared_embedding_provider())` when `ml-extension` is active. All 14 `DreamBackend` methods remain implemented via Cypher with graceful degradation; `DreamBackend` trait unchanged. 3 new feature-gated tests (`embed_tests`): `test_akar_dream_backend_bridge`, `test_dream_without_ml_extension`, `test_dream_with_ml_extension`. Gate `test [akar-core]` (default features) unchanged **1,964 total / 0 failed**; feature path (ml-extension) 3 new tests pass; fmt + clippy `-D warnings` clean.

- **feat(ml/ext) — P89.4: UDF `embed_text()` via Extension framework (2026-09-04)** — new `MlExtension` in `akar-ml` (module `extension`, gated behind new `extension` feature that stacks on `onnx-embedding`) implementing the `Extension` trait (following the `LlmExtension` pattern). `MlExtension::load()` registers `embed_text(text)` as a `ScalarFunction::CustomScalar`: the closure takes `&[Value]`, computes a dense embedding via `FastEmbedProvider` lazily initialized from a process-wide `OnceLock<Arc<dyn EmbeddingProvider>>` singleton (model resolved from `AKAR_EMBED_MODEL` env var, default `BGESmallENV15` / 384 dims, via fastembed's own `FromStr`), and returns `Value::List(Vec<Value::Double>)`. `akar-ml/Cargo.toml` gains optional `akar-extension` + `akar-function` + `akar-common` deps and `extension` feature; dev-dep `akar-catalog`. `akar-main/Cargo.toml` gains `ml-extension` feature (`dep:akar-ml` + `akar-ml/extension`); `database.rs::register_builtin_extensions()` registers `MlExtension` behind `#[cfg(all(feature = "ml-extension", not(akar_wasm)))]` (excluded from WASM). Model init stays lazy (not in `register_builtin_extensions`, which is blocking). 3 unit tests: `test_ml_extension_name`, `test_embed_text_function_registered`, `test_embed_text_invalid_arg`. Gate path (default features) unaffected — extension code is cfg-gated. fmt + clippy clean (`-D warnings`).

- **feat(ml/dream) — P89.3: EmbeddingProvider trait & DreamBackend bridge (2026-09-04)** — generic `EmbeddingProvider` trait defined in `akar-ml/src/embed.rs` (behind `onnx-embedding` feature): `fn embed_dense(&[&str]) -> Result<Vec<Vec<f32>>>`, `fn dimensions() -> usize`, `fn model_name() -> &str`. `FastEmbedProvider` implements the trait. `akar-dream` depends on `akar-ml` optionally via new `embed` feature flag; re-exports `EmbeddingProvider` when enabled. `DreamOrchestrator::run_cycle()` accepts `Option<&dyn EmbeddingProvider>` (cfg-gated; `None` backward-compatible). `run_rem()` accepts the provider and routes embeddings to `find_bridges()` (TODO: content fetching from backend deferred to P89.5). `DreamControl::run()` in `akar-server` forwards the parameter. `akar-server/Cargo.toml` gains `embed` feature that propagates to `akar-dream/embed`. All callers pass `None` (backward-compatible). 4 clippy warnings fixed in P89.1/P89.2 code (`unnecessary_to_owned`, `derivable_impls`). Gate `test [akar-core]` remains **59 tests / 0 failed** across akar-dream + akar-server + akar-ml; fmt + clippy clean.

- **feat(ml) — P89.1: fastembed ONNX in-process embedding infrastructure di akar-ml (2026-09-04)** — module `embed` baru di `akar-ml` dengan `FastEmbedProvider` wrapper thread-safe (`Send + Sync`, `Arc<Mutex<Option<TextEmbedding>>>`, lazy init) untuk dense text embedding via ONNX Runtime tanpa API eksternal. Public API: `try_default()` (BGE-small-en-v1.5, 384 dims), `try_new(EmbedProviderConfig)`, `try_from_user_defined(onnx_bytes, tokenizer_files, pooling, dims)` (offline/air-gapped), `embed_texts(&[&str]) -> Vec<Vec<f32>>`, `embed_text(&str) -> Vec<f32>`, `dimensions()`, `model_name()`. Feature flag `onnx-embedding` menggate seluruh module (default off — tidak mempengaruhi WASM build). Dependencies: `fastembed = "6"` + `parking_lot = "0.12"` (keduanya opsional). 6 unit tests (config default, creation, batch embed, single embed, Send+Sync assertion, clone shares state). Gate `test [akar-core]` tetap **1,964 hijau**; fmt + clippy bersih.

- **feat(ml) — P89.2: sparse embedding, BGE-M3 multi-output, & cross-encoder reranking di akar-ml (2026-09-04)** — Sparse, multi-modal (BGE-M3), dan reranking providers ditambahkan ke `akar-ml/src/embed.rs`:
  1. **SparseEmbedding** output type — `SparseEmbedding { indices, values }` dengan `len()`, `is_empty()`, `to_dense(dim)` converter + `From<fastembed::SparseEmbedding>` impl.
  2. **MultiEmbeddingOutput** — joint output dari BGE-M3: `dense: Vec<Vec<f32>>`, `sparse: Vec<SparseEmbedding>`, `colbert: Vec<Vec<Vec<f32>>>` + `From<Bgem3EmbeddingOutput>`.
  3. **EmbeddingModelChoice** enum — `Dense(EmbeddingModel)`, `Sparse(SparseModel)`, `Multi(Bgem3Model)` untuk memetakan ke fastembed model enum tanpa coupling.
  4. **SparseEmbedProvider** — thread-safe wrapper `SparseTextEmbedding` (SPLADE++_en_v1 atau BGE-M3 sparse). API: `try_default()`, `try_new(SparseProviderConfig)`, `embed_texts(&[&str])`, `embed_text(&str)`, `model_name()`.
  5. **Bgem3Provider** — thread-safe wrapper `Bgem3Embedding` (BGE-M3 INT8, 1024d dense). API: `try_default()`, `try_new(Bgem3ProviderConfig)`, `embed_texts(&[&str]) -> MultiEmbeddingOutput`, `embed_text(&str)`, `dense_dimensions()`, `model_name()`.
  6. **RerankProvider** — thread-safe wrapper `TextRerank` (cross-encoder). API: `try_default()`, `try_new(RerankProviderConfig)`, `rerank(query, documents)`, `rerank_with_documents(query, documents)`, `model_name()`.
  7. `try_from_user_defined` updated — `pooling` parameter removed (uses `UserDefinedEmbeddingModel::new()` builder).
  8. 18 new unit tests (17 type/construction tests + 1 re-verified P89.1 embed test). Feature flag `onnx-embedding` unchanged. Gate `test [akar-core]` tetap hijau; fmt + clippy bersih.

## [0.1.19] - 2026-08-31

### Added

- **feat(parser/binder/planner/processor) — P88: aggregate-function `DISTINCT` (`COUNT(DISTINCT x)`, `collect(DISTINCT x)`, dst.) kini didukung; menutup gap parity essential terakhir §18 #3 (2026-08-31)** — desain non-breaking: `Expression::FunctionCall(String, Vec<Expression>)` tetap tuple-2 (39 match sites tak disentuh); parser me-mangle nama → `{lower}_distinct` (`count_distinct`), di-demangle hanya saat menghasilkan nama output:

  1. **Parser** (`cypher.pest` + `parser/expression.rs`) — `function_args` menerima leading `distinct_flag`; `mangle_distinct_aggregate` allow-list (count/sum/avg/min/max/collect/stddev/variance) → `DISTINCT` pada fungsi non-agregat error (`"DISTINCT is only supported in aggregate functions, got '{name}'"`); validasi argumen ≥1 non-star.
  2. **Binder** (`binder/mod.rs`) — return type di-resolve dari `base` (nama tanpa suffix `_DISTINCT`).
  3. **Optimizer** (`aggregate_detection.rs`) — `is_aggregate_name` strip suffix `_DISTINCT`; `aggregate_ref_name` demangle untuk nama kolom output.
  4. **Processor (split-aggregation)** — `split_distinct_name`; `SharedAggregateState` + `distinct_flags` + akumulator single-pass `distinct_accum` (per-(group,function) seen-set `Vec<Value>`, NULL dibuang, linear scan first-seen). Seen-set **tidak bisa di-merge antar shard** → jalur distinct melewati seluruh fast path Arrow + shard-merge; finalize melepas seen-set lalu `AggregateHashTable::build_output`.
  5. **Fix pendukung** — hasil `collect(...)` (kolom `List`) tidak bisa disimpan via `store_value_in_vector` (menandai List null — bug laten yang terkuak P88); kolom tersebut kini dibangun langsung via `arrow_array_from_values` (`build_output_direct`, `aggregatehashtable.rs`).
  6. 2 tes baru: `test_aggregate_distinct_parses` (unit parser: nama `count_distinct`, tolak `abs(DISTINCT …)`) + `test_aggregate_distinct` (integration: COUNT/SUM/collect DISTINCT, GROUP BY `COUNT(*)`, NULL-exclusion, DROP). Follow-up kairos: `_translate_collect_distinct` (`akar_compat.py`) bisa dihapus.

  Gate `test [akar-core]`: **1,964 total / 0 ignored / 1,964 passed / 0 failed** (was 1,962 + 2 tes baru); fmt + clippy `--all-targets -D warnings` bersih. — `0127912`

- **docs(parser/binder/planner) — P87: parity eksplisit superset akar vs C++ & gap terakhir didokumentasikan (2026-08-31)** — audit & dokumentasi (BUKAN implementasi):
  1. **Parser:** 33 `Statement` vs 22 C++ `oC_Statement` grammar rules (`Cypher.g4:8-30`); akar superset (semua statement C++ non-auth 1:1; +13 varian akar).
  2. **Binder:** 33 `BoundStatement` vs 20 C++ `StatementType` (`statement_type.h:8-29`).
  3. **Planner:** 59 `LogicalOperator` vs 50 C++ `LogicalOperatorType` (`logical_operator.h:13-64`), kategorisasi 1:1 (37) / merge (6) / inline connection-layer (7).
  4. Gap essential terakhir (kategori c) = per-aggregate-function `DISTINCT` — tercatat SPEC §18 #3 → **P88** (kini telah ditutup oleh P88).
  5. SPEC §3.3 diperbarui (catatan parity + §18 #2 dikoreksi 58). Dokumentasi murni — tanpa perubahan kode produksi.

  Gate `test [akar-core]`: tidak berubah (docs-only); doc-check PASS. — `0127912`

- **docs(processor) — P85: parity matrix operator fisik C++ 58 ↔ akar 55 di SPEC §3.3 (audit + dokumentasi; bukan implementasi) (2026-08-31)** — audit eksplisit tiap `PhysicalOperatorType` (kuzu-vela `src/include/processor/operator/physical_operator.h:17-76`) → operator akar, dikategorikan (a)/(b)/(c):

  1. **Hasil audit:** enum C++ sebenarnya **58 entries**, bukan 67 (klaim lama SPEC §18 #2 & plan miscounted; `EXTENSION_CLAUSE` di enum tapi jatuh ke default string-switch). Akar: **55 executors** = 50 `Physical*` + 5 infra (`ResultCollector`, `DummySink`, `DummySimpleSink`, `Profile`, `Partitioner`).
  2. **Kategori (a) — split-phase C++ di-merge jadi 1 operator akar (9 C++ → 4 akar):** `HASH_JOIN_BUILD`+`HASH_JOIN_PROBE` → `PhysicalHashJoin`; `INTERSECT_BUILD`+`INTERSECT` → `PhysicalIntersect`; `TOP_K`+`TOP_K_SCAN` → `PhysicalTopK`; `ORDER_BY`+`ORDER_BY_MERGE`+`ORDER_BY_SCAN` → `PhysicalOrderBy` (BlockMergeSort internal).
  3. **Kategori (b) — infra/connection-layer (bukan pipeline op, tapi fungsional eksplisit):** `Partitioner` (wired `map_projection.rs:381`), `DummySink`/`DummySimpleSink`/`Profile`/`ResultCollector` (`missing_ops.rs`), `SEMI_MASKER` → diinternalisasi ke `PhysicalSemiJoin`/`PhysicalAntiJoin`, `TABLE_FUNCTION_CALL` → `FunctionRegistry.execute_table_function` (`standalone_call.rs:151`), 17 op DDL/admin DB inline di `connection/ddl.rs`/`copy.rs` (`ALTER`, `ATTACH`/`DETACH`/`USE_DATABASE`, `CREATE_MACRO`/`SEQUENCE`/`TABLE`/`TYPE`, `DROP`, `COPY_TO` → `export_csv`/`export_parquet` `standalone_call.rs:849`, `EXPORT`/`IMPORT_DATABASE`, `INSTALL`/`LOAD`/`UNINSTALL_EXTENSION`, `TRANSACTION`).
  4. **Kategori (c) — operator benar-benar missing fungsional: KOSONG (∅).** Setiap dari 58 op C++ punya padanan fungsional di akar → **P86 dibatalkan** (tidak ada yang perlu diimplementasi). Akar justru punya executor tanpa padanan C++ enum (`PhysicalCopyFrom`, `PhysicalArtIndexRangeScan`, `PhysicalExplain`, `PhysicalCountRelTable`, `PhysicalCreateFtsIndex`, `PhysicalFtsScan`, `PhysicalVectorSimilarityScan`, `PhysicalAccumulate`, `PhysicalUnion`).
  5. **SPEC di-update:** tabel parity matrix penuh di §3.3 Processor + §18 Known Limitations #2 dikoreksi (58 bukan 67; essential parity ~100%; count gap = split-phase merge + connection-layer inline, bukan missing). **Dokumentasi murni — tanpa perubahan kode.**

  Gate `test [akar-core]`: tidak dijalankan ulang (perubahan hanya SPEC.md/plan/CHANGELOG); doc-check PASS. [Uncommitted]

- **feat(ddl/catalog) — P84: `CREATE TYPE <name> AS <type>` type-alias + `COMMENT ON TABLE` kini punya catalog backing store + kolom `comment` di `SHOW TABLES` (2026-08-31)** — kedua DDL tidak lagi error `"no catalog backing store"` (`akar-main/src/connection/ddl.rs`, sebelumnya PLANNED gap parity Parser/Binder → ~100%):
  1. **Catalog** (`akar-catalog/src/lib.rs`) — field `type_aliases: HashMap<String, TypeAliasEntry>` (`TypeAliasEntry { type_alias_id, name, type_name }`) dan `table_comments: HashMap<String, String>`, keduanya `#[serde(default)]` (format on-disk lama tetap terbaca), key lowercase. Methods: `create_type_alias` (Result<(), CatalogError>; duplikat → `AlreadyExists`; `next_id` + `bump_version`), `get_type_alias`, `type_aliases()`, `set_table_comment`, `get_table_comment`, `table_comments()`, `resolve_type_alias` (chain alias 32 iter, cycle guard `eq_ignore_ascii_case` → None).
  2. **Binder** (`akar-binder/src/binder/mod.rs`) — helper `parse_type_resolved` (parse builtin dulu, fallback `catalog.resolve_type_alias` → re-parse base); dipakai di node table, rel table, ALTER ADD COLUMN, `bind_create_type` (kini juga tolak duplikat nama alias dari catalog).
  3. **ddl.rs** (`akar-main/src/connection/ddl.rs`) — arm `BoundCreateType` → `create_type_alias` + success message; `BoundCommentOnTable` → `set_table_comment` + success message; physical ALTER ADD COLUMN resolve alias sebelum `Binder::parse_type` (jalur fisik re-parse tipe).
  4. **Parser grammar** (`cypher.pest`) — `type_name` fallback `!primary_key ~ identifier` (alias); `create_node_table` → `table_elements` (`table_element = { primary_key | column_def }`, ordered-choice karena PEG tak me-rollback → `PRIMARY KEY` ditelan identifier); `add_column` → `"ADD" ~ "COLUMN"?`. Handler `parse_ddl` di-update. Efek samping: node table tanpa PK / >1 PK kini lolos parse → Tolak di parser (PK kedua → `"Only one PRIMARY KEY clause is allowed per table"`) & binder wajibkan PK (`"CREATE NODE TABLE requires a PRIMARY KEY (missing ...)"`).
  5. **SHOW TABLES** (`standalone_call.rs`) — kolom ke-3 `comment` (via `get_table_comment`); `format_result` menurunkan jumlah kolom dari baris pertama → aman untuk pemakai kolom lama.
  - `is_catalog_mutating_ddl` sudah mencakup `BoundCreateType`/`BoundCommentOnTable` → penolakan di explicit txn aktif otomatis. 7 tes baru `tests/test_p84_type_alias_comment.rs` (alias basic + roundtrip + catalog-check; alias chain Age→Years; error duplikat/unknown base; ALTER ADD COLUMN alias; comment basic + overwrite; comment not found; comment di SHOW TABLES) + `test_ddl_errors` (missing PK / multiple PK) tetap hijau. Gate `test [akar-core]`: **1,962 total / 0 ignored / 1,962 passed / 0 failed** (was 1,955 + 7 tes baru); fmt + clippy `--all-targets -D warnings` bersih. — `b4d3f75`

- **test(function) — P78: orphan `akar-function/src/scalar_tests.rs` dihapus; 2 tes NaN `f64` di-port ke integration `tests/scalar_tests.rs` (2026-08-30)** — mod `#[cfg(test)]` (~161 tes) di `src/scalar_tests.rs` tak pernah dideklarasikan `mod scalar_tests;` di `lib.rs` → tak pernah dikompilasi/dijalankan. Audit overlap: 159/161 tes duplikat identik (modulo whitespace) dari integration `tests/scalar_tests.rs` yang merupakan superset lebih baru (23 tes tambahan: make_date, greatest/least, constant_or_null, date error paths). Keputusan: **hapus** (bukan wire-in + dedup) — nilai coverage wire-in ≈ 0 karena integration sudah menguji API yang sama via jalur public. Dua tes yang hanya ada di orphan — `test_comparison_nan_equality` + `test_comparison_nan_order` (NaN==NaN → Eq true/NotEq false; NaN > semua finite; NaN==NaN bukan gt/lt; Int64(1) < NaN cross-type) — di-port ke `tests/scalar_tests.rs` agar cakupan tak hilang. Gate `test [akar-core]`: **1,938 total / 0 ignored / 1,938 passed / 0 failed** (was 1,936 + 2 tes baru); fmt + clippy `-D warnings` bersih; plan + SPEC + CHANGELOG direconcile. — `80dfed3`

- **docs(optimizer) — P75: 3 optimizer passes diformalkan off-by-design & didokumentasikan di SPEC §3.3 (BUKAN implementasi) (2026-08-30)** — `OrderByPushDown`, `AggregateFusion`, dan `CommonSubexpressionElimination` di-return `operators.to_vec()` (NO-OP) dan kini diberi label eksplisit **off-by-design** di tabel optimizer SPEC §3.3 dengan alasan arsitektural yang presisi (bukan sekadar "belum dikerjakan"): `OrderByPushDown` butuh operator `MergeUnion` baru (UNION dieksekusi concatenation polos → sort per-branch bukan sort global; rewrite lama malah DROP global ORDER BY — `ladybug.rs:10-16`); `AggregateFusion` butuh rewrite argumen outer terhadap merged output schema yang tak bisa diekspresikan flat pass (menghasilkan NULL & COUNT(*) yang salah — `aggregate_fusion.rs:4-18`); `CSE` off-by-design menunggu cost model terbukti. **Keputusan:** JANGAN implement ketiganya tanpa operator/cost model baru yang terbukti; doc-comment file sudah memuat rasio penuh dan kini direfleksikan di SPEC. Dokumentasi/spesifikasi saja — **tanpa perubahan kode produksi**, gate `test [akar-core]` tidak berubah (tetap 1,936); doc-check PASS; plan + SPEC + CHANGELOG direconcile. [Uncommitted]

- **feat(server/dream) — P77b: `GraphBackend` — mapping nyata 17 method `DreamBackend` ke akar graph DB; `DreamControl::with_db` menggantikan stub `GraceBackend` pada jalur produksi (2026-08-30)** — the 17-method `DreamBackend` no longer degrades silently to no-ops when the server owns a database: `akar-server/src/dream.rs` now defines `GraphBackend` (17 real methods over akar's Cypher, parameterized via `Connection::prepare` + `Connection::execute` with `Vec<(&str, Value)>` — the analog of kairos Python `_execute(cypher, params)`), plus helpers `q()` (swallow-error prepare+execute), `rows()` (materialize `Row = Vec<(String,Value)>`), `scalar()`, `as_f64/as_usize/as_string`, and `by_name`. Method semantics mirror the authoritative Python reference `kairos/kairos/dream_akar_store.py`: `sample_for_dream` (recent by `created_at DESC` / random by `hash(id)` / low-salience, dedup by id, honor `max_memories` via `LIMIT $limit`), `get_connections`, `strengthen_edge` (clamped ≤1.0), `weaken_edge`, `prune_edge`, `update_supersedes`, `create_bridge_edges` (`OPTIONAL MATCH ... WHERE existing IS NULL CREATE`), `get_communities` (reads `m.community`, index-aligned by max id), `write_communities` (`UNWIND $batch` with `Value::Struct` rows). `find_bridges`/`extract_afe_facts`/`write_afe_facts`/`run_synthesis`/`recompute_dae` stay empty/zero to mirror mock semantics. **Graceful degradation:** `GraphBackend` never creates the `Memory`/`Connected` schema (kairos's `GraphStore` owns it); if tables are absent, binds fail and every method degrades to empty/zero — indistinguishable from `GraceBackend`, so the server stays safe on non-kairos DBs. `DreamControl` now holds an internal `DreamEngine` enum (`Grace(DreamOrchestrator<GraceBackend>)` / `Graph(Box<DreamOrchestrator<GraphBackend>>)`); `DreamControl::new()` keeps `GraceBackend` (tests unchanged), `DreamControl::with_db(db: Arc<Database>)` uses `GraphBackend`, and `accept_loop` (`lib.rs`) now wires `with_db(db.clone())` on the production path. The `Graph` variant is boxed (clippy `large_enum_variant`). 6 new unit tests incl. `seeded_db()` helper (creates `Memory` node table + `Connected` rel table, seeds 6 memories + 5 chain edges): state transitions, all-zero Grace, degrades-on-empty-DB, samples+reads edges, mutates edges, and `with_db` running a full real cycle. One dedup guard bug fixed along the way: `id == 0` (treats a legitimate `id:0` node as invalid) → detect `Value::Null` instead. Gate `test [akar-core]`: akar-server **6 unit + 42 integration + 1 doctest all passing, 0 failed / 0 ignored** workspace-wide; fmt + clippy `-D warnings` bersih; doc-check PASS. [Uncommitted]

- **feat(server/dream) — P77: `dream_control` op live dari stub `not_available` → lifecycle `status`/`pause`/`resume`/`run` di akar-server (2026-08-30)** — the `dream_control` wire op (added P66) no longer returns a hardcoded `not_available` row. `akar-server` now depends on `akar-dream`; a new `akar-server/src/dream.rs` module owns the per-server dream lifecycle: `DreamControl` holds `Arc<Mutex<DreamOrchestrator<GraceBackend>>>` + paused flag + last-stats, with `state()`/`run()`/`pause()`/`resume()`/`last_stats()`; `GraceBackend` implements all 17 `DreamBackend` methods as documented no-ops returning zero/empty (so a full 7-phase `run_cycle` executes safely against an empty graph, identical semantics to the existing `#[cfg(test)] MockBackend`). `SessionConfig` gained `dream: Arc<DreamControl>`, threaded once per server in `accept_loop` (`lib.rs:262`). `handle_dream_control` (`session.rs:214`) maps action → lifecycle op, returns columns `[action, status, note, dream_id]` where `status` is `idle`/`running`/`paused`; `""`/unknown default to `status` (backward-compatible with kairos `resume`/`status`/`pause` senders). 2 unit tests (`dream.rs`: state transitions, all-zero graceful backend) + 2 new integration tests (`run_advances_dream_id`, `pause_resume`) + the 2 existing `server_tests.rs` dream assertions updated `not_available`→`idle`/`running`/`paused`/`dream_id`. The 17-method production mapping onto the akar graph DB is a separate tracked task **P77b** (plan). Gate `test [akar-core]`: **1,932 total / 0 failed / 0 ignored** (was 1,928 + 4 new tests); fmt + clippy `-D warnings` bersih. [Uncommitted]

### Changed

- **perf(storage/processor) — P79 batch 8: string-dictionary single-copy `Rc<str>`; TopK stream bounded heap (bukan materialisasi penuh); OrderBy large-path materialisasi sort-key saja (2026-08-30)** — tiga NIT audit:

  1. **`StringDictionary` (akar-storage/src/string_dictionary.rs)** — `intern()` semula melakukan `s.to_string()` **dua kali** (dua alokasi heap identik: satu untuk `strings: Vec<String>`, satu untuk key `HashMap<String, u32>`) dan menyimpan duplikat permanen string di dua struktur. Kini satu alokasi `Rc<str>` (`Rc::from` di `intern`, `Rc::clone` untuk `strings`, key map berubah ke `HashMap<Rc<str>, u32>`); lookup memakai `s.as_ref()` (leverase `Rc<str>: Borrow<str>`). `deserialize` membangun ulang dengan Rc; `memory_usage` menghitung byte string sekali (bukan ganda). Penghematan memori 2× untuk kamus string besar di jalur kompresi `StringDictionary` (compression.rs tetap lokal, tidak lintas-thread → `Rc` aman). Semantik & format on-disk identik.

  2. **`PhysicalTopK::execute` (akar-processor/src/physical/order_aggregate/topk.rs)** — semula materializes seluruh `num_fields` kolom tiap baris ke `all_values` bahkan ketika hanya kolom sort-key yang dibutuhkan heap → O(rows × num_fields) memori & alokasi. Kini stream langsung dari chunk input ke bounded max-heap (`capacity = limit + offset`) dengan argumen `row_idx` (global) di-track secara manual; nilai sort-key per baris dibaca per-chunk via `chunk.get_value` hanya untuk kolom sort-key. Semantik hasil identik (test `same_order`/`only_value_columns_subset`).

  3. **`PhysicalOrderBy::execute` large-path (>10_000 rows)** — semula materializes **semua** kolom ke `PayloadChunk` padahal `BlockMergeSorter::new(block_size, sort_keys)` hanya membaca kolom sort-key; materialisasi penuh bisa ratusan kolom → memori & memcpy sia-sia terutama untuk projection kolom lebar. Kini hanya kolom sort-key unik yang in-range dimaterialisasi (`sort_cols`/`key_values`), di-remap ke indeks kompak (`compact_keys`) dengan filter out-of-range (reserved/duplicate sort keys), dan guard `sort_cols.is_empty()` → `(0..total_rows).collect()` menjaga kasus degenerate. `BlockMergeSorter` tetap menerima key indeks kompak.

  Gate `test [akar-core]` workspace `cargo test` (tanpa `--all-features`): **1,955 total / 0 ignored / 1,955 passed / 0 failed** (tanpa tes baru; storage 356 + processor 164 + full workspace hijau, incl. integration ORDER BY di akar-main); fmt + clippy `--all-targets -D warnings` bersih. Plan: item P79 TopK/OrderBy & string-dictionary ditandai selesai; item persistence rewrite (beririsan P60.1) **defer** ke P60.1 (overlap penuh, mitigasi P68 checkpoint-threshold ada). — `8a63903`

- **perf(test) — P83: runtime gate `test [akar-core]` ditekan dari ~7m57s (analisis htmlReport 2026-08-30; 7 grup menyerap ~5.8m/73%) via pengurangan workload tanpa melemahkan assert (2026-08-30)** — 8 sub-batch: (P83.1) `test_proptest.rs:37,62,112` `setup_db_on_disk()` → `setup_db()` (TempDir dileak, skip recursive-delete tiap case; tetap file-backed WAL — bukan memory-DB, test_helpers.rs:15-16) 53.09s→43.33s; (P83.2) `repro_multihop.rs:198` `n` 1500→400 + assert `Int64(151500)`→`Int64(40400)` (=400×101; 2 query pushdown-validation tetap) ~1m10s→9.39s; (P83.3) `test_plan_cache_timing.rs:33` tabel 10K→3K row (execution-dominated; `N=2000`/warmup 10/threshold 2.0 TETAP — jangan sentuh mitigasi flake P51.48b) ~39.5s→7.41s; (P83.4) `test_wcoj_bench.rs:92,134` repeat loop timing 5→2 (assert `10_000`/`10_660` + `plan contains Intersect` tetap) ~22.5s→12.09s; (P83.5) `test_idle_timeout_triggers_shutdown` `server_tests.rs:614` — investigasi: tes memakai `connect_tcp`+`client2.ping_op()` yang memblokir 30s read-timeout (remote.rs:416) karena listener tetap ter-bind setelah accept loop keluar; ditulis ulang → `assert!(ts._server.is_shutting_down())` memakai getter publik baru `Server::is_shutting_down()` (additive, lib.rs) menyusul idle 1s + sleep 3s, round-trip blocking 30s dihapus (35.13s→3.02s); (P83.6) `test_crash_recovery.rs` record/iterasi dikurangi (`write-burst` 500→250 / 300→200; `write` 1000→500; truncation 500→250/300→150/200→100; checkpoint-stress 200→100; threshold 50→25) 14.26s→7.38s; (P83.7) `akar-llm/src/lib.rs` `ollama_embed` tambah `.timeout_connect(Some(CONNECT_TIMEOUT))` — const baru `CONNECT_TIMEOUT: Duration = Duration::from_millis(500)` (HTTP_TIMEOUT 30s tetap) — `test_ollama_embed_fails_if_not_running` 2.06s→0.51s. **Batas keras kontrak terpenuhi:** TANPA `#[ignore]` (0 ignored) & TANPA tmpfs (win32); assert & semantik tiap tes dipertahankan, hanya workload dikecilkan. (P83.8) Verifikasi akhir: gate workspace `cargo test` (tanpa `--all-features` = run config `test [akar-core]`) **1,955 total / 0 ignored / 1,955 passed / 0 failed**; fmt + clippy `--workspace --all-targets -D warnings` bersih; doc-check PASS; plan + CHANGELOG direconcile. - `3c39d2f`

- **perf(storage/transaction) — P82: kontrak MVCC `commit_history` diubah dari `&[(u64,u64)]`/`Vec<(u64,u64)>` menjadi `&HashMap<u64,u64>`/`HashMap<u64,u64>` — look-up visibilitas O(H) per baris → O(1) (2026-08-30)** — `is_txn_committed_before` (akar-storage/src/version_info.rs) yang semula linear-scan seluruh history untuk tiap baris/record versi (O(rows×history) per scan) kini `commit_history.get(&txn_id).is_some_and(|&ts| ts <= snapshot_ts)` — O(1) lookup; `TransactionLifecycle::commit_history` (`akar-transaction/src/lib.rs:403`) `Mutex<Vec<(u64,u64)>>` → `Mutex<HashMap<u64,u64>>`, `push_commit_history` → `insert`, `is_visible` → `history.get(&txn_id)`; `commit_history_snapshot` meng-clone `HashMap` sekali (bukan Vec), menghilangkan pasangan `(txn_id, commit_ts)` duplikat-detect yang tak berguna. Threading di-update: `QueryProcessor.commit_history`/`with_snapshot`/clone `execute_internal` + `ExecutionContext.commit_history` (akar-processor); signature `scan_column`/`get_value_with_snapshot`/`to_column_major_data_with_snapshot*` (table.rs:426/657/712/737/808), `get_value_with_snapshot`/`get_value_owned_with_snapshot`/`is_row_visible` (node_group.rs:436/460/476), `is_visible` (version_info.rs:78/204), param `_commit_history` (column_chunk.rs:137/167, tetap tak terpakai); situs wiring akar-main (query.rs:229-244/422-426/696/744; ddl.rs:375-402) + `get_value` (table.rs:645) ikut. **Semantik tak berubah** (lookup kunci txn_id identik; urutan history tidak pernah jadi kontrak); `STORAGE_VERSION` tetap 1 (tidak ada format on-disk berubah — catatan: signature `pub fn` MVCC adalah public API 0.x, keputusan version bump 0.2.0 vs internal-family di-defer bersama rilis). Tes: literal `&[(u64,u64)]` tes → `HashMap::from`/`HashMap::new`; 1 tes baru `test_version_info_history_lookup_semantics` (absent vs committed-before vs committed-after; comment `// Empty history: inserted row invisible; deleted row still visible`). Gate `test [akar-core]`: **1,955 total / 0 ignored / 1,955 passed / 0 failed** (1,954 + 1 tes baru); fmt + clippy `-D warnings` bersih; doc-check PASS. - `e8c0d2d`

- **perf(processor) — P79: UNION DISTINCT dedup `deduped.contains(row)` O(n²) → O(n) hash-bucket (2026-08-30)** — `merge_union_chunks` (`akar-processor/src/processor/union_helpers.rs:55-70`) menggantikan scan linear `deduped.contains(row)` (kuadratik atas `Vec<Vec<Value>>` saat banyak baris unik) dengan hash-bucket membership index: `HashMap<u64, Vec<usize>>` hash→indeks, ekualitas eksak hanya di-cek pada tabrakan hash. Helper baru `hash_row` memakai `hash_value_into` (coverage penuh semua `Value` termasuk `List`/`Map` rekursif; pola yang sama sudah dipakai `merge_optional_chunks` untuk join-nya), dengan posisi value ikut di-hash agar permutasi satu baris tak berbagi satu hash. `Value` hanya mendapati `PartialEq` (tanpa `Hash`/`Eq` — `Double(f64)`), jadi bucket + perbandingan eksak, bukan `HashSet<Vec<Value>>`. Semantik & urutan output identik (first-seen order dipertahankan; hanya jalur `!all` yang berubah). 16 tes union (unit + `tests_only`, incl. `test_union_distinct_dedup`, `test_union_distinct_all_duplicates`) hijau; akar-processor lib 157/157; workspace `cargo test` semua suite **0 failed / 0 ignored** (total tes tak berubah → SPEC 1,938 tetap; satu failure timing/socket non-reproducible di run pertama, hijau di rerun workspace penuh); fmt + clippy `-D warnings` bersih. - `396943b`

- **perf(processor) — P79: `MultiplicityReducer` dedup `format!("{:?}", row_keys)` per-baris (alokasi String duplikat) → hash-bucket (2026-08-30)** — `PhysicalMultiplicityReducer::execute` (`akar-processor/src/physical/misc.rs:29-88`) menggantikan konstruksi kunci `format!("{:?}", row_keys)` + `HashSet<String>` (alokasi String + clone per baris, dan string-Debug sebagai kunci kesetaraan) dengan hash-bucket membership index `HashMap<u64, Vec<usize>>` di atas tuple `Vec<Value>` yang dipertahankan lintas chunk; ekualitas eksak (`==`) hanya di-cek pada tabrakan hash. Helper `hash_row(row: &[Value]) -> u64` (posisi value ikut di-hash agar permutasi tak bertabrakan) dipindah ke `physical/common.rs` — home kanonik di samping `value_hash`/`hash_value_into` — dan `union_helpers.rs` memakai varian bersama itu (duplikasi privat dihapus; UNION DISTINCT tetap 22 tes hijau). Semantik output identik (first-seen order per key-tuple tetap, dedup lintas semua chunk), satu perubahan perilaku yang disengaja: dua kunci `Double(NaN)` yang sebelumnya dianggap sama lewat string `"NaN"` kini **tidak** dianggap duplikat (IEEE `NaN != NaN`, konsisten `PartialEq`), sama dengan perilaku `merge_union_chunks` — dikunci via tes. 5 tes unit baru (`physical::misc::tests`: empty input, dedup lintas chunk, subset `key_columns`, key multi-kolom, NaN). Gate `test [akar-core]`: **1,943 total / 0 ignored / 1,943 passed / 0 failed** (1,938 + 5 tes baru); fmt + clippy `-D warnings` bersih. - `8495965`

### Fixed

- **fix(storage) — P79: `spill_and_clear`/`clear`/`restore_spilled` kini reset `version_info` — stale MVCC row-offset records tak lagi menabrak baris baru hasil reuse buffer (2026-08-30)** — `append_row_with_txn` (`akar-storage/src/node_group.rs:128-159`) mencatat `vi.insert(txn, num_nodes)` (offset lokal pre-increment) namun `spill_and_clear` (L165-181) mereset `num_nodes = 0` **tanpa** mereset `version_info` → append pasca-spill mencatat offset `0..M` yang bertabrakan dengan record lama `0..N`; karena `restore_spilled` (L191-230) menyusun ulang kolom (spill files dulu, baru in-memory), mapping offset menjadi korup dan visibilitas MVCC (`is_row_visible`) salah — baris baru bisa tak terlihat sampai commit timestamp yang tak akan datang. Fix mengikuti semantik `ChunkedNodeGroup::resetVersionAndUpdateInfo` Kuzu (`kuzu-vela/src/storage/table/chunked_node_group.cpp:125-132`): semua titik yang mengosongkan buffer untuk reuse/kembali-menomori row kini memanggil `version_info.reset()` — `VectorVersionInfo::reset` + `VersionInfo::reset` (clear `inserted`/`deleted` maps per vector) ditambahkan di `version_info.rs`, plus aggregator `VersionInfo::num_inserters()`/`num_deleters()`. Semantik desain yang diterima: baris hasil spill+restore kehilangan atribusi txn → "pre-existing visible" (tidak ada metadata txn per baris di spill file; koreksi penuh = persist version_info ke spill file seperti Kuzu — di luar scope NIT). `flush()` (L326-343) juga mengosongkan buffer tanpa reset — dicatat sebagai pertimbangan lanjutan, TIDAK disentuh batch ini (audit hanya menamai `spill_and_clear`). 4 tes baru (`test_version_info_reset_clears_all_records` + `test_vector_version_info_reset_clears_all_records` di version_info.rs; `test_clear_resets_version_info` + `test_spill_and_clear_resets_version_info` di node_group.rs — yang terakhir: 20 baris txn-tagged dengan spill threshold 64B auto-spill, restore → `num_inserters()==0` + semua 20 baris `is_row_visible` di snapshot 0). Gate `test [akar-core]`: **1,954 total / 0 ignored / 1,954 passed / 0 failed** (1,950 + 4 tes baru); fmt + clippy `-D warnings` bersih. - `0f82ef7`

- **fix(storage) — P79: CSV COPY `NULL` option (`null_str`) kini di-honor; empty-field→NULL jadi konfigurabel (2026-08-30)** — `coerce_string_to_value` (`akar-storage/src/csv_reader.rs:219-262`) dan parser bersarang `parse_list`/`parse_struct`/`parse_map` (`L610-737`) menggantikan tes null hardcoded `trimmed.is_empty()` dengan perbandingan terhadap sentinel `CsvReaderConfig.null_str` (default `""` — empty string) yang selama ini **di-parse dari opsi COPY `NULL`** (`from_options` L72, contoh `COPY ... FROM ... (NULL 'NA')`) tapi **tidak pernah dipakai** → opsi mati. Literal `null` (case-insensitive) tetap NULL (fixture `load-from-test/*` dependen). Perilaku default tidak berubah; dengan `null_str` non-default sel kosong kini mengikuti tipe kolom (String → `""`, numerik → `TypeCoercion`) dan sel bernilai sentinel → NULL, konsisten di elemen List/Struct/Map. 2 tes baru (`test_read_csv_custom_null_str`: literal → NULL + sel kosong bukan NULL; `test_read_csv_custom_null_str_nested`: List/Map dengan sentinel `NA`) + `test_config_from_options` diperluas dengan opsi `NULL`. Gate `test [akar-core]`: **1,945 total / 0 ignored / 1,945 passed / 0 failed** (1,943 + 2 tes baru); fmt + clippy `-D warnings` bersih. - `d64273e`

- **fix(main) — P79: swallow poisoned-lock di `Database::get_table_id` dihilangkan (2026-08-30)** — `Database::get_table_id` (`database.rs:439`) memakai `self.catalog.lock().ok()?` sehingga lock catalog yang rusak (poisoned oleh panic thread lain) **ditelan diam-diam** dan dibaca sebagai "tabel tidak ada" (`None`) — bisa memicu salah-error/mis-route transaksi. Kini memakai pola baku repo `map_err(|e| format!("Lock poisoned: {e}"))` dan mengembalikan `Result<Option<u64>, String>`; satu-satunya pemanggil (akar-migrate `main.rs:91,148`, idempotency check) menambahkan `.map_err(|e| anyhow::anyhow!(e))?`. Sibling yang sama-kelas di `connection/transaction.rs:122` (`rollback_write_txn`) — `txn_resources.lock().ok().and_then(...)` menelan poison dan diam-diam melewati pembersihan resource — kini mencatat `tracing::error!` saat lock poisoned (cleanup best-effort tetap berjalan, tidak menggagalkan rollback). Tak ada `.lock().ok()` tersisa di `akar-main/src`. Audit 2 NIT `database.rs:359,408-411` (nomor baris lama; lokasi aktual 439). Gate `test [akar-core]`: **1,947 total / 0 ignored / 1,947 passed / 0 failed** (tanpa tes baru); fmt + clippy `-D warnings` bersih. - `5692fda`

- **fix(algo) — P79: `random_walk`/`node2vec` RNG modulo bias `next_u32() % bound` dihilangkan (rejection sampling); `SimpleRng` GDS di-dedup (2026-08-30)** — `SimpleRng` (LCG seeded tanpa ekstra dependensi) semula diduplikasi di `akar-algo/src/gds/random_walk.rs:4-22` dan `node2vec.rs:4-22`, keduanya memakai `(next_u32 as usize) % bound` yang bias untuk bound non-pangkat-dua (beberapa sisa nilai u32 mendapat probabilitas lebih tinggi — relevan untuk sampling statistik). Kini satu impl bersama di `gds/rng.rs`, `gen_range` memakai **rejection sampling** (buang `x >= 2^32 - (2^32 % bound)` lalu `x % bound`; ≤1 draw ekstra hampir selalu) sehingga tiap bucket `0..bound` sama akuratnya — evaluasi `u64` menghindari overflow, `bound == 1` early-return, batas > `u32::MAX` di-clamp. `gen_float` (init embedding + weighted walk `generate_walks` node2vec) ikut dipindah, semantik unchanged. Fungsi tujuan `compute_random_walk`/`compute_node2vec` tak berubah (tetap seeded deterministik — tes `same_inputs_same_output` dua-duanya hijau; walk generation hanya diperiksa invariannya, tak ada tes yang mengunci nilai raw RNG lama). 2 tes baru (`gds::rng::tests`: in-bounds + determinism lintas bound kecil; unbiased — 1M draw `bound=10` spread bucket < 0.4% vs variant bias lama yang skewing ~7%). Gate `test [akar-core]`: **1,947 total / 0 ignored / 1,947 passed / 0 failed** (1,945 + 2 tes baru); fmt + clippy `-D warnings` bersih. - `09c6c2a`

- **fix(processor) — P79: `PhysicalIntersect::execute_binary` chunk-group split merata; chunk latent di-drop dihilangkan, panic saat `len < num_builds` dicegah (2026-08-30)** — `execute_binary` (akar-processor/src/physical/join_ops.rs:437-460) semula memotong vektor chunk dengan `build_chunks.chunks(len / num_builds)` — untuk `len % num_builds != 0`, sisa chunk terakhir (contiguous, len = remainder) **tidak pernah masuk `build_chunks`** → baris dari chunk itu diam-diam hilang dari hasil intersect (mis. 5 chunk → 2 side, side terakhir hanya dapat 2 chunk, chunk ke-5 dibuang). Kini distribusi merata: `base = len / num_builds`, `extra = len % num_builds`, side pertama `extra` mendapat `base + 1` chunk (`size = base + usize::from(side < extra)`), slicing `build_chunks[start..end]` → seluruh chunk terpakai. Saat `len < num_builds`: side ekstra mendapat 0 chunk → `execute_sides` menangani build table kosong (`build_tables.iter().any(|t| t.is_empty())` → return empty) tanpa panic — sebelumnya slicing chunk ke-0 di-take dari index > len. 2 tes baru (`test_intersect_uneven_chunks_no_drop`: 5 chunk build → 2 side, cross product 3×2=6 baris memastikan chunk ke-5 tak hilang; `test_intersect_less_chunks_than_sides_no_panic`: 1 chunk, 3 side → hasil kosong, tak panic). Gate `test [akar-core]`: **1,949 total / 0 ignored / 1,949 passed / 0 failed** (1,947 + 2 tes baru); fmt + clippy `-D warnings` bersih. - `1256a3a`

- **fix(fts) — P79: `stem_word` branch `-ment` duplikat (dead) dihapus; `-ingly`/`-edly` TETAP dipertahankan (terbukti reachable) (2026-08-30)** — `stem_word` (akar-fts/src/lib.rs) memakai regas `ends_with("ment")` dua kali berturut — branch kedua di L146-147 (memotong `-ment` lalu berhenti) tidak bisa dijangkau karena branch pertama sudah menangani pola yang sama persis → dead branch. Branch `-ment` tunggal kini dikonsolidasi; branch `-ingly`/`-edly` yang diklaim "dead" di catatan audit lama **justru diperlukan**: kata berakhiran `-ingly`/`-edly` berakhiran `gly`/`dly` (bukan `ing`/`ed`), jadi branch `ing`/`ed` sebelumnya gagal memotong — branch `-ingly`/`-edly` (di L120) TAIL-call untuk kembali menangani akhiran `-ing`/`-ed` dan tetap reachable. 1 tes baru (`test_stem_ingly_edly_after_ing`: `stem_word("amazingly") == "amaz"`, `stem_word("reportedly") == "report"` — memastikan `-ingly`/`-edly` tetap dipotong berlapis setelah `-ing`/`-ed`). Gate `test [akar-core]`: **1,950 total / 0 ignored / 1,950 passed / 0 failed** (1,949 + 1 tes baru); fmt + clippy `-D warnings` bersih. - `96f7fda`

- **fix(main) — P79: leaked write-transaction pada early-error path `begin_write_txn` ditutup via rollback (2026-08-30)** — `Connection::begin_write_txn` (akar-main/src/connection/transaction.rs:9-27) memanggil `tm.begin_write()?` (untuk register txn) lalu `self.txn_resources.lock()`; jika lock resources gagal (poisoned oleh panic thread lain), **txn yang sudah terdaftar di TransactionManager bocor tanpa derelease** — `Database::begin_write` memegang write lock sampai txn manual di-drop, sehingga txn berikutnya kelaparan (write lock tak pernah dilepas; state internal listener tidak pernah di-unregister). Kini `let mut txn` di-declare mutable dan pada early-error path `tm.rollback(&mut txn)` dipanggil (melakukan `deregister` + `release_write` — dikonfirmasi di akar-transaction/src/lib.rs:935-946) sebelum return Err → tidak ada txn bocor, write lock selalu dilepas. Level TransactionManager sendiri aman (`begin_write` di lib.rs:789-798 memanggil `acquire_write()?` SEBELUM `register`, jadi tidak ada leak pada level itu — leak murni di connection layer). Tanpa tes baru (jalur error-butuh poison lock sulit di-trigger deterministik); gate `test [akar-core]`: **1,950 total / 0 ignored / 1,950 passed / 0 failed** (tanpa tes baru); fmt + clippy `-D warnings` bersih. - `f02972b`

- **fix(storage) — P79: clippy `field_reassign_with_default` pada 2 tes `null_str` (`test_read_csv_custom_null_str`, `test_read_csv_custom_null_str_nested`) dibersihkan (2026-08-30)** — kedua tes batch d64273e membangun `let mut config = CsvReaderConfig::default(); config.null_str = ...` → clippy `-D warnings` (lint `field_reassign_with_default`) error. Kini memakai struct-update syntax `CsvReaderConfig { null_str: "...".into(), ..Default::default() }` — setara semantik, test-only, tanpa perubahan perilaku. Gate `test [akar-core]`: **1,950 total / 0 ignored / 1,950 passed / 0 failed** (tanpa perubahan jumlah tes; clippy workspace `--all-targets -D warnings` kini bersih penuh); fmt bersih. - `0e374da`

## [0.1.18] - 2026-08-30

### Added

- **feat(ddl/cypher) — P80: `CREATE NODE TABLE` accepted type array-with-dimension `FLOAT[384]` → `List` (2026-08-30)** — a column declared with a capacity (e.g. `embedding FLOAT[384]`) now parses and binds instead of failing, so `akar-server` can self-bootstrap a fresh `Memory`/Metadata schema whose embedding column is declared `FLOAT[384]` (blokir kairos P1). Path A — the engine treats `FLOAT[384]` exactly like the empty form `FLOAT[]`: grammar `type_name` (`akar-core/akar-parser/src/cypher.pest`) adds `array_suffix = { "[" ~ integer? ~ "]" }` after `primitive_type` (accepts `[]` and `[N]`, repeatable; `map_type`/`struct_type`/`union_type` precedence unchanged); the DDL parser already carries the raw `type_name` string (`parser/ddl.rs`) so no change there; the binder `parse_type` (`akar-binder/src/binder/mod.rs`) now maps any primitive followed by a bracket group to `LogicalTypeID::List` (was `ends_with("[]")` only → `FLOAT[384]` errored `Unknown type`). The numeric dimension is intentionally not tracked at engine level — this mirrors the existing Python translator and vector dims remain declared separately via `CREATE VECTOR INDEX ... WITH (dims=N)`. 2 unit tests (parser `test_create_node_table_array_with_dims`; binder `parse_type_array_with_dims_maps_to_list`) + 1 end-to-end integration `test_p80_array_dims_ddl.rs` (create `Memory` with `FLOAT[384]`, insert a list, read back as `Value::List`). Gate `test [akar-core]` default: **0 failed / 0 ignored** across the workspace; fmt + clippy `-D warnings` bersih. Full catalog-level dimension fidelity deferred to P81 (separate, non-blocker).

## [0.1.17] - 2026-08-30

### Added

- **feat(parser/binder) — P74: `LIMIT $limit` / `SKIP $skip` parameterized; kairos tak lagi perlu inline `LIMIT {int(limit)}` (2026-08-30)** — the `limit`/`offset` grammar rules (`akar-core/akar-parser/src/cypher.pest`) now accept a `parameter` in addition to a literal integer, so `RETURN ... LIMIT $limit` (and `SKIP $skip`) parse instead of being rejected/dropped. `parse_limit_skip` (`parser/dml.rs`) emits the parameter name (mutually exclusive with a literal `u64`); the AST and bound `ReturnClause`/`BoundReturnClause` carry `limit_param`/`skip_param` alongside `limit`/`skip`. The bound parameter is resolved to a concrete non-negative `u64` in `substitute_params_in_statement` (`akar-main/src/connection/substitute.rs`) — the same per-execution stage that resolves `$ch`/`$rows` etc. — via new `resolve_limit`/`resolve_skip`/`param_to_u64` helpers (UInt64 accepted, non-negative Int64 accepted, everything else errors). `prepared_statement.rs` now collects `limit_param`/`skip_param` into the declared parameter list so a missing `$limit` still errors, and the AST/bound substitution paths thread the two new fields through. Because resolution happens at the substitution layer, the optimizer/planner/physical stay on a concrete `u64` (`LogicalLimit`/`PhysicalLimit` untouched) — P74.3 satisfied by design. 7 new integration tests (`tests/test_p74_limit_param.rs`): literal control, `LIMIT $limit`, `LIMIT $limit SKIP $skip`, positive `Int64`, missing-param error, negative rejected, non-integer rejected. Gate `test [akar-core]` default: **1,925 total / 0 failed / 0 ignored** (was 1,918 + 7 new tests); fmt + clippy `-D warnings` bersih; doc-check PASS.

## [0.1.16] - 2026-08-29

### Added

- **feat(adbc) — P70: real Arrow translation + parameter binding in ADBC surface (2026-08-28)** — `AdbcStatement::execute_arrow` (`akar-main/src/adbc.rs`) no longer returns a dummy empty batch: it wraps each query-result `DataChunk`'s columnar Arrow arrays into a schema'd `RecordBatch` (one batch per chunk), deriving column names from `field_names` (fallback `column_{i}`) and each column's Arrow type from the underlying array — correct even for all-null columns. `AdbcPreparedStatement::bind` (was a no-op `Ok(())`) now parses each string bind value into a typed `Value` (bool / int64 / float / string) and `execute` runs through `Connection::prepare` + `Connection::execute` so `$name` params are substituted exactly like the wire path; bind arity is validated against the declared parameters. 5 new tests: real batch schema/values, 0-row match (schema-typed empty batch), DDL no-chunks, bind+execute with re-bind, wrong-arity error. Gate `test [akar-core]`: **1,905 total / 0 failed**; fmt + clippy `-D warnings` bersih. [Uncommitted]

- **feat(vector) — P71.1: explicit `CALL vector_similarity_scan(..., query_vector, k)` runs HNSW ANN (2026-08-28)** — the explicit vector ANN read path now actually executes instead of erroring in the table-function registry. `DbStandaloneCallHandler` (`akar-main/src/connection/standalone_call.rs`) gained `execute_vector_similarity_scan_call`: it validates the 4 args, evaluates the query-vector list literal (new recursive helper `eval_ast_expr_to_value_deep`, handles `Expression::List` → `Value::List`), resolves the index by column with fallback to the first index on the table, builds a `PhysicalVectorSimilarityScan` against the catalog's HNSW, and returns its `DataChunk`s. Previously `vector_similarity_scan` was registered as `TableFunction::Custom`, which `FunctionRegistry::execute_table_function` rejects (`registry.rs:1423-1427`) — the call is now routed before the registry. 1 new feature-gated test `call_vector_similarity_scan_runs_hnsw` (`tests/test_vector_index.rs`, `--features vector-extension`): 2 rows inserted, `CALL` returns 2 nearest + Double distance column with both ids. Gate `test [akar-core]` default (no features): **1,905 total / 0 failed**; feature-gated vector suite 6/6. Schema output (field_names / `_id`) deferred to P71.2.

- **feat(vector) — P71.2: schema'd `PhysicalVectorSimilarityScan` output (field_names + `_id` + distance) (2026-08-28)** — the vector-scan output `DataChunk` now carries real column names and an internal row-identity column instead of empty `field_names`. `PhysicalVectorSimilarityScan::execute` (`akar-processor/src/physical/write_ops/vectorsimilarityscan.rs`) output layout is now `[table columns..., distance, _id]` with `field_names = ["id","content","embedding","distance","_id"]` for a 3-column `Memory` table: `distance` is the `Double` HNSW distance at index `num_cols`, and `_id` is the `Int64` physical row offset (the `node_id` HNSW returns) at `num_cols + 1` — located **by name** via `row_id_column_index` (`delete.rs:186`) so DELETE/SET/INSERT/extend machinery can retarget rows, matching the `PhysicalArtIndexRangeScan` precedent (`copyfrom.rs:258-278`). Empty names previously degraded every consumer to synthetic `column_{i}`/`col{i}` headers (server wire `session.rs:331`, CSV `standalone_call.rs:872`, ADBC `adbc.rs:169`); these now surface real headers. Extension of `call_vector_similarity_scan_runs_hnsw` asserts `field_names` exactly and `_id` as Int64. Gate `test [akar-core]` default: **1,905 total / 0 failed**; feature suite 6/6; fmt + clippy `-D warnings` bersih.

- **feat(vector) — P71.3: `CALL vector_similarity_scan` now plans through `LogicalVectorSimilarityScan` (2026-08-28)** — `LogicalVectorSimilarityScan` (`akar-core/akar-planner/src/logical_operator.rs:416`) was never constructed by any binder/planner (only defined + consumed); `CALL vector_similarity_scan(...)` was routed as a bespoke build inside `DbStandaloneCallHandler`. It is now planned: `QueryPlanner::plan_standalone_call` (`planner.rs`) detects `vector_similarity_scan`, evaluates the 4 literal args (table, column, query-vector list via `extract_f64_list`, top_k) and emits a `LogicalOperator::VectorSimilarityScan` that flows through the normal operator pipeline — mapper `map_scan.rs:123` resolves the HNSW index **by column** from `ctx.table_catalog` (fallback first-on-table, same convention as the previous handler) and constructs/executes `PhysicalVectorSimilarityScan`. The logical struct now carries `table_name` + `column_name` (concrete index name derived downstream at mapping time), ready for P71.4's SQL-pattern rewrite. The `P71.1` handler-build path is superseded by the pipeline; output schema (`field_names`/`_id`/distance) unchanged from P71.2. Gate `test [akar-core]` default: **1,905 total / 0 failed**; feature suite 6/6; fmt + clippy `-D warnings` bersih.

- **feat(vector) — P71.4: `VectorSimilarityDetection` pass aktif; `MATCH ... WHERE cosine_similarity(...) > thr ORDER BY ... LIMIT k` di-rewrite ke ANN scan (2026-08-28)** — the flat optimizer pass `VectorSimilarityDetection` (`akar-core/akar-optimizer/src/passes/flat/vector_similarity.rs`) is no longer a literal no-op. It detects the canonical pattern `[ScanNode(pred = cosine_similarity(var.col, [q]) >/= thr), OrderBy(cos DESC), Projection, Limit(k)]` — any extra conjunct, an ASC sort, a non-matching query vector, or a missing `LIMIT` is left alone — and rewrites `ScanNode` → `[VectorSimilarityScan(alias = scan alias), Filter(cos > thr), OrderBy, Projection, Limit]`, preserving the threshold, the RETURN projection and the `_id`/distance columns (FilterPushDown pass 2 already absorbed the single predicate into `ScanNode.predicate`, which the pass consumes). Two bugs found + fixed along the way: (1) the `Filter` was originally emitted *before* the scan so it ran on a dummy input — now `[VectorSimilarityScan, Filter, ...]`; (2) the physical `PhysicalVectorSimilarityScan` built its output List (FLOAT[]) columns via the legacy `ValueVector`/`from_legacy` path, which collapsed `m.embedding` to NULL on read-back, so the downstream cosine filter dropped every row — the operator now encodes every column (including `embedding` and the `distance`/`_id` tail) through `arrow_array_from_values` (`akar-common/src/arrow_vector.rs`), which turns `Value::List` into a proper `ListArray`. `LogicalVectorSimilarityScan` gained `alias: Option<String>` (the `CALL` path passes `None`; the MATCH rewrite passes the scan alias), and `map_scan.rs` prefixes the scan's `field_names` with that alias so the filter/order-by re-evaluation resolves `m.embedding`. 5 new pass unit tests (pattern fire / no-fire on extra conjunct, ASC, different query vector, no LIMIT) + end-to-end `match_vector_similarity_rewrites_to_vector_scan` (`tests/test_vector_index.rs`, `--features vector-extension`): 2 rows, query `[0.9,0.1]` with threshold `0.5` keeps only id 1 (cos ≈ 0.994), threshold `0.0` keeps both in DESC order id 1 then id 2. Gate `test [akar-core]` default (no features): **1,909 total / 0 failed**; feature-gated vector suite 7/7; fmt + clippy `-D warnings` bersih. Committed `6158eab`.

- **feat(vector) — P71.5: cosine scale-invariance end-to-end untuk jalur baca ANN (MATCH rewrite) (2026-08-28)** — pointer P71.5 #1 diselesaikan: unit-level scale-invariance sudah ada sejak P51.46 (`DistanceMetric::Cosine::compute` & `cosine_similarity` menormalkan KEDUA operan), tapi belum dibuktikan di jalur baca ANN yang baru aktif (P71.4) — di mana HNSW metric & evaluasi ulang `cosine_similarity` pada filter/ORDER BY memakai stored vector RAW non-unit-norm. Test end-to-end baru `match_vector_scan_cosine_scale_invariant` (`tests/test_vector_index.rs`, `--features vector-extension`) menginsert dua arah yang sama seperti test P71.4 tapi di-scale (`[10,0]` naik, `[0,0.5]` turun) dan menegaskan threshold/urutan identik dengan kasus unit-norm — membuktikan read path scale-invariant konsisten. Pointer P71.5 #2 (`n_scan`/ef) & #3 (output schema) **tetap deferred** sesuai gate-nya (perlu bukti profil / kasus konkret). Hanya test ditambah, tanpa kode produksi; gate default `test [akar-core]` tak berubah (**1,909 total / 0 failed**, test baru feature-gated); feature suite 8/8; fmt + clippy `-D warnings` bersih.

- **feat(ddl) — P72: `CREATE [NODE|REL] TABLE IF NOT EXISTS` di-parse & idempotent di binder + physical (2026-08-29)** — the parser no longer rejects the `IF NOT EXISTS` clause on `CREATE NODE TABLE` / `CREATE REL TABLE`: `create_node_table`/`create_rel_table` grammar (`akar-core/akar-parser/src/cypher.pest`) gains optional `if_not_exists?` (reusing the existing `if_not_exists` rule), the AST structs `CreateNodeTable`/`CreateRelTable` (`ast.rs`) carry a `pub if_not_exists: bool`, and `parse_ddl` (`parser/ddl.rs`) sets it — previously this clause errored with `expected or_replace, index_type, or node_pattern`, so kairos `_ensure_dream_schema` logged an error on every init. The flag threads the whole pipeline so the create is a **no-op when the table already exists**: the binder (`akar-binder/src/binder/mod.rs`) treats `CatalogResult::AlreadyExists` with `if_not_exists` as a skip (via `BoundCreateNodeTable`/`BoundCreateRelTable.if_not_exists`), the planner (`akar-planner/src/planner.rs` + `LogicalCreateNodeTable`/`LogicalCreateRelTable`) forwards it, and the physical executor (`akar-processor/src/processor/mapper/map_ddl.rs`) short-circuits before `create_art_index`/storage create when `get_node_table_by_name`/`get_rel_table_by_name` finds the table — preventing metadata/storage desync. Query without the clause still errors on duplicates (unchanged). 2 new parser tests (`test_create_node_table_if_not_exists`, `test_create_rel_table_if_not_exists`) + 2 end-to-end idempotency tests (`test_create_node_table_if_not_exists_idempotent` verifies re-create is a no-op and existing data is untouched; `test_create_rel_table_if_not_exists_idempotent` verifies the same for rel tables and that the non-clause form still errors). Gate `test [akar-core]` default: **1,913 total / 0 failed / 0 ignored** (was 1,909 + 4 new tests); fmt + clippy `-D warnings` bersih.

### Fixed

- **chore(lint) — `akar-storage` `ArtIndex::lookup`: match-on-option → `?` (clippy `question_mark`) (2026-08-29)** — pre-existing clippy `-D warnings` gate failure (surfaced by current toolchain): the `match current.get_child(byte) { Some(child) => ..., None => return None }` in `art_index.rs:171` is replaced with `current = current.get_child(byte)?; depth += 1;` — identical semantics, gate clean.

- **fix(server) — Windows: accepted sockets inherit nonblocking from the listener; idle-session busy-spin pegged all CPU cores (2026-08-29)** — `akar_server` busy-spun to ~100% CPU (kairos production: 168 idle Hermes connections → ~1,200% CPU, 12-core host pegged). Root cause: the accept loop sets the listener nonblocking (`akar-server/src/lib.rs`), and on Windows the accepted `TcpStream`s **inherit** that mode; `set_read_timeout` is ignored on nonblocking sockets, so every session's `read_frame` returned `Err(WouldBlock)` immediately and `handle_client`'s loop (`session.rs:75-78`) continued without parking → one spinning thread per connection. Repro: 2,091,921 `WouldBlock`/sec per connection with inherited nonblocking vs ~9 wakeups/sec after fix. Fix: `handle_client` now forces `stream.set_nonblocking(false)` before setting read/write timeouts, so blocked reads park on the 250 ms timeout instead of spinning. Verified: 80 idle clients → 0.0% avg CPU (peak 2.3% of one core); `cargo check -p akar-server` + 40/40 tests clean.

- **fix(write) — P73.0+P73.1: batch `UNWIND $rows AS row CREATE (... row.field ...)` — premis "menulis 0 rows" terpatahkan; kontrak output `PhysicalInsertNode` diperbaiki (2026-08-29)** — pengujian end-to-end membuktikan **tulisannya benar** (write persist) di path main-connection dan server wire (kairos production) untuk node AND rel: `UNWIND $rows AS row CREATE (... row.id ...)` menulis 2 node (`readback [[1,"a"],[2,"b"]]`); `UNWIND … MATCH … CREATE (a)-[:R {...}]->(b)` menulis 2 edge. Operator yang dirujuk investigasi plan lama ternyata **dead code**: `PhysicalCreateNode`/`PhysicalCreateRel` (`recursiveextend.rs`) tidak di-wire ke mapper — operator aktual adalah `PhysicalInsertNode`/`PhysicalInsertRel` (`insert.rs` via `map_update.rs`). **Root cause sebenarnya = kontrak output operator tulis:** `PhysicalInsertNode::execute` mengeluarkan `DataChunk` 2 kolom panjang-beda (count len=1, ids len=N) tanpa kolom variabel output → result query "1 rows in 2 columns" dan `RETURN n.id,n.name` → garbel `[[Int64(2), Int64(0)]]`; `PhysicalInsertRel` hanya count len=1 → RETURN collapse ke 1 baris. **P73.1 fix:** `PhysicalInsertNode` kini mengeluarkan chunk well-formed satu baris per node (kontrak konsisten) — kolom `_id` (row offset utk OCC `record_insert_writes`, konvensi sama dengan MERGE/set.rs) + kolom property node ter-bind ke `out_var_name` (`n.id`, `n.name`) via `physical_type_from_logical` + `ValueVector::set_value`, dengan kolom kompleks (List/Struct) di-skip bila tak ter-materialisasi. Efek: CREATE no-RETURN melapor `N rows` benar, `RETURN n.id,n.name` mem-proyeksi nilai asli (`[[1,"a"],[2,"b"]]`), OCC tetap jalan. 5 tes baru: `test_p73_unwind_create_param.rs` (4) + `test_server_unwind_param_create_persists` (server). Gate `test [akar-core]` default: **1,918 total / 0 failed / 0 ignored** (was 1,913 + 5 new tests); fmt + clippy `-D warnings` bersih.

- **fix(write) — P73.2: kontrak output `PhysicalInsertRel` diperbaiki — `CREATE ... RETURN` rel mem-proyeksi nilai asli & no-RETURN melapor N rows (2026-08-29)** — menuntaskan P73 untuk sisi rel. `out_var_name` ditambahkan ke `LogicalCreateRel` (dari `edge.variable`, anonim → `""`) dan di-thread ke `PhysicalInsertRel` (`map_update.rs`), lalu `PhysicalInsertRel::execute` (`insert.rs`) mengeluarkan chunk well-formed satu baris per rel menggantikan kolom count len=1 yang membuat `RETURN a.id,b.id,r.weight,r.type` collapse ke `[[Int64(2)]]`. Output kini = kolom `_id` (edge offsets, ditempatkan **terdepan** supaya OCC `record_insert_writes` resolve `_id` eksak sebelum `.*_id` node pass-through) + **pass-through seluruh kolom input** (sehingga `a.id`, `b.id` resolve) + kolom property rel ter-bind ke `out_var_name` (`r.weight`, `r.type`) via `physical_type_from_logical` + `ValueVector::set_value`, kolom kompleks (List/Struct) di-skip bila tak ter-materialisasi (`tracing::warn!`). Efek terverifikasi di `param_unwind_createrel_batch_persists` (diperkuat dengan assert eksplisit): no-RETURN summary `Returned 2 rows in 9 columns` (sebelum 1 rows/1 columns), RETURN mem-proyeksi `[[1,2,0.5,SUPPORTS],[2,1,0.4,BRIDGE]]`, write 2 edge persist; sisi plus, rel kini tercatat di OCC `record_insert_writes`. **P73.3**: coverage integrasi kairos-friendly (node+rel, no-RETURN & RETURN) sudah tercakup di `test_p73_unwind_create_param.rs`; gate `test [akar-core]` default **1,918 total / 0 failed / 0 ignored** (tanpa tes baru di batch ini — assert diperkuat; total tetap); fmt + clippy `-D warnings` bersih; SPEC/CHANGELOG/plan direconcile. `1f81a54` (P73.0+P73.1) + commit batch 2.

## [0.1.15] - 2026-08-27

### Added

- **feat(server) — P69: array-of-object parameter support (2026-08-26)** — `json_value_to_akar_value` (`session.rs`) now maps JSON objects → `Value::Struct` (was: `"Object parameters are not supported"`), recursively inside arrays. `value_to_expression` (`prepared_statement.rs`) maps `Value::Struct` → `Expression::Map` and `Value::Map` → `Expression::Map`, so `UNWIND $batch AS row ... row.field` works end-to-end for batched row objects (afe/dae/dream batch-write pattern). Known gap (documented in test): `UNWIND $rows AS row CREATE (... row.field ...)` writes 0 rows due to a complex-value pipeline limitation in `PhysicalUnwind → PhysicalCreateNode` (RETURN-based field access works). 2 new server tests: `test_parameterized_array_of_objects_unwind`, `test_parameterized_array_of_objects_dml`. Gate `test [akar-core]`: 0 failed.

- **docs — README.md for 4 published crates missing them (2026-08-26)** — `akar-dream`, `akar-search`, `akar-ml`, `akar-migrate` were `publish = true` in `PUBLISH_ORDER` but had no `README.md` (crates.io page would render empty). Added READMEs following the established pattern (description + components + usage + test count). `doc-check.py` (root tools) gained a rule: `publish = true` crates must carry a `README.md` or explicit `readme =` field — verified to flag a missing one and clear once added.

- **feat(server) — P68: `--checkpoint-threshold` flag in akar-server (2026-08-26)** — `akar_server` gained a `--checkpoint-threshold <bytes>` CLI flag (default **16 MiB**) replacing the hardcoded `checkpoint_threshold: -1`. With `-1`, `maybe_checkpoint` fired on EVERY write → `checkpoint_with_drain` → `persist_all_tables` rewrote all column mirrors ≈ **1s/write** and serialized reads behind the write lock (kairos finding). Default is now a positive WAL-size threshold (`wal_size() > threshold`), so checkpointing happens only when the WAL grows past it. `0` disables auto-checkpoint; `-1` restores checkpoint-per-write. Embedded `SystemConfig::default()` remains `-1` (unchanged behavior + tests). Gate `test [akar-core]`: 0 failed.

### Fixed

- **fix(storage) — P61: fail-loud WAL recovery (2026-08-27)** — WAL recovery no longer falls back to a fresh, empty database on a corrupt/replay error. That fallback silently destroyed every committed write still living only in the WAL (`Database::new` → `storage_manager.recover()`). It now fails open: `Database::new` returns `Err` ("Refusing to start with an empty database") and the WAL file is left untouched so an operator can repair it. Persist-before-checkpoint and post-replay persist errors likewise abort before WAL truncation instead of warning. Prepared CREATE/MERGE DML now journals a self-sufficient I/U record + COMMIT (was: zero WAL records → rows silently lost on crash). `SystemConfig::default()` behavior unchanged. 3 crash-recovery tests updated to assert the fail-loud contract (`test_wal_zeroed_out_recovery`, `test_wal_random_bytes_recovery`, `test_wal_single_byte_recovery`). Gate `test [akar-core]`: 0 failed.

## [0.1.14] - 2026-08-26

### Added

- **feat(server) — P66: `dream_control` op in akar-server (2026-08-26)** — `WireRequest` gains `action: Option<String>` field; akar-server dispatch now routes `"dream_control"` to a stub handler returning `not_available` (DreamBackend integration deferred). Empty/missing action defaults to `"status"`. `RemoteDatabase::dream_control(action)` added for Rust clients. kairos `_rpc_json` routes the op to the wire instead of raising locally. 2 new server tests: `test_dream_control_status`, `test_dream_control_default_action`. Gate `test [akar-core]`: **1,903 total / 0 failed**.

### Fixed

- **fix(storage) — P67: checkpoint drain timeout under concurrent writers (2026-08-26)** — `commit_transaction` no longer passes a drain function to `maybe_checkpoint` for auto-checkpoints. The drain waited up to 30s for all active transactions to leave before checkpointing — always timing out with concurrent writers (Hermes gateway + kairos client). Skipped because: the committing txn is already deregistered from lifecycle, other transactions' writes live in their local buffers (not global WAL/BM), and the WAL handles crash recovery regardless. Explicit `CHECKPOINT` commands still drain. 1 new server test: `test_concurrent_writes_no_drain_timeout`. Gate `test [akar-core]`: **1,904 total / 0 failed**.

- **feat(binder/server) — P65: server-side array parameter binding (2026-08-26)** — `json_value_to_akar_value` (`session.rs`) converts JSON `Array` → `Value::List` recursively, enabling array params to flow through the prepared-statement pipeline end-to-end. `cell_value` (`session.rs`) handles `PhysicalTypeID::List` → `Value::List` so arrays round-trip from storage back to the wire. `value_to_expression` (`prepared_statement.rs`) converts `Value::List` → `Expression::List` for parameter substitution; `substitute_params` uses it in the `Parameter` arm. `bind_unwind` (`binder/mod.rs`) now accepts `Expression::Parameter` (resolved at substitute time), so `UNWIND $arr AS x` works with array params. 5 new server tests: `test_int_array_unwind_binding`, `test_float_array_unwind_binding`, `test_string_in_array_binding`, `test_mixed_scalar_and_array_params`, `test_empty_array_param`. Gate `test [akar-core]`: **1,901 total / 0 failed**.

## [0.1.13] - 2026-08-26

### Added

- **feat(server) — P64: parameter binding in wire protocol (2026-08-26)** — `WireRequest` gains an optional `params: Option<HashMap<String, serde_json::Value>>` field. When present and non-empty, `akar-server` executes the query through the prepared-statement pipeline (`conn.prepare` + `conn.execute`) with JSON→Akar `Value` conversion, instead of the plain string query path. `RemoteDatabase` gains `query_with_params(query, params)` for Rust clients. kairos `KuzuClientStore._rpc_json` now forwards the `params` dict from `_execute` to the wire. Fixes `RETURN $x` with `{"x":42}` returning `null` in daemon mode (kairos Finding #26). 6 new server tests: `test_parameterized_return_value`, `test_parameterized_string_param`, `test_parameterized_bool_and_null`, `test_parameterized_float_param`, `test_parameterized_dml_with_params`, `test_empty_params_falls_back_to_plain_query`. Gate `test [akar-core]`: **29/29 passed** (akar-server suite).

## [0.1.12] - 2026-08-25

### Changed

- **chore(deps) — align all akar-* dep version specs to actual crate versions (2026-08-25)** — 100+ stale `version =` fields across 35 Cargo.toml files updated to match actual published/inherited versions (e.g. akar-main deps: akar-algo 0.1.0→0.1.6, akar-storage 0.1.3→0.1.7, akar-processor 0.1.2→0.1.5). Workspace resolver ignores these (uses path), but stale specs are misleading for crates.io publishing and IDE hints. All `optional = true` flags preserved. `cargo check` + `cargo test -p akar-main -p akar-processor` pass.

### Fixed

- **fix(processor) - P63: rel-table traversal crashes on strings >255 bytes (2026-08-25)** - `PhysicalExtend` (`recursiveextend.rs`), `PhysicalOptionalExtend` (`optionalextend.rs`), dan `PhysicalPackedExtend` (`packedextend.rs`) menyalin kolom node/rel melalui `ValueVector` legacy yang meng-hard-cap string inline di 255 byte (`store_value_in_vector`/`push_string` di `akar-common/src/vector.rs`), sehingga `MATCH (a:Memory)-[r:Connected]->(b:Memory)` gagal dengan `Cannot store string of N bytes: inline string storage limit is 255 bytes` bila ada node dengan properti string > 255 byte (mis. `Memory.content`). Node-scan sudah handle string panjang via Arrow; hanya jalur extend yang belum. Fix: kolom `String` (plus List/Array/Struct yang sudah lama) dialihkan ke `build_arrow_from_values` (StringBuilder Arrow, tanpa batas inline). Regresi: `test_rel_scan_long_string_dest_property` di `test_p5312_rel_scan_binding.rs` (content 600 byte, dulu crash kini survive + count). Ditemukan via kairos legacy import (kairos Finding #8). Gate `test [akar-core]`: paket akar-main + akar-processor **0 failed** (rel-scan & integration suites hijau).

## [0.1.11] - 2026-08-25

### Added

- **feat(server) — P62: akar-server daemon binary (2026-08-25)** — `akar-server` sekarang ship binary `akar_server` (src/bin/akar_server.rs) dengan CLI berbasis clap: `--db`, `--port`, `--addr`, `--auth-token`, `--idle`, `--json-sidecar`, `--read-only`. Fitur: (1) **auth token** — koneksi wajib kirim token di frame pertama; tanpa `--auth-token` semua client diterima; (2) **idle timeout** — monitor thread shut down server setelah tidak ada request selama N detik; (3) **sidecar JSON** — tulis metadata (protocol, host, port, token, pid, started_at) ke file JSON saat startup, hapus saat shutdown; (4) **wire ops baru** — `ping`, `flush`, `stats` (query count + uptime), `export`, `shutdown` via `WireRequest.op`; (5) **graceful shutdown** via `ctrlc` handler + shutdown op. Gate `test [akar-core]`: **1,889 passed / 0 failed / 0 ignored** (121 suite). Akar-server: 12 existing + 11 new tests = 23/23.

- **test(vector) — regression scale-invariance cosine metric (2026-08-25, P51.46)** — `DistanceMetric::Cosine::compute` (`akar-vector/src/hnsw.rs:76-84`) dan `cosine_similarity` (`lib.rs:129-140`) menormalkan KEDUA operan saat compute — stored vector TIDAK diasumsikan pre-normalized oleh jalur tulis (HNSW menyimpan raw vector; index cosmetik `PhysicalVectorSimilarityScan` = dead code per AGENTS.md §5). Tes baru `test_cosine_metric_is_scale_invariant`: mengubah skala stored vector (0.5/2/10/0.001) tidak mengubah distance kosinus; zero-norm → distance 1.0 (guard tanpa panic). Mutan (normalisasi hanya query) akan membuat scaling mengubah hasil — test ini menangkapnya. Gate `test [akar-core]`: **1,878 passed / 0 failed / 0 ignored** (121 suite). Metrik tes `SPEC.md` direkonsiliasi 1.872 → 1.878.

- **test(parser) — AST-based DETACH/ORDER BY/star + whitespace-tolerant COUNT(*) (2026-08-25, P51.45)** — tiga text-sniffing di parser diganti proper AST check via named grammar rules: `star = { "*" }` (deteksi `count(*)` tanpa `replace(" ","")`), `detach_kw = { "DETACH" }` (flag detach dari parse tree, bukan `starts_with`), `sort_dir = { "ASC" | "DESC" }` (arah sort dari token, bukan `ends_with("DESC")`). Grammar case-sensitive dipertahankan. Tes baru: `test_count_star_whitespace_padded` (`COUNT( * )`/`( *)`/`(* )`), `test_detach_delete_flag_via_ast` (DETACH vs plain DELETE), `test_order_by_desc_via_ast` (DESC/ASC/default). Gate `test [akar-core]`: **1,878 passed / 0 failed / 0 ignored** (121 suite). Metrik tes `SPEC.md` direkonsiliasi (akar-parser 88 → 91).

- **test(storage) — spiller restore + NodeTable batch insert dengan spiller (2026-08-25, P51.44)** — `NodeGroup::restore_spilled` (merge spill files + in-memory kembali ke kolom, cleanup file) + `NodeTable::set_spiller` meng-attach spiller ke NodeGroups selama bulk insert; `test_restore_spilled_reconstructs_full_group` (threshold 64B memicu spill di tengah 20 baris, restore mengembalikan 20 baris lengkap) dan `test_node_table_batch_insert_with_spiller_restores_all_rows` (2000 baris batch, seluruh baris terbaca setelah restore). Gate `test [akar-core]`: **1,878 passed / 0 failed / 0 ignored** (121 suite). Metrik tes `SPEC.md` direkonsiliasi (akar-storage 343 → 345).

### Changed

- **chore(release) — v0.1.11: akar-algo 0.1.5→0.1.6 (2026-08-25)** — bump patch akar-algo (tracing fix + P51.44–P51.46 KISS/DRY refactor). Tag **v0.1.11** + GitHub Release. Lainnya tidak di-bump: akar-server (P62, non-breaking, `publish = true` tapi belum di-publish ke crates.io) dan akar-python (`publish = false`, hanya PyPI).

- **chore(release) — akar-storage 0.1.6→0.1.7 + akar-main 0.1.6→0.1.7 + akar-server 0.1.2→0.1.3 (2026-08-25)** — bottom-up publish ke crates.io: **akar-storage** 0.1.7 (P51.44 set_spiller/live spiller plumbing) → **akar-main** 0.1.7 (P62 WireRequest/WireResponse/ServerStats types, P51.44 spiller wiring) → **akar-server** 0.1.3 (P62 daemon binary: clap CLI, auth token, idle timeout, sidecar JSON, wire ops ping/flush/stats/export/shutdown, graceful shutdown via ctrlc). Non-breaking; existing wire protocol tidak berubah.

- **P51.44 — KISS: `auto_checkpoint` dead config & spiller plumbing mati dibuat live (2026-08-25)** — (1) `auto_checkpoint` di `SystemConfig` sebelumnya TAK PERNAH dibaca engine (`maybe_auto_checkpoint` hanya baca `checkpoint_threshold`); kini di-wire sebagai master switch: `auto_checkpoint=false` → skip auto-checkpoint regardless of threshold (non-breaking, default `true`). (2) spiller plumbing (dead: `Database::spiller()` 0 caller, `NodeGroup::set_spiller`/`with_spiller`/`flush_with_spiller` tak pernah ter-trigger) kini ter-attach: `StorageManager` memegang `spiller: RwLock<Option<Arc<Spiller>>>` + `set_spiller`/`spiller()`, di-set dari `Database::new` (config) dan `Database::set_spill_threshold` (runtime override); `create_node_table`/`restore_node_table` meng-attach spiller ke tabel; `NodeTable::insert_row_with_txn`/`insert_rows_batch_with_txn` menempelkan spiller ke NodeGroup baru saat pembuatan; `append_row_with_txn` auto-spill ketika chunk melebihi threshold (mekanisme yang sudah ada), lalu `restore_spilled` mem-merge data kembali ke kolom in-memory di akhir insert sehingga scan & column mirror tetap otoritatif (tidak ada data yang hilang). `SET spill_threshold` kini fungsional untuk bulk ingest. Spiller juga diberi impl `Clone` manual (AtomicU64 tak Clone). Gate `test [akar-core]`: **1,878 passed / 0 failed / 0 ignored** (121 suite); fmt + clippy `--all-targets -D warnings` bersih. SPEC §11 direkonsiliasi.

- **chore(python) — `akar` 0.1.3 di PyPI: engine refresh tanpa perubahan API (2026-08-24)** — wheel `akar-0.1.3-cp39-abi3-win_amd64.whl` + sdist di-upload via twine ([pypi.org/project/akar/0.1.3](https://pypi.org/project/akar/0.1.3/)); membundel engine Rust baru sejak wheel 0.1.2: security fix IMPORT DATABASE path traversal, WAL durability P60.1/P60.2, perf LPA/Louvain (−24..−53%), adaptasi rand 0.10. Permukaan Python tidak berubah (`akar.search`/`akar.spread`/`akar.dream`, `Database`/`Connection`/`QueryResult`). Verifikasi: `uv run --with akar==0.1.3` — CREATE NODE TABLE + CREATE + MATCH OK; seluruh submodule & binding hadir (catatan penamaan pre-existing: fungsi ter-ekspor sebagai `hybrid_search_py`/`weighted_rrf_fuse_py`). Versi bump di `pyproject.toml` + `Cargo.toml`.

- **docs — rekonsiliasi seluruh dokumentasi proyek ke state aktual (2026-08-25, tanpa id task)** — sinkronisasi komprehensif semua dokumen terhadap kode aktual: (1) `CHANGELOG.md` [0.1.10] kategori diperbaiki ke urutan KAC yang benar (Added → Changed → Fixed); (2) `SPEC.md` §5.1 Logical Types dikoreksi — 4 tipe hilang ditambahkan (DECIMAL, UINT128, JSON, TIME), 2 tipe phantom dihapus (NULL, RDF_VARIANT), total 37 terverifikasi terhadap `LogicalTypeID` enum; (3) 13 per-crate `README.md` test count diperbarui dari ground truth `cargo test -- --list` (mis. akar-parser 67→91, akar-binder 87→101, akar-processor 142→157, akar-algo 34→53); (4) `.github/CODEOWNERS` path basi C++ diganti path aktual Rust; (5) `.github/ISSUE_TEMPLATE/bug.yml` placeholder versi diperbarui; (6) `BENCHMARK_COMPARISON.md` judul dikoreksi; (7) `RELEASE.md` versi hardcoded diganti placeholder. Tanpa perubahan perilaku; gate tidak terpengaruh.

### Fixed

- **fix(algo) — koreksi tracing log jumlah registrasi algoritma graph (2026-08-25)** — `tracing::info!` di `AlgoExtension::init` (`akar-algo/src/lib.rs:871`) menampilkan "15 algorithms × canonical + 15 aliases" padahal aktual 17 canonical + 13 aliases = 30 registrasi. Jumlah canonical dihitung ulang dari `register_table_function`: page_rank, wcc, scc_tarjan, scc_kosaraju, k_core, louvain, spread_activation, spanning_forest, lpa, betweenness, closeness, triangle_count, all_sp_destinations, random_walk, node2vec, shortest_path, weighted_shortest_path (17). — `docs-reconciliation`

- **chore(python) — `akar` 0.1.4 di PyPI (2026-08-25)** — wheel `akar-0.1.4-cp39-abi3-win_amd64.whl` di-upload ([pypi.org/project/akar/0.1.4](https://pypi.org/project/akar/0.1.4/)); membundel engine Rust baru (akar-algo 0.1.6 tracing fix + semua perubahan v0.1.10). Permukaan Python tidak berubah.

## [0.1.10] - 2026-08-24

### Added

- **test(algo) — node2vec: +7 tes & pemecahan fungsi jadi fase ter-tes (2026-08-24, tanpa id task)** — melanjutkan batch 11 tes sebelumnya di `akar-algo/src/gds/node2vec.rs`: `compute_node2vec` dipecah behavior-preserving menjadi `generate_walks(csr, p, q, walks, window, rng)` + `train_embeddings(embeddings, walks, n, dims, window, rng)`; urutan draw RNG seeded identik sehingga semua golden test lama tetap valid, dan parameter `n` dieksplisitkan (mengikuti semantik asli `num_nodes`) alih-alih diturunkan dari `embeddings.len()/dimensions`. Tes baru: `walks=0` dan `window=0` keduanya menghasilkan persis barisan init LCG seed 42 (guard off-by-one loop walk & slice konteks); invarian generasi walk (tiap hop lewat edge CSR asli via `neighbors`, panjang ≤ window, tepat `walks` replika per seed node — sampingan: terkonfirmasi `CSRAdjacency::build` menghasilkan adjacency undirected); simpul isolat → walk single-step; **cek SGD langsung** dot(u,v) naik dari baseline −1 setelah positive-sample update — satu-satunya guard yang mendeteksi loop training yang ter-skip; parameter bias ekstrem `p,q ∈ {0, ∞}` tetap finite & bit-reproducible (jalur bobot ±inf + fallback NaN); self-loop & paralel edge aman. Mutation check penting: assertion awal `output ≠ init replay` ternyata vakum (generasi walk sudah mengonsumsi RNG sebelum init sehingga mutant training-skip lolos) — diganti cek langsung `train_embeddings` yang lumpuh tepat pada mutant tersebut (mutan di-revert bersih). Gate `test [akar-core]`: **1,867 passed / 0 failed / 0 ignored** (121 suite); fmt + clippy `-p akar-algo --all-targets -D warnings` bersih. Run config cepat `Test gds::node2vec::tests` ditambahkan (`.idea`, gitignored). Metrik tes `SPEC.md` direkonsiliasi 1.860 → 1.867.

- **test(algo) — 11 unit tests `compute_node2vec` (2026-08-23, tanpa id task)** — modul `#[cfg(test)]` inline baru di `akar-algo/src/gds/node2vec.rs` (sebelumnya hanya smoke test panjang `lib.rs` + 2 tes jalur CALL). Cakupan: dimensi output flat = nodes × dimensions untuk dims 1/2/8 + kontrak name `"node2vec"`/metadata none; edge case graf kosong, single node, simpul isolat tetap mendapat baris embedding ter-inisialisasi, dan `dimensions=0` → output kosong tanpa panic; invarian semua nilai finite setelah training (siklus 4-simpul) dan dengan parameter bias `p=0.25, q=2.0` (segitiga); determinisme bit-identik pada graf bintang yang mengeksekusi biased sampler + negative sampling; golden value `window=1` → walk tiap start panjang 1 sehingga tidak ada draw RNG saat generasi walk maupun pasangan konteks, output persis barisan init LCG seed 42 yang di-replay independen di tes; dan dua tes exact-value helper SGD privat `update_embedding` (e_u=[1,0], e_v=[0,1]: target positif grad=+0.0125 → `[1.0, 0.0125, 0.0125, 1.0]` menaikkan dot product; target negatif → `[1.0, -0.0125, -0.0125, 1.0]` membalik tanda dot). Mutation check: seed `42→43` melumpuhkan tepat golden-init test; flip tanda gradien `target-prob → prob-target` melumpuhkan tepat kedua tes `update_embedding` (keduanya di-revert bersih via git diff). Gate `test [akar-core]`: **1,860 passed / 0 failed / 0 ignored** (121 suite); fmt `-p akar-algo -- --check` + clippy `-p akar-algo --all-targets -D warnings` bersih. Metrik tes `SPEC.md` direkonsiliasi 1.849 → 1.860.

- **test(json) — edge-case tests `json_type_of` (2026-08-23, tanpa id task)** — menutup gap cakupan error-path di `test_json_type_of` (`akar-json/src/lib.rs`): fungsi mengembalikan `Result<&'static str, String>` tetapi tesnya hanya mengeksekusi 6 jalur sukses tanpa satu pun cabang `Err`. Empat assertion baru: JSON malformed `{invalid}`, string kosong, sampah trailing setelah nilai valid (`42 42` — menjaga semantik EOF-striktnya `serde_json::from_str`), dan varian angka scientific notation `-3.14e2`. Mutation check: mengganti `map_err(|e| ..)?` dengan `unwrap_or(Value::Bool(true))` membuat tepat assertion `is_err()` baru yang gagal menangkap mutant (mutant di-revert bersih). Gate `test [akar-core]`: **0 failed** (121 suite); fmt `-p akar-json -- --check` + clippy `-p akar-json --all-targets -D warnings` bersih. Jumlah tes tidak berubah (assertion ditambahkan ke tes existing).

- **test(function) — 6 error-path tests date scalar functions (2026-08-23, tanpa id task)** — menutup gap cakupan error-path `akar-function/src/scalar/date.rs`: op `MakeDate` sebelumnya TIDAK punya tes sama sekali. Tes baru di `akar-function/tests/scalar_tests.rs`: happy path `make_date(2023, 6, 15) → Date(19523)` + leap day diterima di tahun kabisat; bulan 13 & 0 ditolak dengan pesan eksak `Invalid month: {n}`; `make_date(2023, 2, 30)` dan day 0 ditolak `Invalid date` (jalur `from_calendar_date`); argumen <3 → `requires 3 arguments`; bulan non-integer → `month must be integer`; `date_trunc` unit tak didukung (`decade`) → `not supported` + kurang argumen → `requires 2 arguments`; `date_part` part tak dikenal → `Unknown date_part`. Mutation check: mengganti validasi `Month::try_from(month_val).map_err(...)?` menjadi `unwrap_or(Month::January)` membuat month 13 senyap menjadi Januari — tepat `test_make_date_invalid_month` yang gagal menangkapnya (mutant di-revert bersih via git checkout). Gate `test [akar-core]`: **1,849 passed / 0 failed / 0 ignored** (121 suite); fmt `-p akar-function -- --check` bersih. Metrik tes `SPEC.md` direkonsiliasi 1.843 → 1.849. Temuan sampingan tercatat di backlog plan: `akar-function/src/scalar_tests.rs` orphan (tak pernah dideklarasikan `mod` di lib.rs — ~130 tes duplikat yang tak pernah dikompilasi).

- **test(algo) — 8 unit tests `compute_random_walk` (2026-08-23, tanpa id task)** — modul `#[cfg(test)]` inline baru di `akar-algo/src/gds/random_walk.rs` (sebelumnya hanya smoke test panjang+sum di `lib.rs`). RNG ter-seed (LCG seed 42) dan simpul bertetangga tunggal membuat `gen_range(1)` selalu 0 sehingga walk sepenuhnya deterministik → nilai eksak bisa di-assert: hit persis `[3.0, 2.0]` pada path 2-simpul, `steps=0` hanya menghitung kunjungan awal (`[2.0, 2.0, 2.0]`), simpul isolat break segera (`[0.0, 0.0, 3.0]`), start out-of-bounds di-skip senyap + kontrak name/metadata, invarian total hits pada siklus (= nodes × walks × (steps+1) = 96), linearitas `walks_per_node` (w=10 ≡ tepat 2× w=5), graf kosong → values kosong, dan determinisme input-sama→output-sama pada graf bercabang. Mutation check: menghapus increment kunjungan awal melumpuhkan tepat 4 tes yang mengodekannya. Gate `test [akar-core]`: **1,843 passed / 0 failed** (121 suite); fmt + clippy `-p akar-algo --all-targets -D warnings` bersih. Run config cepat `Test gds::random_walk::tests` ditambahkan (`.idea`, gitignored). Metrik tes `SPEC.md` direkonsiliasi 1.835 → 1.843.

### Changed

- **chore(release) — v0.1.10: sinkronisasi crates.io, 9 crate bottom-up (2026-08-24)** — bump patch untuk semua crate yang berubah kode sejak backlog sync `71273fe`: **akar-storage & akar-main 0.1.6** · **akar-binder/planner/processor/algo 0.1.5** · **akar-ml 0.1.3** · **akar-function/json 0.1.2** (keduanya dikonversi dari `version.workspace = true` ke versi eksplisit agar bisa di-bump independen — preseden llm/ml/neo4j). `akar-azure/akar-common/akar-python` hanya menyentuh `.gitignore`/README/Cargo.lock → tidak direpublish. Tag **v0.1.10** + GitHub Release.
- **perf(storage) — P60.2: typed WAL records dari jalur tulis SQL; WAL jadi satu-satunya sumber recovery semua mode threshold (2026-08-24)** — menuntaskan defer persist mirror (lanjutan P60.1): seluruh operator tulis SQL (`insert`, `delete`, `set`, `merge`, `merge_rel`, `batch_insert`, `copyfrom`) + DML inline DDL kini memancarkan rekaman `Insert`/`Delete`/`Update` bertype ke sink per-transaksi (`WalSink` = `Arc<Mutex<Vec<WALRecord>>>` di akar-processor, helper no-op `log_*_record` di akar-storage), di-buffer `LocalWAL` dan di-bulk-copy ke global WAL **setelah** `prepare_commit` sukses sehingga record transaksi pecundang OCC tak pernah masuk log; payload rel = `[src, dst, props..]` (endpoint UInt64/Int64 hasil koersi PK), DETACH DELETE mencatat edge insiden sebelum detach dengan indeks stabil. `recover()` kini memuat mirror kolom (state checkpoint terakhir) lebih dulu lalu me-replay delta pasca-checkpoint — mirror hanya ditulis checkpoint & recovery, tanpa persist per-komit di mode threshold mana pun. **Bug material yang ditemukan & diperbaiki:** parser `deserialize_values_from_bytes` subset lama tidak mengenal tag UInt64 → endpoint edge ter-decode `Null` + cursor desinkronisasi → replay abort diam-diam di tengah log (SET/DELETE hilang saat restart); fix = delegasi penuh ke `Column::deserialize_value`. Tes: +4 unit roundtrip/noop-sink (`wal.rs`) + integrasi replay SET/DELETE/edges threshold-0 baru; crash test diperketat persis 60 baris; dua tes auto-checkpoint disesuaikan layout WAL baru. Gate `test [akar-core]`: **1,872 passed / 0 failed / 0 ignored** (121 suite); fmt --check + clippy --all-targets `-D warnings` bersih. SPEC §3/§15 direkonsiliasi. — `d0a1447`
- **perf(storage) — P60.1 fase 1: skip persist mirror duplikat saat auto-checkpoint pasti terjadi (2026-08-24)** — `commit_transaction` (`akar-storage/src/lib.rs`) sebelumnya SELALU mem-persist mirror kolom semua tabel di Step 2, lalu `maybe_checkpoint` (Step 5) yang ikut commit sering mem-persist mirror yang sama sekali lagi lewat `checkpoint_with_drain` sebelum truncate WAL — pada config default (`checkpoint_threshold = -1`, selalu checkpoint) tiap commit single-row membayar **2×** tulisan mirror penuh. Fix: skip Step 2 bila checkpoint pasti jalan (`threshold < 0`, atau size-based threshold sudah terlampaui oleh WAL pasca-append — kondisi dicerminkan persis dari keputusan `maybe_checkpoint`); mode `threshold = 0/N-belum-tercapai` tetap persist sinkron karena **mirror kolom adalah satu-satunya sumber recovery jalur SQL** (rekaman WAL-nya tak berisi data row: DML SQL menulis langsung ke tabel saat eksekusi dan global WAL hanya menerima marker `Commit` + blob `LocalWALData` kosong yang di-skip replay). Temuan arsitektur material dari investigasi P60.1: klaim plan semula ("WAL fsync sudah menjamin durabilitas") keliru untuk jalur SQL; opsi defer penuh ke checkpoint kini menjadi tindak lanjut **P60.2** (typed Insert/Delete/Update WAL records dari write path SQL agar replay WAL swadaya). Komentar tes durabilitas + SPEC §3.3/§15 direkonsiliasi. Verifikasi targeted: `test_data_durability` 9/9, `test_crash_recovery` 14/14, `test_catalog_persistence` 6/6, akar-storage lib 343/343. Gate `test [akar-core]`: **1,867 passed / 0 failed / 0 ignored** (121 suite). — `03a7f39`
- **refactor(main) — ekstraksi helper `compute_table_ids` dari `extract_write_tables` (2026-08-24, tanpa id task)** — match pengumpulan table-ID (baris 151–213, ~63 baris: arm CopyFrom/Query/Set/Delete/Create/CreateDml/Merge + sort+dedup) di `extract_write_tables` (`akar-main/src/connection/transaction.rs`) diekstrak ke helper privat `Connection::compute_table_ids(bound)`; `extract_write_tables` menjadi delegator satu-baris dengan signature `pub(crate)` tak berubah (2 pemanggil di `query.rs`: `execute_with_plan`, `execute_query_inner`). Pure code motion, byte-identical untuk blok yang dipindah (net diff +4 baris), perilaku dipertahankan persis. Verifikasi: `get_file_problems` 0 error; IDE lint hanya 2 weak warning pre-existing; `cargo fmt -p akar-main -- --check` + `cargo clippy -p akar-main --all-targets -D warnings` bersih. Gate `test [akar-core]`: **1,867 passed / 0 failed / 0 ignored** (121 suite). Commit `24d9572`.

- **docs(readme) — rekonsiliasi seluruh README proyek ke state aktual (2026-08-24, tanpa id task)** — klaim basi dikoreksi terhadap sumber kebenaran (Cargo.toml workspace, gate `test [akar-core]`, hitungan kode langsung): root `README.md` 32→**35 crates**, ~86K→**~106K LOC** (git ls-files × wc), suite tes 1.594/v0.1.5-era → **1.867 passed / 0 failed** (gate 2026-08-24), logical operators 58→**59**, `akar-algo` 15→**17 graph algorithms** (hitung ulang registrasi `register_table_function`: page_rank, wcc, scc tarjan+kosaraju, k_core, louvain, spread_activation, spanning_forest, lpa, betweenness, closeness, triangle_count, all_sp_destinations, random_walk, node2vec, shortest_path, weighted_shortest_path), daftar fitur ekstensi diperjelas ke nama flag aktual (`algo-extension`, `fts-extension`, dst.); `akar-core/README.md` idem (35 crates, gate baru, GDS 17); `SPEC.md` ikut direkonsiliasi agar konsisten (crates 35, LOC ~106K, algo 17); `akar-python/README.md` versi PyPI 0.1.0→**0.1.2** dan tes Rust 25→**39** (kini mencakup kNN, Louvain, LSTM, spread activation); `akar-common/README.md` varian `Value` 28→**30** (+UInt128/Json/Union) dan Tests 24→**36**. README di `dataset/` (SNAP/LDBC) adalah dokumen pihak ketiga dan tidak disentuh.

- **refactor(planner) — ekstraksi helper `combine_filter_expr` (2026-08-23, tanpa id task)** — kombinasi predikat WHERE baru dengan filter yang terakumulasi sebelumnya (mis. WHERE implisit dari binder atas inline node-properties, P48.17 BUG-A) yang semula inline ~15 baris di arm `BoundClause::BoundWhere` (`akar-planner/src/planner.rs:604`) diekstrak ke helper bebas terdokumentasi `combine_filter_expr` di samping helper planner lain. Pure code motion, semantik dipertahankan persis: ada filter sebelumnya → `And(prev, next)` bertipe Bool dengan `is_constant = prev && next` dan alias `None`; tidak ada → predikat baru diterima verbatim. Verifikasi: `get_file_problems` 0 error; IDE lint bersih; `check [akar-core]` (all features) exit 0; `cargo fmt --all -- --check` + `cargo clippy -p akar-planner --all-targets -D warnings` bersih. Gate `test [akar-core]`: **1,835 passed / 0 failed / 0 ignored** (121 suite). Commit `b14ba85`.

- **perf(algo) — hoist agregasi bobot komunitas Louvain ke scratch buffer reusable (2026-08-23, tanpa id task)** — `BTreeMap<usize, f64>` yang sebelumnya dialokasikan baru untuk tiap simpul pada tiap pass Louvain (`akar-algo/src/lib.rs:1479`, O(V × pass) alokasi transien + insert pohon) diganti tiga buffer yang dialokasi sekali per panggilan: scratch bobot ber-tag generasi (`comm_scratch`/`comm_stamp`, reset malas O(1)) dan daftar `touched` yang di-sort ascending — urutan iterasi identik dengan BTreeMap lama sehingga tie-breaking `gain > best_gain` pada pemilihan komunitas terjaga persis (hoist HashMap pola LPA akan membuat tie-break nondeterministik antar-proses). Benchmark criterion baru `akar-algo/benches/louvain.rs` (graf komunitas deterministik, avg degree 16, baseline `pre_opt`, semua p < 0,05): 2k simpul 1,242→0,584 ms (−52,5%), seeded 2k 1,301→0,631 ms (−51,3%), 10k 6,398→2,982 ms (−53,3%), seeded 10k 6,975→3,416 ms (−51,1%). Verifikasi: fmt + clippy `-p akar-algo --all-targets -D warnings` bersih; Gate `test [akar-core]`: **1,835 passed / 0 failed / 0 ignored** (121 suite); akar-algo 53/53 termasuk keenam tes Louvain. Commit `381bb36`.

- **perf(algo) — reuse HashMap hitungan label di `compute_lpa` (2026-08-23, tanpa id task)** — `HashMap` penghitung label yang sebelumnya dialokasikan baru untuk tiap simpul pada tiap iterasi LPA (O(V × iterasi) alokasi transien) diangkat keluar kedua loop dan dibersihkan via `counts.clear()` per kunjungan simpul (`akar-algo/src/lib.rs:1145`); kapasitas dipertahankan sehingga steady-state bebas alokasi. Benchmark criterion baru `akar-algo/benches/lpa.rs` (graf komunitas deterministik, avg degree 16, max_iters=30): 2k simpul 10,96→7,18 ms (−34,5%), 10k 70,36→48,87 ms (−30,5%), 40k 334,71→255,66 ms (−23,6%). Sidik output identik sebelum/sesudah pada graf 10k (`fnv1a edc5c5723147f065`, jumlah komunitas sama). Verifikasi: fmt + clippy `-D warnings` bersih; `cargo test` seluruh workspace 0 failed (~1.8k tes; akar-algo 53/53 termasuk `test_lpa`). Commit `12af3bb`.

- **refactor(main) — sederhanakan `build_optimized_plan` (2026-08-23, tanpa id task)** — inline variabel antara `optimized` di `Connection::build_optimized_plan` (`akar-main/src/connection/query.rs:175`) menjadi `Ok(optimizer.optimize(logical_plan))`; pembersihan sintaksis murni tanpa perubahan perilaku (2 pemanggil in-file: `query`, `execute_query_inner`). Commit `22819f2`. Gate `test [akar-core]`: **1,835 passed / 0 failed / 0 ignored** (121 suite); `cargo fmt --all -- --check` bersih.

- **refactor(storage) — ekstraksi helper `compute_bytes` & `compute_values` (2026-08-23, tanpa id task)** — dua cleanup extract-function di `akar-storage`, perilaku dipertahankan: (1) encoding union `Value::Union(tag, val)` di `ArtKey::from_value` (`art_key.rs`) diekstrak ke helper `ArtKey::compute_bytes(tag, val)`; (2) `read_values` (`npy_reader.rs`) dipecah validasi panjang buffer + loop decode baru `compute_values(count, dtype, elem_size, raw)` dengan preconditions terdokumentasi. Verifikasi ter-scope (bukan gate penuh): `cargo check -p akar-storage` bersih; `cargo test -p akar-storage` **343 passed / 0 failed** (+1 doctest). Commit `6eeaff8`.

- **chore — crates.io backlog sync (2026-08-23)** — menyinkronkan registry dengan HEAD untuk semua crate yang berubah sejak v0.1.8 tetapi tidak ikut dirilis sesi-sesi sebelumnya. Fix fungsional yang akhirnya sampai ke registry: **akar-binder/planner/processor** (P53.18 WITH-scope, P53.39 SET-RETURN, P53.40 OPTIONAL fan-out), **akar-duckdb** (P51.40 delegation helper + P51.48a row cap) dan konsumennya **delta/iceberg/azure/unity-catalog**, **akar-vector** (P51.41 refactor internal `extract_f64_list`, non-breaking — fungsi lama private), **akar-llm** (P51.48b HTTP timeout + fix kebocoran API key). Bump patch juga untuk delta kosmetik fmt/clippy: algo/search/dream/optimizer/ml/neo4j. `akar-llm`/`akar-ml`/`akar-neo4j` dikonversi dari `version.workspace = true` ke versi eksplisit agar bisa di-bump independen. Versi baru: binder/planner/processor/algo/optimizer 0.1.4 · vector/duckdb/delta/iceberg/azure/unity-catalog 0.1.3 · llm/ml/neo4j/search 0.1.2 · dream 0.1.1.

### Fixed

- **fix(ml) — adaptasi API `rand` 0.10 di `LstmModel::new` (2026-08-24)** — bump Dependabot rand 0.8.7→0.10.2 (komit `b533624`) mematahkan kompilasi workspace `akar-ml/src/lstm.rs` (`thread_rng` dihapus, `gen_range` pindah trait): `rand::thread_rng()` → `rand::rng()`, `rng.gen_range(-limit..limit)` → `rng.random_range(-limit..limit)`, import `rand::Rng` → `rand::RngExt`. Ditemukan saat gate P60.1; blokir seluruh `cargo test` workspace sebelum fix. — `1bf0e5c`
- **security(binder) — IMPORT DATABASE path traversal (CWE-22/CWE-59) (2026-08-23, tanpa id task)** — `bind_import_database` (`akar-binder/src/binder/mod.rs`) membaca `schema/copy/index.cypher` dari `file_path` mentah AST tanpa cek containment; karena `akar-server` mengeksekusi Cypher mentah dari klien TCP remote, path traversal (`'x/../../etc'`) atau `schema.cypher` ber-symlink bisa keluar dari direktori mana pun dan menyuplai file pilihan penyerang ke eksekusi statement. Fix defense-in-depth di bind time: tolak path kosong/NUL dan komponen leksikal `..` apa pun; kanonikalisasi direktori import sebelum akses filesystem; semua bacaan lewat helper `read_file_within_dir` yang re-kanonikalisasi tiap file dan menolak yang resolve di luar direktori import (guard symlink-escape). Path absolut tetap didukung (round-trip EXPORT→IMPORT tak terpengaruh — kairos `repair_schema` aman). 7 tes regresi baru (symlink-escape unix, bentuk backslash windows, NUL/kosong, penolakan di bind sebelum akses fs, containment OK, file hilang). Commit `ed4cc27`. Gate `test [akar-core]`: **1,835 passed / 0 failed / 0 ignored** (121 suite); fmt + clippy `-D warnings` bersih. Metrik tes `SPEC.md` direkonsiliasi 1.751 → 1.835.

## [0.1.9] - 2026-08-23

### Added

- **Sprint 23 (P51.48, P51.48b) — perf & flaky fix** — (1) **P51.48a** DuckDB `query_rows` safety cap: `MAX_QUERY_ROWS = 100_000` prevents OOM on unbounded queries (`akar-duckdb/src/connection.rs:180`). (2) **P51.48b** akar-llm HTTP timeout: `ureq::Agent::new_with_config` with `timeout_global: 30s` on both OpenAI and Ollama endpoints (`akar-llm/src/lib.rs:171,231`). (3) **P51.48c** akar-llm `Box::leak` API key fix: replaced with local `_env_key` variable — no more memory leak (`akar-llm/src/lib.rs:141-148`). (4) **P51.48b** flaky `test_plan_cache_no_hit_regression` fix: threshold 1.5→2.0, warmup 1→10, N 1000→2000. Verified: hit/miss ratio 0.975. Gate `test [akar-core]` 0 failed.

- **Sprint 22 (P58) — weighted RRF fusion** — `weighted_rrf_fuse` added to `akar-search` (Rust) + PyO3 binding `akar.search.weighted_rrf_fuse_py`. Formula: `weight / (k + rank)` per item, rank 1-based. Weight=1.0 equivalent to unweighted RRF. 6 Rust tests. Published: `akar-search` 0.1.1. `memory_client.py:_rrf_fuse` kept as Python (tight coupling with `channel_scores` tracking + downstream PPR/ColBERT/DAE mutations).

- **Sprint 21b — kairos native migration: akar-python 0.1.2 (PyPI)** — akar-python bumped 0.1.1 → 0.1.2; wheel rebuilt with `akar.search` (rrf_fuse, hybrid_search, multi_perspective_recall), `akar.spread` (spread_activation, batch_spread), and `akar.dream` (PyDreamConfig, PyPhaseStats, PyDreamStats) submodules. Published to PyPI: `pip install akar==0.1.2`. Enables kairos to replace Python RRF/spread implementations with native Rust via `import akar.search` / `import akar.spread`.

- **Sprint 21 (P55–P57) kairos-native migration — akar-search + akar-dream crates** — tiga capability kairos dipindahkan ke akar-native (Rust-only, zero Python runtime dependency). (1) **P55 Hybrid RRF Fusion** — crate baru `akar-search`: `rrf_fuse_ref`/`rrf_fuse_owned` (generik, K=60, dedup+rank), `hybrid_search` (vector+FTS→RRF), `multi_perspective_recall_with_id` (N queries→RRF). 17 Rust tests. PyO3 submodule `akar.search` dengan `rrf_fuse`, `hybrid_search`, `multi_perspective_recall`. (2) **P56 Batch Spread Activation** — `batch_spread_activation` di `akar-algo` (CSR shared, N seeds, top-k per seed) + PyO3 binding `akar.spread.batch_spread`. 5 Rust tests. `akar-algo` bumped 0.1.2 → 0.1.3. (3) **P57 Dream Engine** — crate baru `akar-dream`: `DreamBackend` trait (MockBackend for tests), `DreamOrchestrator::run_cycle()` mengorchestrasi 7 fase (NREM→SUPERSEDES→REM→Insight→AFE→Synthesis→DAE), phase stats tracking. `DreamConfig` dengan defaults. PyO3 submodule `akar.dream` dengan `PyDreamConfig`, `PyPhaseStats`, `PyDreamStats`. 5 Rust tests. Workspace: 32→34 crates. Gate `test [akar-core]` tetap 0 failed. akar-python 39/39 tests (termasuk 6 baru: 3 search + 3 dream). Published: `akar-algo` 0.1.3, `akar-search` 0.1.0, `akar-dream` 0.1.0.

- **P55.2 akar-python 0.1.1 — metadata PyPI + re-publish** — pyproject metadata
  diperkaya (authors, 13 classifiers, keywords, readme embed, project urls);
  versi bump `0.1.0` → `0.1.1` (PyPI menolak upload ulang versi sama) dan
  re-publish. Halaman PyPI kini menampilkan README penuh (6KB description),
  classifier GPLv3, dan author. Wheel `akar-0.1.1-cp39-abi3-win_amd64.whl` +
  sdist `akar-0.1.1.tar.gz`. Verifikasi: `pip install akar==0.1.1` + smoke test OK.

- **P55.1 Python wheel `akar` 0.1.0 published to PyPI** — `akar==0.1.0` live di
  [PyPI](https://pypi.org/project/akar/) (wheel `akar-0.1.0-cp39-abi3-win_amd64.whl`
  + sdist `akar-0.1.0.tar.gz`), diverifikasi via TestPyPI terlebih dahulu. Prosedur:
  `maturin build --release` → `maturin sdist` → upload `twine upload --repository pypi
  target/wheels/*` (token via env `TWINE_PASSWORD`; `__token__` sebagai username).
  Catatan: `maturin publish` me-rebuild dari nol (~10 menit) — gunakan **twine** untuk
  upload artefak yang sudah dibangun. Install verifikasi: `pip install akar==0.1.0`
  dari PyPI resmi + smoke test Database/Cypher OK; E2E Kairos (`remember`/`recall`,
  backend kuzudb → akar) OK pada DB fresh. Ini menutup dependensi
  `akar>=0.1.0` di `kairos/pyproject.toml` (sebelumnya gagal karena `akar` belum di PyPI).

### Changed

- **P51.43 — DRY: renderer nilai bersama + parity ringkasan hasil lokal↔remote (2026-08-23)** — (1) `pk_value_to_string` yang terduplikasi di `akar-storage/src/table.rs` dan `akar-main/src/connection/utils.rs` (cakupan tipe berbeda — versi main kehilangan kasus Int8/Int16/UInt*/Float sehingga key lookup bisa tak sinkron utk tipe sempit) dipindah ke `akar_common::types::pk_value_to_string` sebagai satu sumber kebenaran byte-identik antar layer; kedua crate kini me-re-export helper tersebut. (2) `value_to_csv_string` ikut pindah ke akar-common (Null → sel CSV kosong, sisanya kanonik via `pk_value_to_string`). (3) Head logika ringkasan (`message → error → empty`) yang copy-paste di `QueryResult::result_summary`/`Display` dan `WireResponse::result_summary`/`Display` didedupe ke `query_result::result_summary_head` — ringkasan lokal vs remote kini dijamin identik. Tes baru: `test_pk_value_to_string_canonical_keys`, `test_value_to_csv_string_null_is_empty_cell`, `test_query_result_summary_cases`, `test_local_remote_summary_parity`. Gate `test [akar-core]`: **1,829 passed / 0 failed / 0 ignored**.

- **P51.42 — DRY: EXPORT DATABASE dikonsolidasi ke satu implementasi (2026-08-22)** — sebelumnya `EXPORT DATABASE` punya dua jalur: direct `Connection::execute_export_database` (`connection/copy.rs`, lengkap: menulis schema.cypher + copy.cypher + data files) dan jalur planner (`SchemaDdlOp::ExportDatabase` di `connection/query.rs:553`, parsial: hanya schema.cypher + copy.cypher, TANPA data files dan dengan path relatif). Jalur planner kini mendelegasikan ke `Connection::execute_export_database` (sama seperti jalur ImportDatabase yang sudah membangun `Connection::new(&db_sddl)`), sehingga kedua jalur berbagi satu sumber kebenaran dan EXPORT lewat planner ikut menulis data files. Wrapper `generate_schema_cypher`/`generate_copy_cypher` (varian berbasis `&Catalog`, hanya dipakai jalur planner) dihapus beserta import `akar_catalog::Catalog` yang jadi tak terpakai. Divergensi PRIMARY KEY yang dicatat audit sudah tertangani P52.13/P52.26 (kedua jalur sudah pakai generator bersama). Tes export P53.34/P53.34b/P53.37: 3/3. Gate `test [akar-core]`: **1,825 passed / 0 failed / 0 ignored** (121 suite).

- **P51.41 — DRY: `extract_f64_list` dipindah ke akar-common (2026-08-22)** — logika ekstraksi `Vec<f64>` dari `Value::List` numerik (Double/Int64/Int32/Float) yang sebelumnya diduplikasi di `akar_vector::extract_f64_list` (`akar-vector/src/lib.rs`) dan `akar_storage::extract_f64_list_from_value` (`akar-storage/src/vector_index.rs`) kini satu implementasi: `akar_common::types::extract_f64_list` (akar-common sudah punya module `vector`, tapi di sana isinya `LegacyValueVector` — helper diletakkan di `types.rs` yang memang rumah `Value`). akar-vector meng-import helper langsung (8 site pemanggil, 4 fungsi skalar vektor); akar-storage mempertahankan `extract_f64_list_from_value` sebagai thin wrapper ke helper akar-common (memetakan error ke `StorageError`) sehingga 4 pemanggil eksternal (ddl.rs, database.rs, map_ddl.rs, table.rs) tak berubah. Tes baru: `test_extract_f64_list` (list campuran, non-list, item non-numerik, list kosong). Gate `test [akar-core]`: **1,825 passed / 0 failed / 0 ignored** (121 suite).

- **P51.40 — DRY: DuckDB-delegation 4 crate (2026-08-22)** — pola `DuckDbAttachHelper::new() → install_and_load(ext) → (execute_batch setup) → query_rows` yang diulang di delta/iceberg/azure/unity-catalog dirangkum menjadi satu method `DuckDbAttachHelper::query_extension(ext, setup, sql)` (`akar-duckdb/src/attach_helper.rs`) yang sekaligus mengkonversi hasil ke `Vec<DataChunk>` via `duckdb_results_to_akar`. Ke-6 site delegasi (delta_scan, iceberg_scan/metadata/snapshots, azure_scan, uc_scan) kini memanggil helper gabungan — konversi hasil pun tak lagi diduplikasi (sebelumnya hanya unity-catalog yang mengkonversi; yang lain membuang hasil). `CREATE SECRET` (Azure/UC) lewat parameter `setup`. Cek `cargo check --all-features` akar-duckdb + 4 crate: 0 warning. Gate `test [akar-core]`: **1,824 passed / 0 failed / 0 ignored** (121 suite).

- **P51.49 — perf: ekspor Parquet tanpa materialisasi baris + ANALYZE tanpa memegang lock statistik (2026-08-22)** — (1) API baru `write_parquet_from_chunks` di `akar-storage/src/parquet_writer.rs` men-streaming chunk hasil query kolom-major langsung ke Arrow builder; `COPY TO`/`CALL export_parquet()` (`write_parquet_to_file`, ddl.rs) dan EXPORT DATABASE format parquet (`copy.rs`) tidak lagi membangun salinan row-major `Vec<Vec<Value>>` seluruh tabel (puncak memori ~2× data → ~1×). Semantik identik jalur lama: declared physical type menang atas inferensi nilai (semantik P53.37), List menginfer inner type dari item non-null pertama (default Float64), fallback Utf8; nama kolom dari `field_names` chunk (alias `n.id`→`id`, fallback `column_{i}`); dispatch builder scalar/List didedupe ke helper `scalar_builder_for_value` + `list_builder_for_inner`. (2) ANALYZE (`BoundAnalyze`, ddl.rs) kini menghitung statistik TANPA memegang lock `stats_store`: scan penuh + stringify distinct berjalan di atas data katalog, lock hanya diambil sesaat untuk publikasi — pembaca konkuren tak lagi terblokir di belakang ANALYZE database penuh; node path membaca `NodeGroup::get_value` per grup langsung (tanpa binary search global per sel — setara, karena `get_value_with_snapshot(None)` murni chunk read), rel path tetap slice kolom; dua blok inline node/rel (~90 baris) didedupe ke `analyze_node_table` / `analyze_rel_table` / `scan_column_stats`. Tests baru: `test_write_parquet_from_chunks_roundtrip`, `test_write_parquet_from_chunks_declared_all_null_keeps_type`. Gate `test [akar-core]`: **1,824 passed / 0 failed / 0 ignored** (121 suite).

- **P60.1 — `cargo fmt --all` workspace-wide (2026-08-22)** — seluruh drift rustfmt dirapikan (15 file di akar-algo, akar-binder, akar-dream, akar-llm, akar-ml, akar-search, akar-storage, akar-vector) sehingga `cargo fmt --all -- --check` kini bersih — selaras standar CI. Perubahan murni format, tanpa perubahan semantik. Gate `test [akar-core]`: **1,822 passed / 0 failed / 0 ignored** (122 suite).

### Fixed

- **P51.52 — perf(storage): skip tulis `.ovf` kosong + percepatan tes durabilitas/crash-recovery (2026-08-23)** — (1) `save_overflow` (`akar-storage/src/persistence.rs`) kini skip penulisan file `.ovf` bila semua map oversized kosong dan menghapus file basi bila tersisa — sebelumnya tiap persist menulis ulang `.ovf` meski nol baris (I/O sia-sia di jalur commit). (2) `test_crash_recovers_committed_rows_without_double_apply`: N 200→60 baris, budget 120s→60s (semantik recovery tetap terkunci penuh). (3) `test_crash_recovery.rs`: sleep tetap 5s diganti polling marker `checkpoint_done` yang ditulis child setelah `CHECKPOINT` sukses (timeout 30s, poll 25ms) — deterministik di Windows; sleep pasca-WAL dipangkas 1s→200ms. Verifikasi targeted: `test_data_durability` 9/9, `test_crash_recovery` 14/14, persistence akar-storage 9/9.

- **chore: workspace bersih clippy `-D warnings` (clippy 1.96) (2026-08-23)** — membersihkan lint pre-existing yang tertangkap toolchain baru agar standar CI tetap hijau: indentasi doc-list (`akar-storage/art_key.rs`, `akar-main/connection/copy.rs`), `manual_saturating_arithmetic` (`vector_index.rs`), `manual_ignore_case_cmp` (`akar-neo4j/lib.rs`), `useless_vec` pada tes intersect + `#[allow(clippy::module_inception)]` (`akar-processor/processor/tests*.rs`), `field_reassign_with_default` di 3 fase + iterasi `.values()` + impl `Default` untuk `MockBackend` (`akar-dream`), needless borrow (`akar-optimizer/passes/flat/scan_ops.rs`). Tanpa perubahan perilaku.

- **P53.40 — kairos Finding #22: WHERE pada alias agregat WITH + OPTIONAL MATCH tujuan anonim (2026-08-22)** — (1) `MATCH (m:T) OPTIONAL MATCH (m)-[r:R]-(:T) WITH m, COUNT(r) AS cnt WHERE cnt < $n RETURN ...` kini memfilter pipeline teragregasi (semantik HAVING); sebelumnya gagal eksekusi `Variable 'cnt' not found in field_names` (binder sudah benar sejak P53.18, jalur eksekusi yang rusak). (2) `OPTIONAL MATCH (m)-[r:R]-(:T)` dengan destination anonim berlabel kini di-route ke edge-probe `OptionalExtend` dalam mode fan-out: satu baris output per edge yang incident pada source (dedup self-loop pada arah `Both`; 0 edge tetap 1 baris NULL-padded) — mempertahankan kardinalitas multi-edge untuk agregat hilir seperti `COUNT(r)`; fallback lama lewat merge generik meng-cross-product dan menduplikasi tiap baris kiri saat tidak ada kolom bersama. (3) Fast-path perakitan plan saat `scan_ops` kosong (semua scan hidup di dalam pipeline OPTIONAL/DELETE) tidak lagi drop ORDER BY/LIMIT/DISTINCT/SKIP — tail RETURN diekstrak ke helper bersama `append_return_tail` (juga membawa sort-below-projection P53.37 ke jalur fast-path). Files: `akar-planner/src/planner.rs`, `akar-planner/src/logical_operator.rs`, `akar-processor/src/physical/write_ops/optionalextend.rs`. Tests: `akar-main/tests/test_p5340_with_where_aggregate.rs` (10 tes: kontrol WITH→RETURN, WHERE alias filter/pass-all/no-optional, bare var group key ×3, OPTIONAL direct count/raw rows/order-limit). Gate: **1,822 passed / 0 failed / 0 ignored** (121 suite).

- **P53.18 / P53.39 — kairos drop-in bugs (2026-08-21)** — (1) **P53.18** `WITH` clause now resets the scope to its projection: bare variables and aliases (aggregates like `COUNT(r) AS cnt`) become in-scope for trailing `WHERE`/`RETURN`. Previously the alias list was discarded, so `WITH m, COUNT(r) AS cnt WHERE cnt < $n` failed with `Variable 'cnt' not in scope` (kairos Finding #13, dream REM phase). (2) **P53.39** `MATCH ... SET ... RETURN` with no matching row now returns **zero rows**; previously the SET emitted a phantom `count=0` chunk that the RETURN turned into `[[0]]` (kairos P0/P59.1). A terminal SET (no RETURN) still reports "updated: N" via column 0. Files: `akar-binder/src/binder/mod.rs`, `akar-planner/src/planner.rs`, `akar-planner/src/logical_operator.rs`, `akar-processor/src/physical/write_ops/set.rs`, `akar-processor/src/processor/mapper/map_update.rs`. Tests: `test_bind_with_alias_in_scope_where_and_return`, `test_bind_with_plain_aggregate_return`, `test_p5339_match_set_return_no_match_empty`, `test_p5339_match_set_no_return_reports_zero`. Gate: akar-binder 95/95, akar-main integration 70/70, akar-main full 0 failed.

- **P53.9 compat harness re-pointed to Akar stores + P59.1 discovered** — `akar-core/akar-python/tests/test_kuzu_compat.py` still imported `KuzuDBStore`/`KuzuDBDreamBackend` from `kairos.kuzudb_store`/`kairos.dream_kuzudb_store` (modules deleted in kairos Phase 3 rename). Updated to `AkarStore`/`AkarDreamBackend` from `kairos.akar_store`/`kairos.dream_akar_store`. Harness now **53/53 pass** and surfaced engine gap **P59.1**: `MATCH..SET..RETURN` with no matching node emits one phantom row (`{'col0': 0}`, alias column names lost) where Kuzu returns zero rows — silently broke kairos `_next_id` (id=0) and `set_meta` (lost writes) on fresh DBs; worked around kairos-side via returned-key validation. Engine fix tracked as P59.1 (Sprint 24, `implementation plan.md`).

### Removed

- **Dead-code cleanup — phantom classes in `__init__.pyi` (2026-08-22)** — dropped `NodeVal`, `RelVal`, `KNNResult`, `DatabaseType` from the hand-written type stub: none exist in the compiled `akar` module (verified against wheel 0.1.2 export surface: `Database`, `Connection`, `QueryResult` + submodules `search/spread/dream/knn/louvain/lstm`; `akar.knn` has functions only). Leftovers of the deleted `tools/rust_api` crate. Stub-only change — zero runtime impact. Commit `c1eff2c`.

## [0.1.8] - 2026-08-18

### Added

- **Sprint 21 — kairos-native migration (P55–P57)** — 2 crate baru `akar-search` + `akar-dream` (+27 tes), bindings PyO3 `search` + `dream` (P55.5, P57.12). Gate `test [akar-core]` naik.
- **PyPI `akar` 0.1.0 → 0.1.1** — metadata + penghapusan alias shim `ladybug`.
- `batch_spread_activation` publish — bump akar-algo 0.1.2 → 0.1.3.
- Sinkron `Cargo.lock` akar-vector 0.1.2 lintas workspace.

## [0.1.7] - 2026-08-18

### Added

- **P54.5 (Sprint 20) PyO3 expose kNN/LSTM/spread/louvain submodules** — 4 modul PyO3 baru di `akar-python`: (1) `knn.rs` — wrap `akar_vector::{cosine_similarity,euclidean_distance,dot_product,l2_distance,normalize}` + `knn_search(vectors,query,k,metric)` brute-force (top-k partial sort); (2) `lstm.rs` — wrap `akar_ml::lstm::{LstmModel,LstmConfig,forward_cell,forward_sequence,train,save_model,load_model}` + `LstmCell`/`TrainingResult` pyclasses; (3) `spread.rs` — build CSR dari edge list Python → `akar_algo::compute_spread_activation` (optional edge weights); (4) `louvain.rs` — build CSR → `akar_algo::{compute_louvain,compute_louvain_weighted}` (optional weights + modularity). Semua diregistrasi sebagai submodules via `m.add_submodule()` di `akar` modul (`lib.rs`). `Cargo.toml` ditambah dep `akar-vector/graph/algo/ml` (path). 39 Rust tests (14 baru: knn 4, lstm 4, spread 3, louvain 3). Fix compile: hapus argumen `py` berlebih di `test_lstm_train`, hapus `Python::attach` unused di tests. `cargo check` + `cargo test --lib` hijau (akar-python standalone workspace). Gate `test [akar-core]` **1,774** tidak berubah.
- **P54.4 (Sprint 20) LSTM 1-layer — local ML training crate `akar-ml`** — new crate `akar-core/akar-ml` (standalone, rand 0.8 + serde). 1-layer LSTM with: (1) forward pass (`forward_cell`, `forward_sequence`), (2) full BPTT training (`train` function — accumulated gradients across all timesteps), (3) save/load model as JSON (`save_model`, `load_model`). `LstmModel` with Xavier initialization, 4-gate architecture (input/forget/candidate/output), `LstmConfig` (input_size, hidden_size, output_size). 5 tests: `test_lstm_cell_shapes` (gate ranges), `test_forward_sequence`, `test_train_xor_converge` (XOR loss < 0.05 after 1000 epochs), `test_save_load_roundtrip`, `test_model_new_shapes`. Gate **1,774 passed / 0 failed**.
- **P54.1 (Sprint 20) Louvain upgrade — weighted + seed + modularity** — `compute_louvain_weighted` in `akar-algo/src/lib.rs` now supports: (1) `weight_fn` closure for weighted edges, (2) `seed` for deterministic shuffle via custom LCG (`SimpleRng`), (3) `min_gain` threshold (default 0.001) + `max_iterations` (default 20), (4) modularity Q output in `AlgoResult.metadata`. `compute_louvain` rewritten as thin wrapper (`weight_fn = |_u,_v| 1.0`). `AlgoResult` extended with `metadata: Option<HashMap<String, f64>>` (backward-compatible, all 17 existing constructors updated). CALL louvain() closure parses optional args `(seed?, min_gain?, max_iterations?)` and outputs 3 columns `(node_id, community_id, modularity)`. Batch-mode gain evaluation (no node-ordering dependency). 6 tests: `test_louvain`, `test_louvain_weighted` (two-clique + weak bridge), `test_louvain_modularity_positive`, `test_louvain_seed_deterministic`, `test_louvain_empty`, `test_call_louvain`. Gate **1,765 passed / 0 failed**.
- **P54.2 (Sprint 20) spread activation — single + batch spreading** — `compute_spread_activation` in `akar-algo/src/lib.rs`: BFS-like propagation with `weight_fn`, `decay` (default0.5), `threshold` pruning (default 0.01), `max_hops` (default 3). Returns `(node_id, activation, hop)` sorted by activation descending. BTreeMap-based frontier deduplication prevents cycle inflation; same-hop multi-path accumulation supported. CALL `spread_activation(seed_ids, decay?, threshold?, max_hops?)` → 3 columns `(node_id INT64, activation DOUBLE, hop INT64)`. 5 tests: `test_spread_activation_basic` (linear chain), `test_spread_activation_threshold_pruning`, `test_spread_activation_multi_seed` (convergent paths), `test_spread_activation_empty`, `test_spread_activation_weighted`. Gate **1,769 passed / 0 failed**.
- **P54.3 (Sprint 20) kNN multi-signal re-ranker** — `rerank_knn` and `compute_rerank_score` in `akar-vector/src/lib.rs`: 4-signal weighted combination — embedding `(cos+1)/2`, temporal `exp(-age*0.01)`, frequency `ln(freq+1)/ln(1000)`, graph proximity. Default weights `(0.6,0.15,0.1,0.15)`. `RerankWeights` and `RerankCandidate` structs with `Default` impl. `rerank_knn(candidates, weights, top_k)` returns `Vec<(id, score)>` sorted by descending score. 4 tests: `test_rerank_score_basic` (maxed signals), `test_rerank_score_low_quality`, `test_rerank_top_k` (ordering), `test_rerank_weight_shift` (weight isolation). Gate **1,769 passed / 0 failed**.

### Fixed

- **Flaky crash recovery test stabilized** — `test_crash_recovers_committed_rows_without_double_apply` kini kill child hanya setelah semua 200 baris committed & durable. Sejarah: (1) pendekatan awal menunggu WAL size + sleep → racy di Windows (fsync tak selesai). (2) Marker per-commit (`commit_done_N`) membunuh di tengah aliran tulis → state persist/WAL sobek yang tak bisa di-recover Linux (0 baris). (3) Fix final: child menulis `write_done` setelah loop tulis selesai (tiap commit durable: WAL flush + column-mirror persist), lalu di-SIGKILL saat idle — tanpa clean shutdown, tapi seluruh baris committed sudah durable. `wait_for_wal_size` (dead code) dihapus. — `ecf0244` — gate **1,774** unchanged.

## [0.1.6] - 2026-08-17

### Added

- **P53.11 (Sprint 20) packaging & docs — akar-python README rewrite + build fix pyo3 0.29.2** — `akar-python/README.md` ditulis ulang menyeluruh: API surface (`Database`, `Connection`, `QueryResult`), build dari source (development + release wheel via `maturin build --release`, source dist via `maturin sdist`), Kairos compatibility shim (`kairos/kuzu.py`), daftar Cypher features yang didukung, struktur proyek, instruksi tes. **Fix compile `cargo test --lib`** (regresi upgrade `300a5cd` pyo3 0.24 → 0.29.2 — target lib-test belum pernah dikompilasi sejak upgrade): `PyObject` dihapus di 0.29 → `Py<PyAny>` (5 situs); `Bound::downcast` → `Bound::cast` (`py_to_value`, 3 situs); `Python::with_gil` → `Python::attach` (4 situs tes). `cargo test --package akar-python --lib --all-features` **25/25 hijau** (0.36s; runtime butuh `python313.dll` di PATH dari base uv python). **Verifikasi packaging** (maturin 1.14.1 di `.venv`): `maturin build --release` → wheel `akar-0.1.0-cp39-abi3-win_amd64.whl` (abi3-py39, 2m59s); `maturin sdist` → `akar-0.1.0.tar.gz`; wheel ter-install & smoke test `import akar` + CREATE NODE TABLE + CREATE OK; harness pytest **53/0** hijau terhadap wheel (47.4s). Crate standalone workspace (bukan member `akar-core`); gate `test [akar-core]` **1,751** tidak berubah.
- **P53.10 (Sprint 20) drop-in KuzuDB verification — end-to-end di Kairos** — umbrella task verifikasi `akar-python` sebagai drop-in replacement KuzuDB di proyek Kairos. 5 fase (P53.26–P53.38): (1) UNWIND clause correctness (P53.26–P53.28), (2) SET/MERGE/DELETE write correctness (P53.29–P53.32), (3) precision & maintenance lifecycle (P53.33–P53.35), (4) wrapper polish (P53.36–P53.36b), (5) end-to-end shim + reconcile (P53.34b, P53.37a–c, P53.37 shim, P53.38). Harness P53.9: **53/0** (3× run, 0 flake). Gate **1,751**. Audit: `docs/audits/audit-p5310-kairos-dropin-gaps.md`. Sub-tasks detail di `CHANGELOG.md` (P53.12–P53.38).
- **P53.37 (Fase 5) shim `kairos/kuzu.py` — drop-in alias `import kuzu`/`import ladybug` → `import akar`** — `kairos/kuzu.py` registrasi `sys.modules` aliases (Database, Connection, QueryResult) untuk `kuzu` + `ladybug` saat kairos package di-import; `kairos/__init__.py` import shim sebelum modul lain. Smoke test end-to-end (store memories + embeddings, search_semantic, search_bm25, dream strengthen/prune, close/reopen, weight exact 0.95, embedding round-trip) — **ALL PASSED**. Harness P53.9: **53/0** (3× run, 0 flake). Gate cargo tidak berubah (Python-side only).
- **P53.38 (Fase 5) reconcile — plan cleanup + SPEC.md update** — harness P53.9 **53/0** (3×, 0 flake) terverifikasi; `SPEC.md` §7.2 ditambahkan Python bindings ecosystem (`akar-python` PyO3 0.29.2, compat shim, harness, tested against Kairos); `implementation plan.md` Fase 5 dihapus (task selesai dipindah ke `CHANGELOG.md`); gate **1,751** tidak berubah (Python-side only).
- **Batch C — pytest compat harness (P53.9)** — `akar-python/tests/test_kuzu_compat.py` + `conftest.py` menjalankan metode asli `KuzuDBStore`/`KuzuDBDreamBackend` Kairos (schema bootstrap, store, store_many, get/get_many, connections, search_semantic, revision, stats, export/repair_schema, CHECKPOINT) terhadap `import akar` pada temp DB per-test. Gap-capture via hook pytest `pytest_runtest_makereport`/`pytest_sessionfinish` di `conftest.py` (hook wajib di conftest, bukan test module) → `tests/test_kuzu_compat.gap_report.json`. Hasil (2026-08-14): **34 failed / 17 passed**; 9 gap engine G1–G9 di-identifikasi + diverifikasi probe langsung — rel-scan target binding `b.id == a.id` (G1), List/FLOAT[] → NULL di ORDER BY/LIMIT (G2), grammar chain clauses `cypher.pest` (G3), nested aggregate `COALESCE(MAX/SUM())` (G4), alias RETURN dibuang (G5), `SET m.x=m.x+1` → NULL/0 (G6), reopen-after-close (G7), EXPORT DATABASE path quoting (G8), QUERY_VECTOR_INDEX translation rapuh (G9) → task lanjutan P53.12–P53.19 di `implementation plan.md`; catatan lengkap di `docs/audits/audit-p53-compat-harness.md`. Artefak generated (`*.gap_report.json`, `__pycache__/`, `.pytest_cache/`) di-git-ignore. Gate: Python-side (pytest, tidak mengubah gate cargo 1,649). — `39fa230`.
- **Batch A+B akar-python (drop-in KuzuDB, P53.1–P53.8)** — translation layer `translation.rs` + `param_interp.rs` + executor `lib.rs`: DDL idempoten — `CREATE NODE/REL TABLE IF NOT EXISTS` → cek `CALL show_tables()` lalu skip/create; `DROP TABLE IF EXISTS` → swallow "not found" (P53.1); `FLOAT[n]` → `FLOAT[]` + registri dims dari DDL (P53.2); `ALTER TABLE ... ADD col TYPE DEFAULT <lit>` → strip DEFAULT + swallow "column already exists" (P53.3); `CALL CREATE/DROP/QUERY_VECTOR_INDEX` → SQL Akar (brute-force MATCH + ORDER BY ekspresi cosine penuh, workaround limit-alias) (P53.4); syntax Kuzu `CREATE VECTOR INDEX IF NOT EXISTS ... FOR/ON/OPTIONS` (P53.5); multi-statement split + `INSTALL`/`LOAD`/`UNINSTALL EXTENSION` no-op (P53.6); interpolasi `$param` → literal ter-escape (string/int/float/bool/list/dict/null, validasi nama `[A-Za-z_][A-Za-z0-9_]*`) (P53.7); `RETURN node` → dict via registri schema + fallback `table_info` (P53.8). Infra tes: pyo3 `auto-initialize` (memungkinkan `cargo test`), `rustfmt.toml` 120-kolom + `cargo fmt`, tes integrasi end-to-end `kairos_ensure_schema_bootstrap_is_idempotent` (bootstrap `_ensure_schema` 2× idempoten + DROP IF EXISTS + ALTER ADD DEFAULT). — `b93e966` — gate **1,649 (1,649 passed)**.
- GDS table functions membaca graf katalog nyata — closure menerima `Option<&dyn GraphDataSource>` lewat `TableFunction::CustomTableWithGraph` + `CatalogGraphSource` (P52.46) — `0290a8c`.

### Removed

- **5 doc-test `ignore` blok** — snippet ilustratif yang tak bisa di-compile standalone dihapus dari doc comment (`akar-storage/column_chunk.rs`, `lazy_scanner.rs`, `spiller.rs` ×2, `akar-main/prepared_statement.rs`) agar tak tercatat sebagai "ignored" test yang membingungkan; tersisa 8 doc-test lulus (0 ignored). Bonus: `test_migration_ingestion` kini jalankan binary `akar-migrate` via `env!("CARGO_BIN_EXE_akar-migrate")` (bukan nested `cargo run` yang memicu rebuild). — `6d6cded` — gate **1,648 (1,648 passed)**.

### Changed

- **P51.50/P51.51 (KISS) dead-code warning build `--all-features` di-silence** — tiga `dead_code` warning feature-gated (`feature native`, hanya muncul saat `--all-features`; CI default/clippy tidak terpengaruh): (1) `UcTableScan::columns`/`rows`/`row_count` tak pernah dibaca — `rows`/`row_count` selalu kosong = scaffolding scan masa depan, `columns` dibangun hanya untuk menurunkan string `schema`; (2) `list_data_files` + `collect_parquet_files` (`akar-iceberg/src/native_reader.rs`) hanya dipakai test `test_list_data_files` — path produksi `list_active_data_files`. Mitigasi: `#[allow(dead_code)]` + doc-note (preseden `format_columns` unity-catalog); penghapusan total ditolak (helper utuh/bertest, berguna untuk tooling). `cargo check --workspace --all-features` kini 0 warning. Audit: `docs/audits/audit-dead-code-allfeatures.md` (prioritas: unity-catalog > iceberg). — `216a852` — gate **1,704 (1,704 passed)**.
- **P51.47 (perf)** — plan-cache hit kini berbagi `Arc<BoundStatement>`/`Arc<Vec<LogicalOperator>>` sehingga tak ada deep-clone operator tree per query; handler processor (sequence, schema-DDL, query, subquery, standalone-call registry) dibangun sekali per Connection dan di-share via `OnceLock` — `create_processor` tak lagi rebuild ~27 handler Arc tiap query. Handler dicache di `Connection` (bukan `Database`) agar `Arc<Database>` kuat di dalam handler tak membentuk reference cycle yang mengunci file lock selamanya. Validasi: `test_plan_cache_no_hit_regression` isolasi ratio hit/miss **0.914** (full-suite 0.950) — `340dbd0` — gate **1,647 (1,642 passed)**.

### Fixed

- **P53.37c (Fase 5) `RETURN count(*)` setelah DELETE melaporkan jumlah terhapus sebenarnya** — harness P53.9 `test_prune_connection_history` (audit D1, `prune_connection_history`) gagal: `MATCH (h:ConnectionHistory) WHERE ... DELETE h RETURN count(*) AS deleted` mengembalikan `1` walau 0 baris cocok (tabel kosong), dan `1` walau 2 baris terhapus. Investigasi: persistensi/visibilitas DELETE node sebenarnya **sudah benar** — scan MVCC (`to_column_major_data_with_snapshot_and_predicate_and_ids` + `VersionInfo`) memfilter baris soft-deleted (probe: setelah DELETE, `MATCH ... RETURN h.id` hanya mengembalikan survivor; audit D1 merekam perilaku versi lama). Root cause: `PhysicalDelete::execute` (`akar-processor/src/physical/write_ops/delete.rs`) selalu meng-emit **chunk count 1-baris** — `RETURN count(*)` (CountStar menghitung `active_rows()`) karenanya selalu `1`. CREATE (P53.25) & SET (P53.30) sudah mengikuti konvensi per-baris; DELETE belum. Fix: (1) 0 baris terhapus → emit chunk 0-baris (skema dipertahankan, pola Filter) sehingga count(*) = 0; (2) N baris terhapus → emit N baris (kolom 0 membawa nilai count per baris, kontrak `get_i64(0,0)` tetap valid untuk `test_delete_set`/FTS). Berlaku untuk node & rel (prune_weak/prune_old_dream_sessions ikut benar). Regression: `test_p5337_delete_node_persist.rs` (5 tes: WHERE+DELETE+re-count dengan survivor, DELETE semua → count 0, single-node D1 probe → count ulang 0, tabel kosong → deleted 0, PK terhapus dapat di-re-insert). — gate **1,751 (1,751 passed)**; sisa gap harness: re-run di P53.38.
- **P53.37a/P53.37b (Fase 5) UNWIND variable dipertahankan melalui multi-pattern MATCH + SET pipeline** — harness P53.9 `test_add_connections_batch` (A2) gagal `Variable 'row' not found in chunk field_names [b.id, ..., a.id, ...]`: query kairos `UNWIND $batch AS row MATCH (a:Memory {id: row.src}),(b:Memory {id: row.tgt}) MERGE (a)-[r:Connected {type: row.type}]->(b) SET r.weight = 1.0, ...` kehilangan binding UNWIND; `test_batch_strengthen_connections` (A3, P53.37b) `SET r.weight = CASE ... e.delta` di pipeline yang sama juga tak diaplikasikan (weight tetap 0.9). Tiga lapis root cause + fix: (1) **optimizer join-reorder membuang UNWIND** — `reorder_joins_segment`/DP (`akar-optimizer/src/join_order.rs`) mengumpulkan sumber baris lewat `collect_scans_recursive`+`get_scan_alias` yang tak punya arm `Unwind` → tree dibangun ulang dari scan node saja, UNWIND (dan join key yang mereferensikan variabelnya) hilang, pipeline jadi `CrossProduct(a,b)+Filter` tanpa kolom `row`. Fix: `Unwind` dijadikan sumber baris kelas-pertama (arm di `collect_scans_recursive` + alias = variabelnya di `get_scan_alias`); DP kini menyertakan UNWIND sebagai relasi ketiga dengan join keys `a.id=row.src`/`b.id=row.tgt` utuh. (2) **`PhysicalExtend` menyalin kolom input via `store_value_in_vector` yang membuang Map/Struct/List ke NULL** (`recursiveextend.rs`) — kolom UNWIND `e` jadi NULL setelah Extend → `SET r.weight = CASE ... + e.delta` dan `WHERE b.id = e.tgt` tak bisa baca variabel → Filter menyaring habis baris (SET dapat 0 baris). Fix: kolom kompleks (List/Array/Struct) disalin lewat `build_arrow_from_values` (jalur Arrow), primitif tetap legacy; berlaku juga untuk kolom node tujuan (embedding FLOAT[] di Extend tak lagi NULL). (3) **salinan qualified stale membayangi kolom fresh + proyeksi fallback posisional** — `append_pipeline_columns` (`set.rs`) menempel `r.weight`/`r.type` (nilai pre-SET dari pipeline) ke chunk output SET sehingga `RETURN r.weight` membaca nilai lama; fix: qualified copy yang basenya sama dengan kolom polos snapshot dibuang → resolusi bare-name jatuh ke kolom table fresh. Proyeksi (`map_projection.rs`): ekspresi yang tak ter-resolve sebagai kolom polos (property access atas variabel map UNWIND, base-nya kolom chunk) kini dievaluasi per-row via evaluator (bukan fallback posisional yang mengembalikan kolom count untuk tiap baris); kasus lain yang tak ter-resolve (scan/optional-merge kehilangan kolom — gap ter-mask pra-ada) tetap memakai fallback posisional agar gate tak turun. Regression: `test_p5337_unwind_multi_match.rs` (6 tes: two-MATCH variable resolution, MERGE edge+type dari UNWIND row, SET applied di pipeline, per-row values via SET, kairos `batch_strengthen_connections` shape dengan CASE+`e.delta`+RETURN `e.src/e.tgt/r.weight` → `Double(0.9500000000000001)`, multi-row UNWIND → edge per elemen) + `akar-optimizer` `test_get_scan_alias_unwind` & `test_reorder_segment_keeps_unwind_source`. — `70a7eb4` — gate **1,746 (1,746 passed)**; test setara harness untuk A2 (`add_connections_batch`) & A3 (`batch_strengthen_connections`) kini green di Rust — re-run harness P53.9 (setelah `maturin develop`) ada di P53.38; sisa gap: P53.37c (DELETE node persist).
- **P53.34b (Fase 5) `EXPORT DATABASE` data files round-trip** — `execute_export_database` hanya menulis `schema.cypher`+`copy.cypher` tanpa file data per tabel → `repair_schema` (EXPORT→fix→DROP→CREATE→IMPORT) tak mengembalikan data (count tetap 0). Tiga bug compounding: (1) `generate_copy_cypher_from_entries` menulis filename relatif (`COPY Memory FROM 'Memory.csv'`) → `bind_copy_from` resolve terhadap CWD (bukan export dir) → file not found → silently skipped. Fix: fungsi menerima `dir: Option<&Path>`, menulis absolute path bila `Some(dir)`. (2) `export_table_data` menjalankan `MATCH (n:Memory) RETURN n` → field_names include `_id` internal (4 kolom) tapi tabel re-created hanya punya 3 kolom katalog → column count mismatch. Fix: pakai explicit column list (`RETURN n.id, n.content, n.embedding`). (3) `logical_type_name(LogicalTypeID::List)` → bare `"List"` via Debug format → parser reject `embedding List` di DDL. Fix: `List` → `"FLOAT[]"`. Regression: `test_p5334b_repair_schema_flow_data_persists_csv` (EXPORT CSV→DROP→CREATE→IMPORT→count==3 + content verify). — `d2174c8` — gate **1,746 (1,746 passed)**.
- **P53.36b (Fase 4) `ORDER BY` kolom tak diproyeksikan** — audit F1 (ditemukan saat re-run harness): `search_bm25`/`search_entity` kairos memakai `MATCH (m:Memory) ... RETURN m.id, m.label ORDER BY m.access_count DESC LIMIT $limit` → gagal `Variable 'm' not found in chunk field_names ["m.id", "m.label"]` karena sort key `m.access_count` dievaluasi terhadap chunk hasil proyeksi yang sudah di-prune. Fix: planner mendorong `OrderBy` ke **bawah** proyeksi bila ada key yang tak dicakup output proyeksi (`projection_covers_sort_key` — key dicakup bila match alias/ekspresi identik/node-var); pass optimizer `top_k` yang melewatkan OrderBy menembus proyeksi (`OrderBy+Projection+Limit`→`Projection+TopK`) dibatalkan bila key tak dicakup, agar sort tetap di jalur pre-proyeksi. `RETURN n.id ORDER BY n.a + n.b` (dulu error) kini valid Cypher dan mengurut dengan benar. Regression: `test_p5337_order_by_unprojected.rs` (4 tes: unprojected col, +LIMIT, alias, properti kedua); P53.23 `order_by_non_projected_column_errors` → `order_by_non_projected_column_sorts`. — `a3d94f3` — gate **1,737 (1,737 passed)**; harness kini **49 passed / 4 failed** (dari 36/15).
- **P53.36 (Fase 4) `get_all()` hasil 0-baris** — audit E4 (latent) melaporkan panic `PrimitiveArray of length 0` di arrow saat `UNWIND ... CREATE` lalu `.get_all()` (jalur harness `_store_many([])`). Pada kode kini tidak ter-repro: `get_all`/`get_next`/`__iter__` sudah guarded oleh `has_next()` (`chunk_idx >= chunks.len()` → false) dan range iterasi `row_end..chunk.size` (kosong bila `size==0`); semua bentuk hasil kosong (`UNWIND [] AS x RETURN x`, `MATCH ... LIMIT 0`, `UNWIND [] AS x CREATE (...) RETURN x`, `FOREACH` kosong) mengembalikan `[]` bersih — kemungkinan sudah ter-fix insidental kerja P53.34/engine. Regression ditambahkan di `test_kuzu_compat.py` (`test_store_many_empty_returns_empty`, `test_empty_store_many_does_not_panic_get_all`) — PASS dengan pyd hasil `maturin develop`; akar-python `cargo test --lib` 25/0. E4 ditutup di audit. — P53.36 — harness kini **47 passed / 6 failed** (dari 36/15).
- **P53.35 (Fase 3) lock database lintas-proses reentrant in-process (E3 close→reopen)** — harness `test_close_and_reopen` gagal di `KuzuDBStore(path)` pertama pada path fresh: fixture `store` pada path **yang sama** (`tmp_path/"mem.db"`) masih hidup selama body test, dan dua `Database` in-process pada satu path bertabrakan di lock lintas-proses (`already open by another process`). Bukan leak close. Fix di `Database::new` (`akar-main/src/database.rs`): lock kini **reentrant dalam satu proses** via registry global `PROCESS_PATH_LOCKS` (`OnceLock<Mutex<HashMap<PathBuf,(File,u32)>>>`) + guard `PathLock` (Drop menurunkan refcount; entri terakhir ditutup → lock OS dilepas). Open pertama mengambil lock OS; open berikutnya pada path sama dalam proses ini berbagi handle alih-alih gagal. Penjaga **lintas-proses tidak berubah** (tiap proses punya registry sendiri → proses kedua tetap ditolak). Regression: `test_same_process_second_open_shares_lock` (3 instance hidup bersamaan + reopen setelah semua drop), `test_cross_process_lock_still_excludes_second_process` (child `crash_sim_child hold-lock`: proses kedua ditolak `already open`, setelah parent lepas → `LOCK-HELD`), `test_shared_lock_allows_multiple_readers`; akar-python `two_databases_on_same_path_coexist_then_reopen` (fixture + store + reopen berbagi); server tests di-reframe ke in-process share (cross-process ditutup oleh test akar-main). — `ac6ab76` — gate **1,733 (1,733 passed)**.
- **P53.34 (Fase 3) `IMPORT DATABASE` + opsi diterima; opsi EXPORT tak lagi dibuang** — audit E2 (`test_repair_schema` kairos, `ok=False`) ter-isolasi: `IMPORT DATABASE "...path" (format="parquet")` gagal parse **`expected EOI`** karena grammar `import_database` (`cypher.pest`) hanya menerima `string` tanpa klausa opsi (kuzu/kairos kirim `(format="parquet")`). Fix: grammar `import_database` kini menerima `export_options?`; `parse_import_database` mem-parsing opsi ke AST `ImportDatabase.options` (`HashMap`). **Bug sibling tersembunyi:** `parse_export_database` juga diam-diam membuang opsi — pest menyarangkan opsi di parent pair `Rule::export_options`, tapi parser match `Rule::export_option` langsung → AST selalu kosong → `format="parquet"` tak pernah sampai binder → `copy.cypher` default CSV; diperbaiki via helper bersama `parse_options_into` (rekursi `export_options → export_option`) yang dipakai keduanya. Diverifikasi pula: `DROP TABLE Memory` dengan rel dependen `Connected` OK (catalog tanpa dependency check), `CREATE VECTOR INDEX ... OPTIONS` ditangani translation (P53.5). Regression: parser `test_import_database_with_format_option`, `test_import_database_options_roundtrip`, `test_export_database_options_roundtrip`; integrasi `test_p5334_export_import_options.rs` (`test_p5334_export_import_format_options_roundtrip` — format sampai `copy.cypher` sebagai `.parquet`; `test_p5334_export_without_options_still_works`). **Gap lanjutan dicatat P53.34b:** `EXPORT DATABASE` hanya menulis `schema.cypher`+`copy.cypher`, bukan file data → `repair_schema` tak mengembalikan data (count 0) — butuh ekspor file per tabel (Fase 5/`copy.rs`). — `1626645` — gate **1,732 (1,732 passed)**.
- **P53.33 (Fase 3 precision) FLOAT read-back & aritmetika Float** — audit E1 (kairos strengthen_connection `0.9500000000000001 != 0.95`) menyimpulkan engine **sudah IEEE f64-exact** (hasil cocok Python f64 bit-for-bit; ekspektasi harness `== 0.95` mustahil karena `0.9 + 0.05` di f64 = `0.9500000000000001`, Python juga). Audit menyingkap **2 bug nyata** yang di-fix: (1) `DataChunk::get_value` (`akar-common/src/data_chunk.rs`) tak punya arm `PhysicalTypeID::Float` → kolom FLOAT dengan Arrow valid terbaca `None`/null; fix = aksesor `get_f32` + arm `Float` di `get_value`. (2) `arithmetic.rs`/`comparison.rs` (`akar-function/src/scalar/`) tak mempromosikan `Value::Float` → `SET f.x = f.x + 0.05` pada kolom FLOAT menulis `Value::Null` lewat fallback; fix = guard Float-promotion (`numeric_to_f64`) di `add/sub/mul/div/mod_values` (div/mod juga zero-check) + arm `Float↔Double`/`Float↔Int64` di `compare_values` dan cross-type float promotion di `values_equal` (NaN-aware `to_bits`). Semantik kolom FLOAT dipertahankan: storage simpan `Double` verbatim, tiap read round-trip f32. Harness `test_kuzu_compat.py` assert dikoreksi `== 0.95` → `== 0.9 + 0.05` (test_strengthen_connection & test_batch_strengthen_connections). Regression test `test_p5333_f64_ieee_arithmetic.rs` (5 tes: IEEE exact double, prepared param Double/Float, SET accumulation `0.9→0.9500000000000001→1.0`, FLOAT readback non-null, SET FLOAT non-null) — **gagal di kode lama, 5/5 lolos di kode baru**. — `4077355` — gate **1,726 (1,726 passed)**.
- **P53.32 (Fase 2 DELETE) baris null-PK dikeluarkan dari scan** — row soft-delete (null `_id`) masih terbaca `MATCH (h:...)` (scan penuh) sebagai baris `[null]`, sehingga klausa `DELETE` (P52.62) gagal menghapus baris target dan `RETURN` setelah DELETE bisa menampilkan baris yang seharusnya hilang. Fix di `PhysicalScan` (`akar-processor/src/physical/scan_filter/scan.rs`) di KEDUA jalur eksekusi: `execute_with_arrow_arrays` (skip `arrays[pk_idx].is_null(r)`) dan `execute_with_value_data` (skip `data[pk_idx].get(r)` bernilai `Some(Value::Null)`/`None`) — PK-null hanya muncul untuk slot soft-deleted (node insert menolak PK null, P53.27; tabel REL tanpa PK tak terpengaruh). Regression test `test_p5332_delete_row_gone_from_scan` (baris terhapus hilang dari scan penuh) & `test_p5332_delete_then_reinsert_same_pk` (slot terhapus bisa di-reuse). — gate **1,721 (1,721 passed)**.
- **P53.31 (Fase 2 MERGE) `PhysicalMerge::execute` ditulis ulang** — `MERGE ... SET ... RETURN` (kairos `_store_many`/`add_connection` shape) menghasilkan NULL / count-salah karena output merge berupa count-chunk tunggal, dan baris `on_match`/`on_create` yang sebenarnya tak pernah sampai ke klausa SET lanjutan. `execute` kini mengagregasi row id matched/created (`row_id_chunk` berkas `_id`), menerapkan `on_match`/`on_create` via `PhysicalSet` per baris, dan membangun output bernama `[kolom tabel pasca-update] + [kolom pipeline] + [_id]` (kontrak mirip `merge_rel`). Input kosong → chunk kosong (P53.25 mirror); input semua-chunk-kosong → baris sintetik 1 (pipeline create-path tetap berjalan). `record_insert_writes`/`record_set_writes` (`map_update.rs`) kini memilih kolom bernama `_id`/`.*_id` bila ada, fallback legacy kolom 0/1 — OCC tracking konsisten dengan output baru. Regression test `test_p5331_merge_set_return_create_writes_value` (create-path SET+RETURN=500, read-after 500; sebelumnya NULL), `test_p5331_merge_set_return_match_updates_value` (match-path 601), `test_p5331_merge_on_create_set_return` (rute DDL `handle_ddl` mengembalikan sukses tanpa baris — write tetap persist, read-after 700), `test_p5331_unwind_merge_set_multi_row` (2 elemen UNWIND → 2 baris SET benar). — gate **1,721 (1,721 passed)**.
- **P53.30 (Fase 2 SET) output `PhysicalSet` berbentuk baris bernama + `_id`** — root cause P53.31: bare `SET` setelah MERGE/CREATE ter-rute lewat `[Merge, Set, Projection]` dan output SET berupa count-chunk, sehingga `RETURN` resolve ke kolom count dan SET lanjutan menargetkan row id = count (jalur create = out-of-range → value di-drop). Output kini `[kolom count (0, unnamed, nilai `updated`) + kolom tabel pasca-update + kolom pipeline + `_id`]` — kolom count dipertahankan agar kontrak `test_delete_set.rs` (`get_i64(0,0)==1`) tetap berlaku. — gate **1,721 (1,721 passed)**.
- **P53.29 (Fase 2 SET) literal list/embedding ditulis via Arrow** — `SET e.vec = [4.0, 5.0, 6.0]` (kairos `_store_many` FLOAT[]) menulis NULL karena `evaluate_item` memakai `ExpressionEvaluator::evaluate` → `ValueVector` legacy tanpa side-storage List (silent `store_value_in_vector` → `Value::Null`). `evaluate_item` kini memakai `evaluate_to_arrow` (native Arrow: list/map/struct literal bertahan, ekstraksi map P53.26 tetap jalan); `build_old_row_chunk` di-refactor jadi `pub(crate)` berbasis Arrow (`build_arrow_from_values`) dan `append_input_columns` diekstrak sebagai `pub(crate) fn append_pipeline_columns`. Regression test `test_p5329_set_list_literal_roundtrip` (FLOAT[] SET → `List([Double(4.0)...6.0])` persist read-after). — gate **1,721 (1,721 passed)**.
- **P53.28 (Fase 1 UNWIND) UNWIND→MATCH iterasi penuh + kolom vektor dibaca** — probe F harness (Kairos 1.3): `UNWIND $ids AS iid MATCH (m:Memory {id: iid}) RETURN m.id, ..., m.embedding` dulu mengembalikan **1 dari 2 baris** dan **`embedding=NULL`**. Keduanya kini benar: setiap elemen UNWIND menghasilkan baris join, dan kolom vektor FLOAT[] terbaca non-NULL sebagai `Value::List([Double(...)])`. Root cause sudah diselesaikan di P53.26 (join key resolution `m.id = iid` → semua elemen match; output `probe()` memakai Arrow `take` sehingga list/map/struct tidak di-drop ke NULL oleh round-trip `store_value_in_vector`); task ini menambahkan regression test yang mengunci perilaku tersebut. Regression test `test_p5327_p5328_unwind_create_null_pk_vector.rs`: `test_p5328_unwind_match_all_rows_with_vector_column` (2 elemen → 2 baris, embedding non-NULL berisi `Double`) & `test_p5328_unwind_match_vector_column_join_keeps_values` (urutan list terbalik vs id — kedua baris tetap keluar, embedding per-id benar; urutan output join tak dijamin, di-assert per-id). — `9b4b64d` — gate **1,712 (1,712 passed)**.
- **P53.27 (Fase 1 UNWIND) NULL-PK CREATE error tegas** — `PhysicalInsertNode::execute` (`akar-processor/src/physical/write_ops/insert.rs`) memanggil `insert_row_with_txn` lewat `if let Ok(row_id) = ...` sehingga **semua** error insert (termasuk "NULL value not allowed for primary key", duplikat PK, coerce gagal) di-swallow — baris yang gagal hilang diam-diam dari output count (silent no-op; `UNWIND $r CREATE (m:Memory {...})` dengan elemen tanpa `id` membuat 0 baris tanpa error). Storage layer sudah mengembalikan `StorageError` yang jelas; fix = propagasi error ke query (`INSERT NODE row {row} failed in '{table}': {e}`) menggantikan `if let Ok`. Behavior: CREATE tanpa PK → error tegas, bukan skip sunyi; pipeline ter-filter (0 baris) tetap output 0 (P53.25, `all size == 0` early-return tak berubah). Regression test `test_p5327_unwind_create_null_pk_errors_loudly` (NULL-PK → error berisi "NULL value not allowed for primary key", 0 baris dibuat) + `test_p5327_unwind_create_with_pk_inserts_all_rows` (positive control: 3 elemen → 3 node). — `9b4b64d` — gate **1,712 (1,712 passed)**.
- **P53.26 (Fase 1 UNWIND) binding UNWIND visible di clause berikutnya** — alias UNWIND (`row.id`/`iid`/`e.src`) kini ter-resolve setelah MATCH/CREATE/SET; error `Variable 'x' not found in chunk field_names` dan silent no-op hilang. Empat lapis root cause + fix: (1) **`expr_to_value` tak menangani Map** (`akar-processor/src/physical/write_ops/unwind.rs`) — item map UNWIND dievaluasi `_ => Value::Null` sehingga seluruh elemen jadi NULL (silent no-op); kini arm `List` + `Map` (nilai `Value::Map` dengan key `Value::String`). (2) **kolom UNWIND dibangun via `store_value_in_vector` yang membuang Map/Struct ke NULL** (`physical/common.rs`) — `PhysicalUnwind::execute` ditulis ulang membangun kolom unwound sekali lewat `build_arrow_from_values` (dijadikan `pub(crate)`), mempertahankan map/struct/list. (3) **property access `row.id` tak mengekstrak key dari map/struct** — helper baru `map_property_value` (`expression_evaluator.rs`) menangani `Value::Map` dan `Value::Struct`, di-wire ke `evaluate_arrow_property_access`, `evaluate_property_access`, dan `evaluate_expression_for_row` (set.rs). (4) **SET snapshot hanya berisi kolom tabel** — `PhysicalSet::execute` melacak `source_rows: Vec<(chunk_idx, row_idx)>`; `build_snapshot_chunk(rows, input, source_rows)` + `append_input_columns` menempel kolom pipeline non-tabel (UNWIND variable) ke snapshot (dibangun via Arrow agar complex value bertahan). **Join key `m.id = row.id`** — `join_helpers.rs` ditulis ulang (`derive_join_bindings`, `JoinKeyBinding { build_col, probe_col, build_map_key, probe_map_key }`, `split_prop`, `resolve_side`): resolusi 1) qualified prop, 2) bare prop, 3) variabel map + ekstraksi key dengan cross-side fallback; `map_join.rs` materializes kolom sintetis ekstraksi key ke chunk (dedupe per (col,key)), join dijalankan atas kolom baru, lalu `strip_join_synthetic_columns` membuangnya dari output (hash: strip `[build_orig, build_orig+build_appended)` + `[build_orig+build_appended+probe_orig, ...+probe_appended)`; semi/anti: strip `[probe_orig, probe_orig+probe_appended)`). **Output HashJoin drop complex value** — `probe()` di `join_ops.rs` menyalin baris via round-trip Value yang membuang Map ke NULL; kini concat + `arrow::compute::take` per sisi (offsets build/probe) mempertahankan kolom map/struct. **Precedence property access** — snapshots memakai plain column names (`id`, `content`, ...), sehingga `row.content` jatuh ke bare-name match kolom tabel `content`; ekstraksi map-variabel kini dijalankan SEBELUM bare-name match di ketiga jalur evaluator (qualified → bound map-var → bare → legacy). Regression test `test_p5326_unwind_scope.rs` (4 tes): scalar UNWIND→MATCH semua elemen (A1), map UNWIND→CREATE satu node per elemen (A4), map UNWIND→MATCH→SET baca `row.content` (A2/A3), scalar UNWIND→MATCH→SET per elemen. — `0e0cf99` — gate **1,708 (1,708 passed)**.
- **P53.25 (follow-up P53.21) eksekusi rantai OPTIONAL MATCH → CREATE** - `add_bridge_batch` (`UNWIND [1] AS row MATCH (a:Memory {id: row}), (b:Memory {id: 3}) OPTIONAL MATCH (a)-[existing:Connected]-(b) WITH a, b, existing, row WHERE existing IS NULL CREATE (a)-[:Connected {weight: 0.9, type: 'bridge'}]->(b) RETURN count(*)`) kini **bereksekusi penuh**: panggilan pertama membuat edge + `count(*)`=1, panggilan kedua `count(*)`=0 tanpa duplikat. Sebelumnya query ter-bind tapi silent no-op (`RETURN count(*)`=0, 0 edge). Empat lapis root cause: (1) **UNWIND salah rute** - `BoundUnwind` di-push ke `delete_exprs` sehingga `MATCH ... WHERE a.id=row AND b.id=3` menjadi Filter top-level pada pipeline terpisah (join tak pernah terjadi); kini `BoundUnwind` mendorong `LogicalUnwind` ke `scan_ops` dan memasukkan variabelnya ke `available_vars`. (2) **OPTIONAL MATCH sisi kanan tak mampu membawa endpoint senyawa** - sisi kanan berupa ScanRel polos (tak bisa membawa `a`/`b` yang sudah di-join); dibuat operator baru **`LogicalOptionalExtend`/`PhysicalOptionalExtend`**: planner mendeteksi edge-probe (2 pattern, keduanya node var di scope, pattern[0] punya edge bervariabel+berlabel tanpa bound, pattern[1] tanpa edge) dan memancarkan `OptionalExtend` ber-children pipeline kiri (CrossProduct+Filter) yang membawa `row|a.id|...|b.id|...`; `PhysicalOptionalExtend` me-probe adjacency fwd lalu rev (`EdgeDirection::Both`), null-pad kolom edge, dan menambah kolom internal `<rel_var>._id` (non-null iff edge ditemukan) sehingga tabel relasi tanpa properti tetap bisa menjawab `existing IS NULL`. (3) **proyeksi `WITH a,b,existing,row`** - `resolve_projection_column_expand` baru meng-ekspansi `Variable` polos ke semua kolom ber-prefix `{name}.` (fallback posisional bila tak ter-resolve). (4) **`WHERE existing IS NULL`** - evaluator kini meng-handle `UnaryOp::IsNull`/`IsNotNull` atas variabel yang punya kolom ber-prefix: AND dari mask null per-kolom (bukan baca kolom 0). **Bug penyerta:** `PhysicalInsertRel`/`PhysicalInsertNode` selalu mengembalikan count-chunk 1-baris meski input pipeline tersaring habis → `RETURN count(*)` mencatat 1 padahal CREATE di-skip; kini pipeline input yang seluruh chunk-nya ber-size 0 tetap mengembalikan 0 baris. Parser/planner/serializer/optimizer (join_order recursi children, cardinality = child pertama, factorization leaf group) disinkronkan. Regression test `test_p5325_add_bridge_batch_creates_edge_once` (first call=1 edge, second call=0, tanpa duplikat), `test_p5325_optional_match_probe_sees_existing_edge` (probe positif: `existing IS NOT NULL`), `test_p5325_optional_match_probe_undirected_both_directions` (probe reverse dari `(b)-(a)`). Bonus: workspace di-`cargo fmt` (rustfmt max_width=120) + gap build all-features pre-existing di `akar-azure` diperbaiki (`retryable_status` minta `u16`, diterima `StatusCode`). - `f25c2a5` - gate **1,704 (1,704 passed)**.
- **P53.20 (G3) MERGE edge dengan node label-less referensi variabel MATCH-bound** — query Kairos `add_connection` (`MATCH (a:Memory {id: row}), (b:Memory {id: 3}) MERGE (a)-[r:Connected {type: 'similar'}]->(b) SET r.weight = 0.7, ...`) gagal `Bind error: CREATE/MERGE requires a label (table name)`: pattern `(a)-[:Connected]->(b)` di MERGE merefer variabel node yang sudah ter-MATCH, tapi `bind_create_patterns`/`bind_merge_clause` menuntut label. Fix dua lapis: (1) **binder** — `bind_merge_clause(&self, m, variables)` menerima scope; node label-less di-resolve dari variabel bound (`ok_or_else` error jelas bila tak ditemukan), label/table diambil dari binding lama, dan node referensi TIDAK di-push ulang ke `new_vars`. (2) **eksekusi** (melampaui scope bind): MERGE edge kini di-rutekan ke operator baru `LogicalMergeRel` (planner: `build_merge_operators`, arm `BoundClause::BoundMerge` memeriksa pattern edge; optimizer/plan-serializer disinkronkan) → `PhysicalMergeRel` (`akar-processor/src/physical/write_ops/merge_rel.rs`): match-or-create edge via forward/reverse adjacency + `props_match` terhadap properti pattern, `insert_rel` + undo `UndoRecord::insert`, output kolom `<edge_var>._id` agar klausa `SET` lanjutan (standalone setelah MERGE; grammar `merge_clause` tidak menerima plain SET) menemukan baris yang benar; `on_match`/`on_create` diterapkan terpisah per edge. **Bug selama implementasi:** `continue` untuk node referensi melewatkan push edge pada iterasi pattern yang sama → `new_vars=[]` → `Bind error: Variable 'r' not in scope for SET` (ditemukan lewat tes, diganti `if !is_reference`). Regression test `test_p5320_merge_edge_label_less` (satu tes penuh): create → match-update tanpa duplikat (weight/timestamp berubah) → type berbeda membuat edge kedua; schema REL menyertakan kolom Kairos asli (weight/type/created_at/event_time/ingestion_time/valid_from). — gate **1,701 (1,701 passed)**.
- **P53.21 (G3) OPTIONAL MATCH variabel existing** — `add_bridge_batch` (`UNWIND [1] AS row MATCH (a:Memory {id: row}), (b:Memory {id: 3}) OPTIONAL MATCH (a)-[existing:Connected]-(b) WITH a, b, existing, row WHERE existing IS NULL CREATE (a)-[:Connected {..}]->(b) RETURN count(*)`) gagal `Bind error: Variable 'a' already defined`: `bind_optional_match` (mod.rs:1156) memakai `allow_existing=false` sehingga node label-less yang menamai variabel lama ditolak. Fix: `bind_pattern` (jalur `allow_existing=false`) kini memperbolehkan node label-less yang merefer variabel node yang sudah ada di scope (same_node disamakan dengan variabel lama, tidak di-declare ulang). Scope task = binding; eksekusi penuh rantai OPTIONAL MATCH→compound-key join→`existing IS NULL`→CREATE **belum diimplementasi** — query berjalan tanpa error tetapi `count(*)` = 0 (silent no-op, tercatat sebagai follow-up engine). Regression test `test_p5321_optional_match_existing_var` menegaskan query ber-parse dan ter-bind (bukan "already defined"/"Bind error"). — gate **1,701 (1,701 passed)**.
- **Koreksi angka gate** — ekspor HTML resmi `Test Results - test_[akar-core].html` (2026-08-16, 4m2s) mencatat **1,701 total / 1,701 passed / 0 failed / 0 ignored**. Urutan terverifikasi: 1,692 (P53.19) → 1,701 (P53.23 +4, P53.24 +3, P53.20 +1, P53.21 +1). Angka "**1,803**" yang ditulis pada entri P53.23/P53.24 (`2f48ecc`) tidak sesuai ekspor HTML mana pun — fabrikasi sesi lama; angka di entri ini yang benar.
- **P53.23 (latent engine) ORDER BY computed sort key** — `resolve_sort_keys` (`akar-processor/src/processor/mapper/map_projection.rs:52`) untuk sort key **ekspresi computed** (mis. `n.a + n.b`, `array_cosine_similarity(...)`) memanggil `resolve_projection_column_index` yang hanya menangani PropertyAccess/Variable → `None` → fallback posisional `col.unwrap_or(i)` memetakan ke kolom ke-i output, jadi `RETURN n.id, n.a, n.b ORDER BY n.a + n.b` mengurutkan berdasarkan `n.id` (silent wrong result; terkena `PhysicalOrderBy` & `PhysicalTopK`/LIMIT). Fix: `resolve_sort_keys` kini mengevaluasi sort key computed per baris via `ExpressionEvaluator` (registry + sequence/subquery handler) atas chunk output proyeksi, menempelkannya sebagai kolom sintetik trailing pada salinan input, menyortir, lalu `strip_sort_columns` membuang kolom sintetik dari output (`take_global_rows` menyalin semua field). Sort key kolom polos tetap resolve by-name. **Bonus fix:** `evaluate_variable` (legacy, `expression_evaluator.rs:658`) punya fallback senyap ke kolom 0 saat variabel tak ditemukan di `field_names` → ORDER BY key yang merefer kolom NON-proyeksi (invalid Cypher, mis. `RETURN b.title ORDER BY b.id`) kini error jelas, bukan urut-salah diam-diam. Regression test `test_p5323_order_by_computed.rs` (4 tes, data anti-korelasi): DESC, error non-projected, TopK+LIMIT, mixed computed+column; `integration_test.rs` DDL-alter di-update mem-proyeksi `b.id`. — `2f48ecc` — gate **1,803 (1,803 passed)**.
- **P53.24 (latent engine) list subscript `x[i]`** — grammar `cypher.pest` `postfix_expr` memunculkan indeks `[i]` sebagai child `Rule::expression`, tapi handler `expression.rs:150` hanya memproses child `Rule::property_access` → `RETURN n.embedding[0]` parse menjadi `PropertyAccess(n, "embedding")`, index di-drop diam-diam (mengembalikan seluruh list; `m["k"]` juga jadi `m`). Fix: child `Rule::expression` kini di-rewrite menjadi `list_extract(x, i + 1)` — subscript Cypher 0-based, `list_extract` 1-based (`akar-function/src/scalar/list.rs:18`, idx>=1, `*i-1`). Keterbatasan terdokumentasi di audit: index OOB → error (bukan NULL), index string → error. Regression test: parser `test_list_subscript_index_kept` (AST `list_extract(n.embedding, 0+1)`) & `test_list_subscript_chain_and_expr_index` (`a[1].b + x[id].c`); integrasi `test_nested_types.rs` di-update — `t.lst[1]` → `Int64(20)`, `t.lst[0]` → `Int64(10)` (baru), `t.lst[10]` → error "out of bounds" (sebelumnya mengembalikan seluruh list). — `2f48ecc` — gate **1,803 (1,803 passed)**.
- **P53.19 (G8) EXPORT/IMPORT DATABASE path quoting** — string literal path (`EXPORT DATABASE "C:/..."`, kairos) kini di-strip lewat `unescape_string` (`expression.rs:402`) alih-alih `trim_matches('\'')` yang hanya menghilangkan kutip tunggal → path kutip-ganda dianggap termuat kutip literal & directory dibuat di path yang salah. Fix di 3 situs `akar-parser/src/parser/ddl.rs`: `parse_export_database` (:143-144), nilai `export_option` (:154), `parse_import_database` (:173). Regression test `test_export_database_double_quoted_path` (double+single quote) & `test_import_database_double_quoted_path` — akar-parser 82/82 hijau. — `df6bd0c` — gate **1,692 (1,692 passed)**.
- **P53.19 (G9) QUERY_VECTOR_INDEX translation** — hasil translasi `CALL QUERY_VECTOR_INDEX(...) RETURN node, distance` → `MATCH ... RETURN {..} AS node, array_cosine_similarity(...) AS distance ORDER BY ...` menghasilkan urutan jarak yang SALAH. Akar masalah di engine: `resolve_sort_keys` (`akar-processor/src/processor/mapper/map_projection.rs:52-61`) untuk ORDER BY **ekspresi computed** (bukan variable/property) memanggil `resolve_projection_column_index` yang mengembalikan `None` → fallback posisional `col.unwrap_or(i)` memetakan ke kolom 0 (id), jadi `ORDER BY array_cosine_similarity(...) DESC` mengurutkan berdasarkan kolom salah. Fix di sisi translasi (`build_vector_query`, `akar-python/src/lib.rs`): `ORDER BY distance DESC` memakai alias proyeksi (resolve via P53.16) alih-alih mengulang ekspresi cosine. Regression test `p5319_g9_query_vector_index_translation` (4 node bertetangga, LIMIT 2, assertion DESC non-increasing + baris pertama ~1.0) — akar-python 24/24 hijau (standalone, gate cargo tak berubah dari sisi python; naik dari tes parser G8). **Latent bug engine tercatat:** ORDER BY ekspresi computed apa pun jatuh ke fallback posisional; `n.embedding[0]` mengembalikan seluruh list (index diabaikan) — secondary notes, bukan scope G9. — `df6bd0c`
- **P53.18 (G7) reopen-after-close** — `db.close()` lalu `Database(path sama)` kini bisa dibuka lagi (sebelumnya `Database '...' is already open by another process` �?" `close()` tak melepas lock). Akar masalah: wrapper Python `Connection` memegang `conn: akar_main::Connection` yang menyimpan `Arc<Database>` (`connection/mod.rs:68-69`); `Database.close()` hanya set `self.db = None`, tapi Arc dari connection membuat `Database` (beserta file lock `_lock_file`) tetap hidup selama wrapper Python masih direferensikan �?" pola kairos close�?'checkpoint�?'reopen G7. Fix di `akar-python/src/lib.rs`: (1) `Connection.conn` kini `Option<akar_main::Connection>`; `close()` drop Arc; helper `conn()` error `"Connection is closed"` bila None; semua jalur eksekusi (`run`/`execute_action`/`table_exists`/`ensure_table_schema`/`build_vector_query`) menerima `&akar_main::Connection` hasil `self.conn()?`. (2) `Database` melacak connection-nya: `connections: Vec<Py<Connection>>`; `Connection::new` (kini `#[new] -> PyResult<Py<Self>>`) mendaftarkan `clone_ref(py)` ke database; `Database.close(py)` menutup paksa semua connection (set `conn=None`) lalu `db=None` �?" lock dilepas meski wrapper Python masih hidup. (3) Guard baru: `Connection::new` setelah DB ditutup error `"Database is closed"`; query setelah `Connection.close()` error (bukan panic). 3 tes regresi (`db_close_releases_lock_even_with_live_connection`, `connection_close_releases_lock_before_db_close`, `closed_database_and_connection_reject_use`) �?" akar-python 23/23 hijau (standalone workspace, bukan member akar-core; gate cargo tidak berubah).
- **P53.17 (G6) SET aritmetika & multi-item atomic** — `SET m.x = m.x + 1` kini membaca nilai lama sebelum write (sebelumnya NULL/0 karena fallback `evaluate_expression_for_row` lama sekadar `chunk.get_value(1, row)` — baca kolom 1, tak pernah mengevaluasi ekspresi), dan klausa SET multi-item (`SET m.a = ..., m.b = m.a * 10`) diterapkan atomik terhadap **satu snapshot pra-write** (semua item dievaluasi dari nilai lama, sesuai semantik Cypher/Neo4j). Restrukturisasi dua lapis: (1) planner — `LogicalSet` kini memegang `items: Vec<SetItem>` (`column_name`, `column_idx`, `value`); `BoundSet` mengelompokkan item satu klausa per (table_name, table_id, is_node) menjadi SATU `LogicalSet` (bukan rantai `[Set(a), Set(b)]` yang membuat item berikutnya hanya menerima count-chunk operator sebelumnya → `row_id_column_index` tak menemukan `_id` → `unwrap_or(0)` membaca nilai count sebagai offset baris; rantai multi-table tetap satu `LogicalSet` per table, klausa SET terpisah tetap op terpisah); on_match/on_create MERGE membungkus tiap item `vec![item]`; serializer, mapper, dan optimizer `projection_pushdown` disinkronkan. (2) eksekusi — `PhysicalSet` mengevaluasi tiap `SetItem` terhadap `DataChunk` snapshot yang dibangun dari data OLD tabel (plain column names sebagai `field_names`), memakai `ExpressionEvaluator` (registry) dengan fallback `evaluate_expression_for_row` yang diperluas (Variable/PropertyAccess/UnaryOp/BinaryOp via `binary_value_op`/List/Map); col_idx di-resolve by name per item; output tetap count-chunk (kontrak `test_delete_set`). Regression test `test_p5317_g6_set_arithmetic.rs` (8 tes): self-reference increment, kombinasi kolom, multi-item old-state, multi-item self-reference, property→property, function call, float arithmetic, filtered rows. Catatan: `MERGE ... SET` single item (chain logical rusak: Merge output count-chunk) adalah bug pre-existing di luar scope G6. — gate **1,690 (1,690 passed)**.
- **P53.16 (G5) RETURN/WITH `AS` alias dihormati** — `RETURN count(m) AS cnt` kini mengekspos kolom `cnt` (sebelumnya nama turunan `COUNT(m)`), `RETURN m.age AS a` → `a`, dan `ORDER BY cnt`/`ORDER BY a` meresolve alias proyeksi (sebelumnya `Variable 'cnt' not in scope`). Tiga akar masalah: (1) `bind_return` membuang `ReturnItem.alias` — `BoundExpression` kini membawa `alias: Option<String>` (31 situs konstruktor `alias: None`; `substitute_in_bound_expr` mempertahankan alias) dan `bind_return` menyalinnya + ORDER BY variabel polos yang cocok dengan alias proyeksi di-resolve ke kolom proyeksi (alias menaungi variabel scope). (2) optimizer `AggregateDetection` membangun ulang ekspresi proyeksi dengan `alias: None` — sekarang selalu `be.alias.clone()`, dan Projection **selalu dipertahankan di atas Aggregate** (tidak lagi hanya untuk nested) agar alias diterapkan terpusat. (3) `ConstantFolding` membangun ulang projection dengan `alias: None` — kini `e.alias.clone()`. Processor: `map_projection` menamai kolom output dari alias di jalur eval & jalur plain-column (rename chunk hasil `PhysicalProjection`), `expression_field_name(alias, expr)`; `map_aggregate` penamaan group-by menyamai `expression_field_name` (PropertyAccess → `var.prop`) agar resolve-by-name presisi. Regression test `test_p5316_g5_alias_return.rs` (7 tes): alias aggregate, nested aggregate, property, constant, ORDER BY alias (data anti-korelasi), group-by+aggregate+ORDER BY alias, no-alias mempertahankan `COUNT(m)`. — gate **1,682 (1,682 passed)**.
- **P53.15 (G4) nested aggregate** — `COALESCE(MAX(m.id), 0)`, `COALESCE(MAX(s.id), 0) + 1`, `COALESCE(SUM(x), 0)` (kairos `_next_id`/`add_revision`/stats; sebelumnya `Unknown function: 'MAX'`) kini bind & eksekusi. Dua akar masalah: (1) optimizer Pass 6 `AggregateDetection` hanya mendeteksi aggregate yang SELURUH ekspresi proyeksi adalah satu panggilan top-level (`RETURN MAX(x)` bekerja, `RETURN COALESCE(MAX(x),0)` tidak). Ditulis ulang menjadi rekursif — `contains_aggregate`/`rewrite_aggregates` mengekstrak tiap panggilan aggregate (COUNT/SUM/AVG/MIN/MAX/STDDEV/VARIANCE/COLLECT) ke operator Aggregate dan menggantinya dengan `Variable(aggregate_ref_name(...))` (nama wajib sinkron `aggregate_field_names` di `map_aggregate.rs`, mis. `MAX(m.id)`/`COUNT(*)`); proyeksi luar tetap dipertahankan DI ATAS Aggregate sebagai pasangan flat `[Aggregate, Projection]` (wrapper Projection ber-children kosong, mengevaluasi ekspresi luar atas chunk hasil aggregate; tak bisa masuk ke children Projection karena `execute_children` menjalankan pipeline internal ber-input kosong). (2) `evaluate_arrow_function_call`/`evaluate_function_call` menerapkan null-shortcut SQL (`arg null → hasil null`) dengan pengecualian `name != "coalesce"` **case-sensitive** — binder mempertahankan huruf SQL, jadi `COALESCE` uppercase dengan arg NULL (mis. MAX tabel kosong) di-shortcut ke NULL, bukan fallback 0; pengecualian kini `eq_ignore_ascii_case`. Semua ekspresi non-aggregate menjadi group-by key. Perilaku top-level lama dipertahankan (Projection di-drop). Regression test `test_p5315_g4_nested_aggregate.rs` (8 tes): MAX/SUM nested, fallback tabel kosong (0), pola `+1` kairos, group-by+nested, top-level, campuran top+nested. **Bug penyerta yang diperbaiki:** COALESCE/IFNULL uppercase dengan arg NULL (tabel berisi row null) kini mengembalikan fallback, bukan NULL. — gate **1,675 (1,675 passed)**.
- **P53.22 (pre-existing all-features build gaps)** — `cargo check --workspace --all-features` (pre-commit gate) gagal di kode extension fitur non-default yang selama ini tak pernah di-compile gate (regresi `7c601a5` batch E/F): (1) `akar-postgres` fitur `native` — cache koneksi memanggil `client.clone()` padahal `tokio_postgres::Client` 0.7.18 **tidak** impl `Clone` (E0599 + E0308); cache kini menyimpan `Arc<Client>` (satu koneksi shared per `conn_str`, `Arc::clone` di arm cache-hit). (2) `akar-duckdb` fitur `bundled` — `DuckDbManager::shared_in_memory()` memanggil `in_memory()` bare di closure `get_or_init` (E0425); diubah ke `Self::in_memory()`. Verifikasi: `check [akar-core]` (release, all-features) **hijau 1m20s**; gate `test [akar-core]` tak terpengaruh (keduanya di balik fitur non-default). Catatan + temuan cleanup `target/` di `docs/audits/audit-pre-existing-gaps-build-features.md` (audit, git-excluded): artefak basi `criterion`/`package`/`release-debug`/`tmp` + semua `kuzu-*` (13) + `libduckdb-sys-*` (10) + `libsqlite3-sys-*` (6) dihapus → **+25 GB** ruang disk (186.2 → 211.5 GB bebas). — `8145cfa` — gate **1,667 (1,667 passed)**.
- **P53.14 (G3) grammar chain clauses** — `query_statement` di `cypher.pest` digeneralisasi menjadi `query_clause+ ~ return_clause? | return_clause` (pest menolak `*` karena `statement` alternation bisa match-empty → "following choices cannot be reached"); `query_clause` membungkus match/optional-match/where/unwind/with/set/delete/create/merge/foreach sehingga klausa boleh dikombinasi bebas urutan. `parse_query_pairs` ditulis ulang meng-iterasi anak `query_clause` (WHERE dipancarkan sebagai `Clause::Where` tersendiri agar struktur `[Match, Where, Return]` lama dipertahankan); MERGE standalone tetap didispatch ke `Statement::Merge`, selain itu disematkan sebagai `Clause::Merge`. Binder: `bind_merge` di-refactor jadi `bind_merge_clause` yang juga memancarkan variabel baru (node + edge pattern); arm `Clause::Merge` di `bind_query`; `BoundClause::BoundMerge`. Planner: `BoundWhere` mid-pipeline dialihkan ke `delete_exprs` sebagai `Filter` saat ada update pending; arm `BoundMerge` mendorong `LogicalMerge`; perakitan diurutkan ulang `scans → extends → Filter → delete_ops → Projection → [Aggregate] → OrderBy → Limit` sehingga SET/DELETE/MERGE berjalan sebelum proyeksi RETURN. Terverifikasi: 7/7 rantai G3 Kairos (strengthen_connection, set_meta, _next_id, add_connection, add_bridge_batch, set_then_return, delete_then_return) parse tanpa error; `MERGE (m:Meta {key:'k'}) SET m.value='v'` mengeksekusi & membuat row. 8 tes parser + 2 tes integrasi baru. **Tertunda (binder, task lanjutan):** `add_connection` (merge-pattern node label-less yang merefer variabel MATCH-bound → "CREATE/MERGE requires a label") & `add_bridge_batch` (OPTIONAL MATCH variabel yang sudah ada → "Variable 'a' already defined"; `bind_optional_match` perlu `allow_existing=true`). — `6c59b3a` — gate **1,667 (1,667 passed)**.
- **P53.13 (G2) List NULL di ORDER BY/LIMIT/TOP-K** — `PhysicalLimit` (partial-chunk), `PhysicalOrderBy`, dan `PhysicalTopK` mem-materialisasi baris output lewat round-trip legacy `ValueVector` (`get_value`/`store_value_in_vector`) yang di arm catch-all-nya **membuang `Value::List` (dan Struct/Array) jadi NULL** — kolom skalar selamat, jadi `MATCH (m:Memory) RETURN m.embedding LIMIT 1` / `ORDER BY m.id` mengembalikan embedding NULL. Fix: helper `take_global_rows()` (`akar-processor/src/physical/common.rs`) memakai Arrow `take` atas array sumber (concat bila chunk > 1) — ketiga operator dialihkan ke sana, kolom complex dipertahankan. Regression test `test_p5313_list_sort_limit.rs` (5 tes, 3-row chunk agar partial-copy terpicu): LIMIT, LIMIT+SKIP, ORDER BY ASC/DESC, ORDER BY+LIMIT. Terverifikasi gagal di kode lama (embedding NULL) & lolos setelah fix. — `79c75f6` — gate **1,658 (1,658 passed)**.
- **P53.12 (G1) rel-scan target binding** — `PhysicalExtend::execute` meresolve baris node destinasi lewat map `pk_to_row` (PK→row) padahal `RelTable.fwd_adj`/`rev_adj` menyimpan **row offset** (bukan PK). Saat PK ≠ row offset (mis. PK mulai dari 1), `MATCH (a:Memory)-[r:Connected]->(b:Memory)` membind `b` ke baris `a` → `RETURN a.id, b.id` mengembalikan `{1,1}` bukan `{1,2}` (`r.weight` sudah benar karena dibaca via `edge_idx`). Fix: hapus map `pk_to_row` di `PhysicalExtend::execute`, resolve baris dest langsung via `dst_offset` (row offset ke node table tujuan); skip-check pakai `dest_num_rows`. Regression test `test_p5312_rel_scan_binding.rs` (4 tes, PK sengaja ≠ offset): binding forward, arah balik, rel-props, multi-hop. — `efa78cf` — gate **1,653 (1,653 passed)**.
- **P53.1 blocker DDL `BOOLEAN`** — `primitive_type` grammar mencoba `"BOOL"` sebelum `"BOOLEAN"` (pest alternatif berurutan tanpa longest-match) → `protected BOOLEAN` (kairos `_ensure_schema`) ter-potong jadi `BOOL`+`EAN`, gagal parse. Urutan dibalik: `"BOOLEAN"` sebelum `"BOOL"`. Regression test `test_create_node_table_boolean_types`. — `b93e966` — gate **1,649 (1,649 passed)**.
- **P53.12 (complex-type query output)** — kolom & literal List/Struct/Map kini round-trip melewati pipeline Arrow alih-alih kolaps NULL (4 lapis: `scan.rs` `logical_to_physical` memetakan List/Array/Map/Struct/Union ke `PhysicalTypeID::List`/`Struct`; `build_arrow_array` (scan) & `to_arrow_array` (storage) + `ArrowVector::from_legacy` + `build_arrow_from_values` (evaluator) membangun `ListArray`/`StructArray` via helper baru `arrow_array_from_values` yang menangani tipe kompleks/nested rekursif; proyeksi map/function dialihkan ke `evaluate_arrow` (bukan ValueVector yang tak punya side-storage); `evaluate_expression_for_row` & `evaluate_constant_expr` (write path CREATE/SET) mengevaluasi literal List/Map rekursif sehingga `FLOAT[]` tersimpan nyata; `DataChunk::get_value` punya arm List/Struct). Regression test feature-gated `test_vector_index.rs`: `RETURN n.embedding`→List, `RETURN {id:n.id}`→Struct, `array_cosine_similarity`→Double; `test_nested_types.rs` di-update (list/struct kini round-trip; `size()`, `list_concat`, nested list bekerja). Catatan: subscript `t.lst[i]` masih di-drop parser (gap terpisah). — `46de39d` — gate **1,653 (1,648 passed)**.
- **P53.13 (hang)** — `bind_create_vector_index` self-deadlock: guard `catalog` #1 tak di-drop sebelum `lock()` #2 (re-entrant `MutexGuard` = hang `WaitOnAddress`). `CREATE VECTOR INDEX` kini selalu ter-bind (sebelumnya hang untuk metric valid). Guard #1 di-scope. Regression test `test_vector_index.rs` (watchdog 15s). Audit 24 call-site `catalog.lock()` — hanya fungsi ini yang double-lock. — `24e9610` — gate **1,649 (1,644 passed)**.
- **P53.14 (flake FTS)** — `PhysicalCreateFtsIndex::execute` memegang DashMap `Ref` (`source_table`) sambil acquire write lock (`create_node_table`/`get_*_mut`) → read→write di shard sama = re-entrant deadlock; seed acak DashMap → tabrakan shard intermiten (flake "lolos isolasi, hang suite penuh"). Fix: snapshot schema+data dalam block, `Ref` di-drop sebelum write. Reproduksi load: hang sebelum fix, 10/10 lolos sesudah; gate penuh 3m42s bersih. — `24e9610` — gate **1,649 (1,644 passed)**.
- **Batch bug P51.28–P51.36** — `DROP TABLE` kini hanya menghapus sequence milik tabel itu sendiri (enumerasi kolom SERIAL exact-match menggantikan prefix-filter `{name}_*_serial` yang menghapus sequence tabel ber-prefix sama, P51.28); commit tulis jadi two-phase: `prepare_commit` (validasi OCC + assign commit_ts + deregister agar checkpoint drain tak menunggu txn sendiri) → durable pipeline (`WAL` → `persist_all_tables`) → `finish_commit` (publish commit_history + release lock); `commit_write_txn` di-reorder mengikuti urutan itu (P51.29); UNION kini menjalankan kedua sisi dengan snapshot MVCC (`with_snapshot`) sehingga tak mencampur baris committed/uncommitted (P51.30); prepared statement diperluas: koleksi + substitusi parameter di pattern properties (MATCH/OPTIONAL MATCH/CREATE), ORDER BY, SET, DELETE, UNWIND, FOREACH sub-statements, CREATE DML, dan MERGE (termasuk ON CREATE/ON MATCH) (P51.31); `LIMIT`/`SKIP` non-u64 (negatif/overflow) kini error tegas, bukan silent-drop (P51.31); `value_to_constant` → `Result<Constant, String>` — UInt64/Int128 overflow error, tipe BLOB ditolak, tak lagi fallback `Constant::Null` (P51.32); CLI: completer aman multibyte (`floor_char_boundary`), Double/Float ditampilkan presisi penuh (bukan `{:.4}`), JSON escape lengkap (`\`, `\n`, `\r`, `\t`, kontrol <0x20) (P51.35); WASM: konversi JS number ke i64 dibatasi rentang pasti, `Database::new` memory-only di browser dengan pesan ramah (P51.36) — 9 tes baru (4 integrasi, 3 parser, 2 unit) — `f901425` — gate **1,647 (1,642 passed)**.
- **P51.17** — HNSW `insert` kini menghormati `_id` sebagai node id: `HnswIndex.nodes` diganti `BTreeMap<usize, HnswNode>` (id sparse diizinkan) sehingga hasil `search` selalu mengembalikan row id asli — sebelumnya node index posisional dipakai sebagai row id dan rusak bila baris NULL/non-vector di-skip saat populate (lookup lalu membaca baris yang salah). `VectorIndexTable::save` menulis node id sebenarnya agar id sparse tahan persist/load (file format lama tetap terbaca). 2 tes baru (`test_insert_honors_sparse_ids`, `test_vector_index_roundtrip_preserves_ids`) — `d5b13c1` — gate **1,638 (1,632 passed)**.
- **Batch G P52 (rekonsiliasi audit)** — art_index: `num_entries` kini ter-increment saat dup-key insert, `serialized_tree_size` dihitung tanpa materialisasi (anti-OOM), prefilter range pakai `upper_inclusive` (P52.51); wal_replayer: first-pass dead dihapus + batch data di-discard saat Rollback (P52.52); checkpoint drain gate kini di-lock `begin_read`/`begin_write` (P52.53); UnwindDedup key (variable, expression) + hanya dedup berurutan (P52.54); LimitPushDown guard ORDER BY/top-k/aggregate (P52.55); hasil agregat punya `field_names` (P52.56); graph output_writer ikuti predecessor chain + depth-guard + dst benar (P52.57); scan Int128/InternalID tak lagi NULL diam-diam (P52.58); akar-llm embedding non-numeric → error & arg override provider/model aktif (P52.59); httpfs `http_get` body dibatasi 64 MiB (P52.60); FFI akar-c ekspor `error_message` via out-param + `akar_error_message_free` (P52.61) — `ef4f143` — gate **1,636 (1,631 passed)**.
- **Batch F P52** — akar-json: doc header dikoreksi (fungsi yang benar-benar ada saja), `json_extract` path hilang → NULL bukan error (query abort), `json_structure` inspeksi semua elemen array (bukan `arr[0]`), depth-guard pada `structure_inner`/`contains_inner` (P52.49); `SET spill_threshold=0` kini benar-benar menonaktifkan spilling (bukan fallback ke default) & `CALL current_setting('concurrent_writes')` baca toggle runtime live (P52.50) — `ca28ffd` — gate **1,628 (1,623 passed)**.
- **Batch 5 P51** — optimizer: DP join reorder dibatasi per-segmen pipeline (tak menyebrangi batas WITH/ORDER BY/LIMIT, P51.23); kondisi join diekstrak dari konjungsi AND di WHERE (P51.24); constant-folding float pakai `==` exact (bukan epsilon) agar selaras runtime (P51.25); aritmetika i64 pakai checked ops agar tak panic/wrap saat planning (P51.26) — `5050b21` — gate **1,619 (1,614 passed)**.
- **Batch HIGH P51** — remote client race dikunci satu mutex (write+read, P51.9); prepared-writes kini ber-OCC (P51.10, tercakup P52.18); CLI script-mode terdeteksi via `stdin.is_terminal()` bukan env/`cfg!(windows)` (P51.13); Delta `load_delta_table` me-replay semua versi log (P51.15); HNSW beam search dibatasi (heap, prune ef-th, search per-layer) — O(ef·M) bukan O(N²) (P51.16); FTS `tokenize()` kompilasi regex sekali via `LazyLock` (P51.18); `persist_all_tables` redundant per-statement dihapus (P51.27) — `3237ad7` — gate **1,619 (1,614 passed)**.
- **P52.18** — isolasi txn putus dari SQL write path: tulis kini SELALU dibungkus txn (mode single & concurrent writer); `txn_id` dialirkan ke operator tulis (insert/delete/set/merge/copyfrom/batchinsert) via `insert_row_with_txn`/`delete_row_with_txn` sehingga `VersionInfo`/`commit_history` terisi dan `is_row_visible` nyata (uncommitted insert/delete tak terlihat reader lain); undo ditangkap tiap op tulis + DDL inline (CreateDml/Merge) dan diterapkan saat rollback/konflik OCC — termasuk rel table (`rollback_transaction` di-extend) — `b35fdf4` — gate **1,619 (1,614 passed)**.
- Batch E: storage/execution/vector/graph + DML DELETE/SET row-id bug (P52.29/P52.31/P52.38–P52.45/P52.47–P52.48/P52.62) — `61b5e0e` — gate 1,607 (1,602 passed).
- Batch E/F: connectors + extension compile fixes — azure/httpfs/duckdb/postgres/iceberg/neo4j (P52.30/P52.32–P52.37/P52.66 + P51.19/P52.33/P52.34) — `7c601a5` — gate **1,619 (1,614 passed)**.

## [0.1.5] - 2026-08-11

### Changed

- Docs: mark release v0.1.5 — `a4380d3` (gate 1,594 hijau).

### Fixed

- CI: test `ice disk path` dibuat platform-independent — `0304a3c`.

## [0.1.4] - 2026-08-10

### Changed

- Baseline final (worktree committed) — gate **1,594 hijau**.
- Rilis v0.1.4: sync backlog crates.io → HEAD bottom-up, 15 crate naik versi — `e158dd3`.

### Fixed

- Batch C: connection txn safety + EXPORT/IMPORT + remote drain (P52.12–P52.15/P52.19/P52.23–P52.28) — `e3c7b5d` — gate 1,581 hijau.
- Batch D: storage correctness (P52.16/P52.17/P52.20/P52.21/P52.22) — `2498794` — gate 1,588 hijau.

## [0.1.3] - 2026-08-10

### Fixed

- Batch 2: MERGE/CREATE multi-pattern + parser + binder cleanup (P51.7/P51.8/P51.37/P51.38) — `a2210e2` — gate 1,548 hijau.
- Batch 3: FFI panic containment + query-result destroy + server bounds (P51.11/P51.12/P51.33/P51.34) — `aca6520` — gate 1,551 hijau.
- Batch 4: connectors — injection, temp-file, all-rows, chunk-fill, DRY (P51.14/P51.20/P51.21/P51.22/P51.39) — `8a7f2df` — gate 1,569 hijau.
- Batch A: execution-engine silent-wrong-result (P52.1/P52.3/P52.8/P52.9/P52.10/P52.11) — `d62c55b` — gate 1,569 hijau.
- Batch B: optimizer passes; 4 → NO-OP terdokumentasi (P52.2/P52.4/P52.5/P52.6/P52.7) — `8564faa` — gate 1,578 hijau.

## [0.1.2] - 2026-08-09

### Changed

- Rilis v0.1.2 (4 crate) — `fa52bf2`.

### Fixed

- Batch 1: correctness SQL inti (P51.1–P51.6) — `9c173da` — gate 1,543 hijau.

---

**Status publish (2026-08-10):** semua 31 crate publishable live di crates.io — akar-storage & akar-main @ 0.1.3, akar-binder/planner/processor/postgres @ 0.1.2, akar-optimizer @ 0.1.2, akar-common/parser @ 0.1.1, akar-server/httpfs/duckdb/sqlite/azure/delta/iceberg/unity-catalog @ 0.1.1, sisanya @ 0.1.0 (rilis v0.1.4 sync backlog crates.io → HEAD, bottom-up, 2026-08-10); akar-c `publish=false`; repo GitHub **public**; tag **v0.1.1** + **v0.1.2** + **v0.1.3** + **v0.1.4** + **v0.1.5** + GitHub Releases live.

[Unreleased]: https://github.com/mlengse/akar/compare/v0.1.10...HEAD
[0.1.10]: https://github.com/mlengse/akar/compare/v0.1.9...v0.1.10
[0.1.9]: https://github.com/mlengse/akar/compare/v0.1.8...v0.1.9
[0.1.8]: https://github.com/mlengse/akar/compare/v0.1.7...v0.1.8
[0.1.7]: https://github.com/mlengse/akar/compare/v0.1.6...v0.1.7
[0.1.6]: https://github.com/mlengse/akar/compare/v0.1.5...v0.1.6
[0.1.5]: https://github.com/mlengse/akar/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/mlengse/akar/compare/v0.1.3...v0.1.4
[0.1.3]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/mlengse/akar/compare/v0.1.1...v0.1.2
[0.1.20]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.20
[0.1.19]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.19
[0.1.18]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.18
[0.1.17]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.17
[0.1.16]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.16
[0.1.15]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.15
[0.1.14]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.14
[0.1.13]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.13
[0.1.12]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.12
[0.1.11]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.11
[0.1.1]: https://github.com/mlengse/akar/releases/tag/v0.1.1
