# Changelog — Akar

> Riwayat revisi Sprint 19 (P51/P52), dipisah dari `implementation plan.md` agar plan hanya berisi pekerjaan yang belum dikerjakan. Fase selesai P1–P50 + AUDIT → [`SPEC.md`](SPEC.md) & git history.
> Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) · Versi: [SemVer 2.0.0](https://semver.org/). Gate = `test [akar-core]`.

## [Unreleased]

### Added

- **Batch C — pytest compat harness (P53.9)** — `akar-python/tests/test_kuzu_compat.py` + `conftest.py` menjalankan metode asli `KuzuDBStore`/`KuzuDBDreamBackend` Kairos (schema bootstrap, store, store_many, get/get_many, connections, search_semantic, revision, stats, export/repair_schema, CHECKPOINT) terhadap `import akar` pada temp DB per-test. Gap-capture via hook pytest `pytest_runtest_makereport`/`pytest_sessionfinish` di `conftest.py` (hook wajib di conftest, bukan test module) → `tests/test_kuzu_compat.gap_report.json`. Hasil (2026-08-14): **34 failed / 17 passed**; 9 gap engine G1–G9 di-identifikasi + diverifikasi probe langsung — rel-scan target binding `b.id == a.id` (G1), List/FLOAT[] → NULL di ORDER BY/LIMIT (G2), grammar chain clauses `cypher.pest` (G3), nested aggregate `COALESCE(MAX/SUM())` (G4), alias RETURN dibuang (G5), `SET m.x=m.x+1` → NULL/0 (G6), reopen-after-close (G7), EXPORT DATABASE path quoting (G8), QUERY_VECTOR_INDEX translation rapuh (G9) → task lanjutan P53.12–P53.19 di `implementation plan.md`; catatan lengkap di `docs/audits/audit-p53-compat-harness.md`. Artefak generated (`*.gap_report.json`, `__pycache__/`, `.pytest_cache/`) di-git-ignore. Gate: Python-side (pytest, tidak mengubah gate cargo 1,649). — `39fa230`.
- **Batch A+B akar-python (drop-in KuzuDB, P53.1–P53.8)** — translation layer `translation.rs` + `param_interp.rs` + executor `lib.rs`: DDL idempoten — `CREATE NODE/REL TABLE IF NOT EXISTS` → cek `CALL show_tables()` lalu skip/create; `DROP TABLE IF EXISTS` → swallow "not found" (P53.1); `FLOAT[n]` → `FLOAT[]` + registri dims dari DDL (P53.2); `ALTER TABLE ... ADD col TYPE DEFAULT <lit>` → strip DEFAULT + swallow "column already exists" (P53.3); `CALL CREATE/DROP/QUERY_VECTOR_INDEX` → SQL Akar (brute-force MATCH + ORDER BY ekspresi cosine penuh, workaround limit-alias) (P53.4); syntax Kuzu `CREATE VECTOR INDEX IF NOT EXISTS ... FOR/ON/OPTIONS` (P53.5); multi-statement split + `INSTALL`/`LOAD`/`UNINSTALL EXTENSION` no-op (P53.6); interpolasi `$param` → literal ter-escape (string/int/float/bool/list/dict/null, validasi nama `[A-Za-z_][A-Za-z0-9_]*`) (P53.7); `RETURN node` → dict via registri schema + fallback `table_info` (P53.8). Infra tes: pyo3 `auto-initialize` (memungkinkan `cargo test`), `rustfmt.toml` 120-kolom + `cargo fmt`, tes integrasi end-to-end `kairos_ensure_schema_bootstrap_is_idempotent` (bootstrap `_ensure_schema` 2× idempoten + DROP IF EXISTS + ALTER ADD DEFAULT). — `b93e966` — gate **1,649 (1,649 passed)**.
- GDS table functions membaca graf katalog nyata — closure menerima `Option<&dyn GraphDataSource>` lewat `TableFunction::CustomTableWithGraph` + `CatalogGraphSource` (P52.46) — `0290a8c`.

### Removed

- **5 doc-test `ignore` blok** — snippet ilustratif yang tak bisa di-compile standalone dihapus dari doc comment (`akar-storage/column_chunk.rs`, `lazy_scanner.rs`, `spiller.rs` ×2, `akar-main/prepared_statement.rs`) agar tak tercatat sebagai "ignored" test yang membingungkan; tersisa 8 doc-test lulus (0 ignored). Bonus: `test_migration_ingestion` kini jalankan binary `akar-migrate` via `env!("CARGO_BIN_EXE_akar-migrate")` (bukan nested `cargo run` yang memicu rebuild). — `6d6cded` — gate **1,648 (1,648 passed)**.

### Changed

- **P51.50/P51.51 (KISS) dead-code warning build `--all-features` di-silence** — tiga `dead_code` warning feature-gated (`feature native`, hanya muncul saat `--all-features`; CI default/clippy tidak terpengaruh): (1) `UcTableScan::columns`/`rows`/`row_count` tak pernah dibaca — `rows`/`row_count` selalu kosong = scaffolding scan masa depan, `columns` dibangun hanya untuk menurunkan string `schema`; (2) `list_data_files` + `collect_parquet_files` (`akar-iceberg/src/native_reader.rs`) hanya dipakai test `test_list_data_files` — path produksi `list_active_data_files`. Mitigasi: `#[allow(dead_code)]` + doc-note (preseden `format_columns` unity-catalog); penghapusan total ditolak (helper utuh/bertest, berguna untuk tooling). `cargo check --workspace --all-features` kini 0 warning. Audit: `docs/audits/audit-dead-code-allfeatures.md` (prioritas: unity-catalog > iceberg). — `216a852` — gate **1,704 (1,704 passed)**.
- **P51.47 (perf)** — plan-cache hit kini berbagi `Arc<BoundStatement>`/`Arc<Vec<LogicalOperator>>` sehingga tak ada deep-clone operator tree per query; handler processor (sequence, schema-DDL, query, subquery, standalone-call registry) dibangun sekali per Connection dan di-share via `OnceLock` — `create_processor` tak lagi rebuild ~27 handler Arc tiap query. Handler dicache di `Connection` (bukan `Database`) agar `Arc<Database>` kuat di dalam handler tak membentuk reference cycle yang mengunci file lock selamanya. Validasi: `test_plan_cache_no_hit_regression` isolasi ratio hit/miss **0.914** (full-suite 0.950) — `340dbd0` — gate **1,647 (1,642 passed)**.

### Fixed

- **P53.26 (Fase 1 UNWIND) binding UNWIND visible di clause berikutnya** — alias UNWIND (`row.id`/`iid`/`e.src`) kini ter-resolve setelah MATCH/CREATE/SET; error `Variable 'x' not found in chunk field_names` dan silent no-op hilang. Empat lapis root cause + fix: (1) **`expr_to_value` tak menangani Map** (`akar-processor/src/physical/write_ops/unwind.rs`) — item map UNWIND dievaluasi `_ => Value::Null` sehingga seluruh elemen jadi NULL (silent no-op); kini arm `List` + `Map` (nilai `Value::Map` dengan key `Value::String`). (2) **kolom UNWIND dibangun via `store_value_in_vector` yang membuang Map/Struct ke NULL** (`physical/common.rs`) — `PhysicalUnwind::execute` ditulis ulang membangun kolom unwound sekali lewat `build_arrow_from_values` (dijadikan `pub(crate)`), mempertahankan map/struct/list. (3) **property access `row.id` tak mengekstrak key dari map/struct** — helper baru `map_property_value` (`expression_evaluator.rs`) menangani `Value::Map` dan `Value::Struct`, di-wire ke `evaluate_arrow_property_access`, `evaluate_property_access`, dan `evaluate_expression_for_row` (set.rs). (4) **SET snapshot hanya berisi kolom tabel** — `PhysicalSet::execute` melacak `source_rows: Vec<(chunk_idx, row_idx)>`; `build_snapshot_chunk(rows, input, source_rows)` + `append_input_columns` menempel kolom pipeline non-tabel (UNWIND variable) ke snapshot (dibangun via Arrow agar complex value bertahan). **Join key `m.id = row.id`** — `join_helpers.rs` ditulis ulang (`derive_join_bindings`, `JoinKeyBinding { build_col, probe_col, build_map_key, probe_map_key }`, `split_prop`, `resolve_side`): resolusi 1) qualified prop, 2) bare prop, 3) variabel map + ekstraksi key dengan cross-side fallback; `map_join.rs` materializes kolom sintetis ekstraksi key ke chunk (dedupe per (col,key)), join dijalankan atas kolom baru, lalu `strip_join_synthetic_columns` membuangnya dari output (hash: strip `[build_orig, build_orig+build_appended)` + `[build_orig+build_appended+probe_orig, ...+probe_appended)`; semi/anti: strip `[probe_orig, probe_orig+probe_appended)`). **Output HashJoin drop complex value** — `probe()` di `join_ops.rs` menyalin baris via round-trip Value yang membuang Map ke NULL; kini concat + `arrow::compute::take` per sisi (offsets build/probe) mempertahankan kolom map/struct. **Precedence property access** — snapshots memakai plain column names (`id`, `content`, ...), sehingga `row.content` jatuh ke bare-name match kolom tabel `content`; ekstraksi map-variabel kini dijalankan SEBELUM bare-name match di ketiga jalur evaluator (qualified → bound map-var → bare → legacy). Regression test `test_p5326_unwind_scope.rs` (4 tes): scalar UNWIND→MATCH semua elemen (A1), map UNWIND→CREATE satu node per elemen (A4), map UNWIND→MATCH→SET baca `row.content` (A2/A3), scalar UNWIND→MATCH→SET per elemen. — `0e0cf99` — gate **1,708 (1,708 passed)**.
- **P53.25 (follow-up P53.21) eksekusi rantai OPTIONAL MATCH → CREATE** - `add_bridge_batch` (`UNWIND [1] AS row MATCH (a:Memory {id: row}), (b:Memory {id: 3}) OPTIONAL MATCH (a)-[existing:Connected]-(b) WITH a, b, existing, row WHERE existing IS NULL CREATE (a)-[:Connected {weight: 0.9, type: 'bridge'}]->(b) RETURN count(*)`) kini **bereksekusi penuh**: panggilan pertama membuat edge + `count(*)`=1, panggilan kedua `count(*)`=0 tanpa duplikat. Sebelumnya query ter-bind tapi silent no-op (`RETURN count(*)`=0, 0 edge). Empat lapis root cause: (1) **UNWIND salah rute** - `BoundUnwind` di-push ke `delete_exprs` sehingga `MATCH ... WHERE a.id=row AND b.id=3` menjadi Filter top-level pada pipeline terpisah (join tak pernah terjadi); kini `BoundUnwind` mendorong `LogicalUnwind` ke `scan_ops` dan memasukkan variabelnya ke `available_vars`. (2) **OPTIONAL MATCH sisi kanan tak mampu membawa endpoint senyawa** - sisi kanan berupa ScanRel polos (tak bisa membawa `a`/`b` yang sudah di-join); dibuat operator baru **`LogicalOptionalExtend`/`PhysicalOptionalExtend`**: planner mendeteksi edge-probe (2 pattern, keduanya node var di scope, pattern[0] punya edge bervariabel+berlabel tanpa bound, pattern[1] tanpa edge) dan memancarkan `OptionalExtend` ber-children pipeline kiri (CrossProduct+Filter) yang membawa `row|a.id|...|b.id|...`; `PhysicalOptionalExtend` me-probe adjacency fwd lalu rev (`EdgeDirection::Both`), null-pad kolom edge, dan menambah kolom internal `<rel_var>._id` (non-null iff edge ditemukan) sehingga tabel relasi tanpa properti tetap bisa menjawab `existing IS NULL`. (3) **proyeksi `WITH a,b,existing,row`** - `resolve_projection_column_expand` baru meng-ekspansi `Variable` polos ke semua kolom ber-prefix `{name}.` (fallback posisional bila tak ter-resolve). (4) **`WHERE existing IS NULL`** - evaluator kini meng-handle `UnaryOp::IsNull`/`IsNotNull` atas variabel yang punya kolom ber-prefix: AND dari mask null per-kolom (bukan baca kolom 0). **Bug penyerta:** `PhysicalInsertRel`/`PhysicalInsertNode` selalu mengembalikan count-chunk 1-baris meski input pipeline tersaring habis → `RETURN count(*)` mencatat 1 padahal CREATE di-skip; kini pipeline input yang seluruh chunk-nya ber-size 0 tetap mengembalikan 0 baris. Parser/planner/serializer/optimizer (join_order recursi children, cardinality = child pertama, factorization leaf group) disinkronkan. Regression test `test_p5325_add_bridge_batch_creates_edge_once` (first call=1 edge, second call=0, tanpa duplikat), `test_p5325_optional_match_probe_sees_existing_edge` (probe positif: `existing IS NOT NULL`), `test_p5325_optional_match_probe_undirected_both_directions` (probe reverse dari `(b)-(a)`). Bonus: workspace di-`cargo fmt` (rustfmt max_width=120) + gap build all-features pre-existing di `akar-azure` diperbaiki (`retryable_status` minta `u16`, diterima `StatusCode`). - `f25c2a5` - gate **1,704 (1,704 passed)**.
- **P53.20 (G3) MERGE edge dengan node label-less referensi variabel MATCH-bound** — query Kairos `add_connection` (`MATCH (a:Memory {id: row}), (b:Memory {id: 3}) MERGE (a)-[r:Connected {type: 'similar'}]->(b) SET r.weight = 0.7, ...`) gagal `Bind error: CREATE/MERGE requires a label (table name)`: pattern `(a)-[:Connected]->(b)` di MERGE merefer variabel node yang sudah ter-MATCH, tapi `bind_create_patterns`/`bind_merge_clause` menuntut label. Fix dua lapis: (1) **binder** — `bind_merge_clause(&self, m, variables)` menerima scope; node label-less di-resolve dari variabel bound (`ok_or_else` error jelas bila tak ditemukan), label/table diambil dari binding lama, dan node referensi TIDAK di-push ulang ke `new_vars`. (2) **eksekusi** (melampaui scope bind): MERGE edge kini di-rutekan ke operator baru `LogicalMergeRel` (planner: `build_merge_operators`, arm `BoundClause::BoundMerge` memeriksa pattern edge; optimizer/plan-serializer disinkronkan) → `PhysicalMergeRel` (`akar-processor/src/physical/write_ops/merge_rel.rs`): match-or-create edge via forward/reverse adjacency + `props_match` terhadap properti pattern, `insert_rel` + undo `UndoRecord::insert`, output kolom `<edge_var>._id` agar klausa `SET` lanjutan (standalone setelah MERGE; grammar `merge_clause` tidak menerima plain SET) menemukan baris yang benar; `on_match`/`on_create` diterapkan terpisah per edge. **Bug selama implementasi:** `continue` untuk node referensi melewatkan push edge pada iterasi pattern yang sama → `new_vars=[]` → `Bind error: Variable 'r' not in scope for SET` (ditemukan lewat tes, diganti `if !is_reference`). Regression test `test_p5320_merge_edge_label_less` (satu tes penuh): create → match-update tanpa duplikat (weight/timestamp berubah) → type berbeda membuat edge kedua; schema REL menyertakan kolom Kairos asli (weight/type/created_at/event_time/ingestion_time/valid_from). — gate **1,701 (1,701 passed)**.
- **P53.21 (G3) OPTIONAL MATCH variabel existing** — `add_bridge_batch` (`UNWIND [1] AS row MATCH (a:Memory {id: row}), (b:Memory {id: 3}) OPTIONAL MATCH (a)-[existing:Connected]-(b) WITH a, b, existing, row WHERE existing IS NULL CREATE (a)-[:Connected {..}]->(b) RETURN count(*)`) gagal `Bind error: Variable 'a' already defined`: `bind_optional_match` (mod.rs:1156) memakai `allow_existing=false` sehingga node label-less yang menamai variabel lama ditolak. Fix: `bind_pattern` (jalur `allow_existing=false`) kini memperbolehkan node label-less yang merefer variabel node yang sudah ada di scope (same_node disamakan dengan variabel lama, tidak di-declare ulang). Scope task = binding; eksekusi penuh rantai OPTIONAL MATCH→compound-key join→`existing IS NULL`→CREATE **belum diimplementasi** — query berjalan tanpa error tetapi `count(*)` = 0 (silent no-op, tercatat sebagai follow-up engine). Regression test `test_p5321_optional_match_existing_var` menegaskan query ber-parse dan ter-bind (bukan "already defined"/"Bind error"). — gate **1,701 (1,701 passed)**.
- **Koreksi angka gate** — ekspor HTML resmi `Test Results - test_[akar-core].html` (2026-08-16, 4m2s) mencatat **1,701 total / 1,701 passed / 0 failed / 0 ignored**. Urutan terverifikasi: 1,692 (P53.19) → 1,701 (P53.23 +4, P53.24 +3, P53.20 +1, P53.21 +1). Angka "**1,803**" yang ditulis pada entri P53.23/P53.24 (`2f48ecc`) tidak sesuai ekspor HTML mana pun — fabrikasi sesi lama; angka di entri ini yang benar.
- **P53.23 (latent engine) ORDER BY computed sort key** — `resolve_sort_keys` (`akar-processor/src/processor/mapper/map_projection.rs:52`) untuk sort key **ekspresi computed** (mis. `n.a + n.b`, `array_cosine_similarity(...)`) memanggil `resolve_projection_column_index` yang hanya menangani PropertyAccess/Variable → `None` → fallback posisional `col.unwrap_or(i)` memetakan ke kolom ke-i output, jadi `RETURN n.id, n.a, n.b ORDER BY n.a + n.b` mengurutkan berdasarkan `n.id` (silent wrong result; terkena `PhysicalOrderBy` & `PhysicalTopK`/LIMIT). Fix: `resolve_sort_keys` kini mengevaluasi sort key computed per baris via `ExpressionEvaluator` (registry + sequence/subquery handler) atas chunk output proyeksi, menempelkannya sebagai kolom sintetik trailing pada salinan input, menyortir, lalu `strip_sort_columns` membuang kolom sintetik dari output (`take_global_rows` menyalin semua field). Sort key kolom polos tetap resolve by-name. **Bonus fix:** `evaluate_variable` (legacy, `expression_evaluator.rs:658`) punya fallback senyap ke kolom 0 saat variabel tak ditemukan di `field_names` → ORDER BY key yang merefer kolom NON-proyeksi (invalid Cypher, mis. `RETURN b.title ORDER BY b.id`) kini error jelas, bukan urut-salah diam-diam. Regression test `test_p5323_order_by_computed.rs` (4 tes, data anti-korelasi): DESC, error non-projected, TopK+LIMIT, mixed computed+column; `integration_test.rs` DDL-alter di-update mem-proyeksi `b.id`. — `2f48ecc` — gate **1,803 (1,803 passed)**.
- **P53.24 (latent engine) list subscript `x[i]`** — grammar `cypher.pest` `postfix_expr` memunculkan indeks `[i]` sebagai child `Rule::expression`, tapi handler `expression.rs:150` hanya memproses child `Rule::property_access` → `RETURN n.embedding[0]` parse menjadi `PropertyAccess(n, "embedding")`, index di-drop diam-diam (mengembalikan seluruh list; `m["k"]` juga jadi `m`). Fix: child `Rule::expression` kini di-rewrite menjadi `list_extract(x, i + 1)` — subscript Cypher 0-based, `list_extract` 1-based (`akar-function/src/scalar/list.rs:18`, idx>=1, `*i-1`). Keterbatasan terdokumentasi di audit: index OOB → error (bukan NULL), index string → error. Regression test: parser `test_list_subscript_index_kept` (AST `list_extract(n.embedding, 0+1)`) & `test_list_subscript_chain_and_expr_index` (`a[1].b + x[id].c`); integrasi `test_nested_types.rs` di-update — `t.lst[1]` → `Int64(20)`, `t.lst[0]` → `Int64(10)` (baru), `t.lst[10]` → error "out of bounds" (sebelumnya mengembalikan seluruh list). — `2f48ecc` — gate **1,803 (1,803 passed)**.
- **P53.19 (G8) EXPORT/IMPORT DATABASE path quoting** — string literal path (`EXPORT DATABASE "C:/..."`, kairos) kini di-strip lewat `unescape_string` (`expression.rs:402`) alih-alih `trim_matches('\'')` yang hanya menghilangkan kutip tunggal → path kutip-ganda dianggap termuat kutip literal & directory dibuat di path yang salah. Fix di 3 situs `akar-parser/src/parser/ddl.rs`: `parse_export_database` (:143-144), nilai `export_option` (:154), `parse_import_database` (:173). Regression test `test_export_database_double_quoted_path` (double+single quote) & `test_import_database_double_quoted_path` — akar-parser 82/82 hijau. — `df6bd0c` — gate **1,692 (1,692 passed)**.
- **P53.19 (G9) QUERY_VECTOR_INDEX translation** — hasil translasi `CALL QUERY_VECTOR_INDEX(...) RETURN node, distance` → `MATCH ... RETURN {..} AS node, array_cosine_similarity(...) AS distance ORDER BY ...` menghasilkan urutan jarak yang SALAH. Akar masalah di engine: `resolve_sort_keys` (`akar-processor/src/processor/mapper/map_projection.rs:52-61`) untuk ORDER BY **ekspresi computed** (bukan variable/property) memanggil `resolve_projection_column_index` yang mengembalikan `None` → fallback posisional `col.unwrap_or(i)` memetakan ke kolom 0 (id), jadi `ORDER BY array_cosine_similarity(...) DESC` mengurutkan berdasarkan kolom salah. Fix di sisi translasi (`build_vector_query`, `akar-python/src/lib.rs`): `ORDER BY distance DESC` memakai alias proyeksi (resolve via P53.16) alih-alih mengulang ekspresi cosine. Regression test `p5319_g9_query_vector_index_translation` (4 node bertetangga, LIMIT 2, assertion DESC non-increasing + baris pertama ~1.0) — akar-python 24/24 hijau (standalone, gate cargo tak berubah dari sisi python; naik dari tes parser G8). **Latent bug engine tercatat:** ORDER BY ekspresi computed apa pun jatuh ke fallback posisional; `n.embedding[0]` mengembalikan seluruh list (index diabaikan) — secondary notes, bukan scope G9. — `df6bd0c`
- **P53.18 (G7) reopen-after-close** — `db.close()` lalu `Database(path sama)` kini bisa dibuka lagi (sebelumnya `Database '...' is already open by another process` �?" `close()` tak melepas lock). Akar masalah: wrapper Python `Connection` memegang `conn: akar_main::Connection` yang menyimpan `Arc<Database>` (`connection/mod.rs:68-69`); `Database.close()` hanya set `self.db = None`, tapi Arc dari connection membuat `Database` (beserta file lock `_lock_file`) tetap hidup selama wrapper Python masih direferensikan �?" pola kairos close�?'checkpoint�?'reopen G7. Fix di `akar-python/src/lib.rs`: (1) `Connection.conn` kini `Option<akar_main::Connection>`; `close()` drop Arc; helper `conn()` error `"Connection is closed"` bila None; semua jalur eksekusi (`run`/`execute_action`/`table_exists`/`ensure_table_schema`/`build_vector_query`) menerima `&akar_main::Connection` hasil `self.conn()?`. (2) `Database` melacak connection-nya: `connections: Vec<Py<Connection>>`; `Connection::new` (kini `#[new] -> PyResult<Py<Self>>`) mendaftarkan `clone_ref(py)` ke database; `Database.close(py)` menutup paksa semua connection (set `conn=None`) lalu `db=None` �?" lock dilepas meski wrapper Python masih hidup. (3) Guard baru: `Connection::new` setelah DB ditutup error `"Database is closed"`; query setelah `Connection.close()` error (bukan panic). 3 tes regresi (`db_close_releases_lock_even_with_live_connection`, `connection_close_releases_lock_before_db_close`, `closed_database_and_connection_reject_use`) �?" akar-python 23/23 hijau (standalone workspace, bukan member akar-core; gate cargo tidak berubah).
- **P53.17 (G6) SET aritmetika & multi-item atomic** — `SET m.x = m.x + 1` kini membaca nilai lama sebelum write (sebelumnya NULL/0 karena fallback `evaluate_expression_for_row` lama sekadar `chunk.get_value(1, row)` — baca kolom 1, tak pernah mengevaluasi ekspresi), dan klausa SET multi-item (`SET m.a = ..., m.b = m.a * 10`) diterapkan atomik terhadap **satu snapshot pra-write** (semua item dievaluasi dari nilai lama, sesuai semantik Cypher/Neo4j). Restrukturisasi dua lapis: (1) planner — `LogicalSet` kini memegang `items: Vec<SetItem>` (`column_name`, `column_idx`, `value`); `BoundSet` mengelompokkan item satu klausa per (table_name, table_id, is_node) menjadi SATU `LogicalSet` (bukan rantai `[Set(a), Set(b)]` yang membuat item berikutnya hanya menerima count-chunk operator sebelumnya → `row_id_column_index` tak menemukan `_id` → `unwrap_or(0)` membaca nilai count sebagai offset baris; rantai multi-table tetap satu `LogicalSet` per table, klausa SET terpisah tetap op terpisah); on_match/on_create MERGE membungkus tiap item `vec![item]`; serializer, mapper, dan optimizer `projection_pushdown` disinkronkan. (2) eksekusi — `PhysicalSet` mengevaluasi tiap `SetItem` terhadap `DataChunk` snapshot yang dibangun dari data OLD tabel (plain column names sebagai `field_names`), memakai `ExpressionEvaluator` (registry) dengan fallback `evaluate_expression_for_row` yang diperluas (Variable/PropertyAccess/UnaryOp/BinaryOp via `binary_value_op`/List/Map); col_idx di-resolve by name per item; output tetap count-chunk (kontrak `test_delete_set`). Regression test `test_p5317_g6_set_arithmetic.rs` (8 tes): self-reference increment, kombinasi kolom, multi-item old-state, multi-item self-reference, property→property, function call, float arithmetic, filtered rows. Catatan: `MERGE ... SET` single item (chain logical rusak: Merge output count-chunk) adalah bug pre-existing di luar scope G6. — gate **1,690 (1,690 passed)**.
- **P53.16 (G5) RETURN/WITH `AS` alias dihormati** — `RETURN count(m) AS cnt` kini mengekspos kolom `cnt` (sebelumnya nama turunan `COUNT(m)`), `RETURN m.age AS a` → `a`, dan `ORDER BY cnt`/`ORDER BY a` meresolve alias proyeksi (sebelumnya `Variable 'cnt' not in scope`). Tiga akar masalah: (1) `bind_return` membuang `ReturnItem.alias` — `BoundExpression` kini membawa `alias: Option<String>` (31 situs konstruktor `alias: None`; `substitute_in_bound_expr` mempertahankan alias) dan `bind_return` menyalinnya + ORDER BY variabel polos yang cocok dengan alias proyeksi di-resolve ke kolom proyeksi (alias menaungi variabel scope). (2) optimizer `AggregateDetection` membangun ulang ekspresi proyeksi dengan `alias: None` — sekarang selalu `be.alias.clone()`, dan Projection **selalu dipertahankan di atas Aggregate** (tidak lagi hanya untuk nested) agar alias diterapkan terpusat. (3) `ConstantFolding` membangun ulang projection dengan `alias: None` — kini `e.alias.clone()`. Processor: `map_projection` menamai kolom output dari alias di jalur eval & jalur plain-column (rename chunk hasil `PhysicalProjection`), `expression_field_name(alias, expr)`; `map_aggregate` penamaan group-by menyamai `expression_field_name` (PropertyAccess → `var.prop`) agar resolve-by-name presisi. Regression test `test_p5316_g5_alias_return.rs` (7 tes): alias aggregate, nested aggregate, property, constant, ORDER BY alias (data anti-korelasi), group-by+aggregate+ORDER BY alias, no-alias mempertahankan `COUNT(m)`. — gate **1,682 (1,682 passed)**.
- **P53.15 (G4) nested aggregate** — `COALESCE(MAX(m.id), 0)`, `COALESCE(MAX(s.id), 0) + 1`, `COALESCE(SUM(x), 0)` (kairos `_next_id`/`add_revision`/stats; sebelumnya `Unknown function: 'MAX'`) kini bind & eksekusi. Dua akar masalah: (1) optimizer Pass 6 `AggregateDetection` hanya mendeteksi aggregate yang SELURUH ekspresi proyeksi adalah satu panggilan top-level (`RETURN MAX(x)` bekerja, `RETURN COALESCE(MAX(x),0)` tidak). Ditulis ulang menjadi rekursif — `contains_aggregate`/`rewrite_aggregates` mengekstrak tiap panggilan aggregate (COUNT/SUM/AVG/MIN/MAX/STDDEV/VARIANCE/COLLECT) ke operator Aggregate dan menggantinya dengan `Variable(aggregate_ref_name(...))` (nama wajib sinkron `aggregate_field_names` di `map_aggregate.rs`, mis. `MAX(m.id)`/`COUNT(*)`); proyeksi luar tetap dipertahankan DI ATAS Aggregate sebagai pasangan flat `[Aggregate, Projection]` (wrapper Projection ber-children kosong, mengevaluasi ekspresi luar atas chunk hasil aggregate; tak bisa masuk ke children Projection karena `execute_children` menjalankan pipeline internal ber-input kosong). (2) `evaluate_arrow_function_call`/`evaluate_function_call` menerapkan null-shortcut SQL (`arg null → hasil null`) dengan pengecualian `name != "coalesce"` **case-sensitive** — binder mempertahankan huruf SQL, jadi `COALESCE` uppercase dengan arg NULL (mis. MAX tabel kosong) di-shortcut ke NULL, bukan fallback 0; pengecualian kini `eq_ignore_ascii_case`. Semua ekspresi non-aggregate menjadi group-by key. Perilaku top-level lama dipertahankan (Projection di-drop). Regression test `test_p5315_g4_nested_aggregate.rs` (8 tes): MAX/SUM nested, fallback tabel kosong (0), pola `+1` kairos, group-by+nested, top-level, campuran top+nested. **Bug penyerta yang diperbaiki:** COALESCE/IFNULL uppercase dengan arg NULL (tabel berisi row null) kini mengembalikan fallback, bukan NULL. — gate **1,675 (1,675 passed)**.
- **P53.22 (pre-existing all-features build gaps)** — `cargo check --workspace --all-features` (pre-commit gate) gagal di kode extension fitur non-default yang selama ini tak pernah di-compile gate (regresi `7c601a5` batch E/F): (1) `akar-postgres` fitur `native` — cache koneksi memanggil `client.clone()` padahal `tokio_postgres::Client` 0.7.18 **tidak** impl `Clone` (E0599 + E0308); cache kini menyimpan `Arc<Client>` (satu koneksi shared per `conn_str`, `Arc::clone` di arm cache-hit). (2) `akar-duckdb` fitur `bundled` — `DuckDbManager::shared_in_memory()` memanggil `in_memory()` bare di closure `get_or_init` (E0425); diubah ke `Self::in_memory()`. Verifikasi: `check [akar-core]` (release, all-features) **hijau 1m20s**; gate `test [akar-core]` tak terpengaruh (keduanya di balik fitur non-default). Catatan + temuan cleanup `target/` di `docs/audits/audit-pre-existing-gaps-build-features.md` (audit, git-excluded): artefak basi `criterion`/`package`/`release-debug`/`tmp` + semua `kuzu-*` (13) + `libduckdb-sys-*` (10) + `libsqlite3-sys-*` (6) dihapus → **+25 GB** ruang disk (186.2 → 211.5 GB bebas). — `8145cfa` — gate **1,667 (1,667 passed)**.
- **P53.14 (G3) grammar chain clauses** — `query_statement` di `cypher.pest` digeneralisasi menjadi `query_clause+ ~ return_clause? | return_clause` (pest menolak `*` karena `statement` alternation bisa match-empty → "following choices cannot be reached"); `query_clause` membungkus match/optional-match/where/unwind/with/set/delete/create/merge/foreach sehingga klausa boleh dikombinasi bebas urutan. `parse_query_pairs` ditulis ulang meng-iterasi anak `query_clause` (WHERE dipancarkan sebagai `Clause::Where` tersendiri agar struktur `[Match, Where, Return]` lama dipertahankan); MERGE standalone tetap didispatch ke `Statement::Merge`, selain itu disematkan sebagai `Clause::Merge`. Binder: `bind_merge` di-refactor jadi `bind_merge_clause` yang juga memancarkan variabel baru (node + edge pattern); arm `Clause::Merge` di `bind_query`; `BoundClause::BoundMerge`. Planner: `BoundWhere` mid-pipeline dialihkan ke `delete_exprs` sebagai `Filter` saat ada update pending; arm `BoundMerge` mendorong `LogicalMerge`; perakitan diurutkan ulang `scans → extends → Filter → delete_ops → Projection → [Aggregate] → OrderBy → Limit` sehingga SET/DELETE/MERGE berjalan sebelum proyeksi RETURN. Terverifikasi: 7/7 rantai G3 Kairos (strengthen_connection, set_meta, _next_id, add_connection, add_bridge_batch, set_then_return, delete_then_return) parse tanpa error; `MERGE (m:Meta {key:'k'}) SET m.value='v'` mengeksekusi & membuat row. 8 tes parser + 2 tes integrasi baru. **Tertunda (binder, task lanjutan):** `add_connection` (merge-pattern node label-less yang merefer variabel MATCH-bound → "CREATE/MERGE requires a label") & `add_bridge_batch` (OPTIONAL MATCH variabel yang sudah ada → "Variable 'a' already defined"; `bind_optional_match` perlu `allow_existing=true`). — `6c59b3a` — gate **1,667 (1,667 passed)**.
- **P53.13 (G2) List NULL di ORDER BY/LIMIT/TOP-K** — `PhysicalLimit` (partial-chunk), `PhysicalOrderBy`, dan `PhysicalTopK` mem-materialisasi baris output lewat round-trip legacy `ValueVector` (`get_value`/`store_value_in_vector`) yang di arm catch-all-nya **membuang `Value::List` (dan Struct/Array) jadi NULL** — kolom skalar selamat, jadi `MATCH (m:Memory) RETURN m.embedding LIMIT 1` / `ORDER BY m.id` mengembalikan embedding NULL. Fix: helper `take_global_rows()` (`akar-processor/src/physical/common.rs`) memakai Arrow `take` atas array sumber (concat bila chunk > 1) — ketiga operator dialihkan ke sana, kolom complex dipertahankan. Regression test `test_p5313_list_sort_limit.rs` (5 tes, 3-row chunk agar partial-copy terpicu): LIMIT, LIMIT+SKIP, ORDER BY ASC/DESC, ORDER BY+LIMIT. Terverifikasi gagal di kode lama (embedding NULL) & lolos setelah fix. — `79c75f6` — gate **1,658 (1,658 passed)**.
- **P53.12 (G1) rel-scan target binding** — `PhysicalExtend::execute` meresolve baris node destinasi lewat map `pk_to_row` (PK→row) padahal `RelTable.fwd_adj`/`rev_adj` menyimpan **row offset** (bukan PK). Saat PK ≠ row offset (mis. PK mulai dari 1), `MATCH (a:Memory)-[r:Connected]->(b:Memory)` membind `b` ke baris `a` → `RETURN a.id, b.id` mengembalikan `{1,1}` bukan `{1,2}` (`r.weight` sudah benar karena dibaca via `edge_idx`). Fix: hapus map `pk_to_row` di `PhysicalExtend::execute`, resolve baris dest langsung via `dst_offset` (row offset ke node table tujuan); skip-check pakai `dest_num_rows`. Regression test `test_p5312_rel_scan_binding.rs` (4 tes, PK sengaja ≠ offset): binding forward, arah balik, rel-props, multi-hop. — `efa78cf` — gate **1,653 (1,653 passed)**.
- **P53.1 blocker DDL `BOOLEAN`** — `primitive_type` grammar mencoba `"BOOL"` sebelum `"BOOLEAN"` (pest alternatif berurutan tanpa longest-match) → `protected BOOLEAN` (kairos `_ensure_schema`) ter-potong jadi `BOOL`+`EAN`, gagal parse. Urutan dibalik: `"BOOLEAN"` sebelum `"BOOL"`. Regression test `test_create_node_table_boolean_types`. — `b93e966` — gate **1,649 (1,649 passed)**.
- **P53.12 (complex-type query output)** — kolom & literal List/Struct/Map kini round-trip melewati pipeline Arrow alih-alih kolaps NULL (4 lapis: `scan.rs` `logical_to_physical` memetakan List/Array/Map/Struct/Union ke `PhysicalTypeID::List`/`Struct`; `build_arrow_array` (scan) & `to_arrow_array` (storage) + `ArrowVector::from_legacy` + `build_arrow_from_values` (evaluator) membangun `ListArray`/`StructArray` via helper baru `arrow_array_from_values` yang menangani tipe kompleks/nested rekursif; proyeksi map/function dialihkan ke `evaluate_arrow` (bukan ValueVector yang tak punya side-storage); `evaluate_expression_for_row` & `evaluate_constant_expr` (write path CREATE/SET) mengevaluasi literal List/Map rekursif sehingga `FLOAT[]` tersimpan nyata; `DataChunk::get_value` punya arm List/Struct). Regression test feature-gated `test_vector_index.rs`: `RETURN n.embedding`→List, `RETURN {id:n.id}`→Struct, `array_cosine_similarity`→Double; `test_nested_types.rs` di-update (list/struct kini round-trip; `size()`, `list_concat`, nested list bekerja). Catatan: subscript `t.lst[i]` masih di-drop parser (gap terpisah). — `46de39d` — gate **1,653 (1,648 passed)**.
- **P53.13 (hang)** — `bind_create_vector_index` self-deadlock: guard `catalog` #1 tak di-drop sebelum `lock()` #2 (re-entrant `MutexGuard` = hang `WaitOnAddress`). `CREATE VECTOR INDEX` kini selalu ter-bind (sebelumnya hang untuk metric valid). Guard #1 di-scope. Regression test `test_vector_index.rs` (watchdog 15s). Audit 24 call-site `catalog.lock()` — hanya fungsi ini yang double-lock. — `24e9610` — gate **1,649 (1,644 passed)**.
- **P53.14 (flake FTS)** — `PhysicalCreateFtsIndex::execute` memegang DashMap `Ref` (`source_table`) sambil acquire write lock (`create_node_table`/`get_*_mut`) → read→write di shard sama = re-entrant deadlock; seed acak DashMap → tabrakan shard intermiten (flake "lolos isolasi, hang suite penuh"). Fix: snapshot schema+data dalam block, `Ref` di-drop sebelum write. Reproduksi load: hang sebelum fix, 10/10 lolos sesudah; gate penuh 3m42s bersih. — `24e9610` — gate **1,649 (1,644 passed)**.
- **Batch bug P51.28–P51.36** — `DROP TABLE` kini hanya menghapus sequence milik tabel itu sendiri (enumerasi kolom SERIAL exact-match menggantikan prefix-filter `{name}_*_serial` yang menghapus sequence tabel ber-prefix sama, P51.28); commit tulis jadi two-phase: `prepare_commit` (validasi OCC + assign commit_ts + deregister agar checkpoint drain tak menunggu txn sendiri) → durable pipeline (`WAL` → `persist_all_tables`) → `finish_commit` (publish commit_history + release lock); `commit_write_txn` di-reorder mengikuti urutan itu (P51.29); UNION kini menjalankan kedua sisi dengan snapshot MVCC (`with_snapshot`) sehingga tak mencampur baris committed/uncommitted (P51.30); prepared statement diperluas: koleksi + substitusi parameter di pattern properties (MATCH/OPTIONAL MATCH/CREATE), ORDER BY, SET, DELETE, UNWIND, FOREACH sub-statements, CREATE DML, dan MERGE (termasuk ON CREATE/ON MATCH) (P51.31); `LIMIT`/`SKIP` non-u64 (negatif/overflow) kini error tegas, bukan silent-drop (P51.31); `value_to_constant` → `Result<Constant, String>` — UInt64/Int128 overflow error, tipe BLOB ditolak, tak lagi fallback `Constant::Null` (P51.32); CLI: completer aman multibyte (`floor_char_boundary`), Double/Float ditampilkan presisi penuh (bukan `{:.4}`), JSON escape lengkap (`\`, `\n`, `\r`, `\t`, kontrol <0x20) (P51.35); WASM: konversi JS number ke i64 dibatasi rentang pasti, `Database::new` memory-only di browser dengan pesan ramah (P51.36) — 9 tes baru (4 integrasi, 3 parser, 2 unit) — `f901425` — gate **1,647 (1,642 passed)**.
- **P51.17** — HNSW `insert` kini menghormati `_id` sebagai node id: `HnswIndex.nodes` diganti `BTreeMap<usize, HnswNode>` (id sparse diizinkan) sehingga hasil `search` selalu mengembalikan row id asli — sebelumnya node index posisional dipakai sebagai row id dan rusak bila baris NULL/non-vector di-skip saat populate (lookup lalu membaca baris yang salah). `VectorIndexTable::save` menulis node id sebenarnya agar id sparse tahan persist/load (file format lama tetap terbaca). 2 tes baru (`test_insert_honors_sparse_ids`, `test_vector_index_roundtrip_preserves_ids`) — `d5b13c1` — gate **1,638 (1,632 passed)**.
- **Batch G P52 (rekonsiliasi audit)** — art_index: `num_entries` kini ter-increment saat dup-key insert, `serialized_tree_size` dihitung tanpa materialisasi (anti-OOM), prefilter range pakai `upper_inclusive` (P52.51); wal_replayer: first-pass dead dihapus + batch data di-discard saat Rollback (P52.52); checkpoint drain gate kini di-lock `begin_read`/`begin_write` (P52.53); UnwindDedup key (variable, expression) + hanya dedup berurutan (P52.54); LimitPushDown guard ORDER BY/top-k/aggregate (P52.55); hasil agregat punya `field_names` (P52.56); graph output_writer ikuti predecessor chain + depth-guard + dst benar (P52.57); scan Int128/InternalID tak lagi NULL diam-diam (P52.58); akar-llm embedding non-numeric → error & arg override provider/model aktif (P52.59); httpfs `http_get` body dibatasi 64 MiB (P52.60); FFI akar-c ekspor `error_message` via out-param + `akar_error_message_free` (P52.61) — `ef4f143` — gate **1,636 (1,631 passed)**.
- **Batch F P52** — akar-json: doc header dikoreksi (fungsi yang benar-benar ada saja), `json_extract` path hilang → NULL bukan error (query abort), `json_structure` inspeksi semua elemen array (bukan `arr[0]`), depth-guard pada `structure_inner`/`contains_inner` (P52.49); `SET spill_threshold=0` kini benar-benar menonaktifkan spilling (bukan fallback ke default) & `CALL current_setting('concurrent_writes')` baca toggle runtime live (P52.50) — `ca28ffd` — gate **1,628 (1,623 passed)**.
- **Batch 5 P51** — optimizer: DP join reorder dibatasi per-segmen pipeline (tak menyebrangi batas WITH/ORDER BY/LIMIT, P51.23); kondisi join diekstrak dari konjungsi AND di WHERE (P51.24); constant-folding float pakai `==` exact (bukan epsilon) agar selaras runtime (P51.25); aritmetika i64 pakai checked ops agar tak panic/wrap saat planning (P51.26) — `5050b21` — gate **1,619 (1,614 passed)**.
- **Batch HIGH P51** — remote client race dikunci satu mutex (write+read, P51.9); prepared-writes kini ber-OCC (P51.10, tercakup P52.18); CLI script-mode terdeteksi via `stdin.is_terminal()` bukan env/`cfg!(windows)` (P51.13); Delta `load_delta_table` me-replay semua versi log (P51.15); HNSW beam search dibatasi (heap, prune ef-th, search per-layer) — O(ef·M) bukan O(N²) (P51.16); FTS `tokenize()` kompilasi regex sekali via `LazyLock` (P51.18); `persist_all_tables` redundant per-statement dihapus (P51.27) — `3237ad7` — gate **1,619 (1,614 passed)**.
- **P52.18** — isolasi txn putus dari SQL write path: tulis kini SELALU dibungkus txn (mode single & concurrent writer); `txn_id` dialirkan ke operator tulis (insert/delete/set/merge/copyfrom/batchinsert) via `insert_row_with_txn`/`delete_row_with_txn` sehingga `VersionInfo`/`commit_history` terisi dan `is_row_visible` nyata (uncommitted insert/delete tak terlihat reader lain); undo ditangkap tiap op tulis + DDL inline (CreateDml/Merge) dan diterapkan saat rollback/konflik OCC — termasuk rel table (`rollback_transaction` di-extend) — `b35fdf4` — gate **1,619 (1,614 passed)**.
- Batch E: storage/execution/vector/graph + DML DELETE/SET row-id bug (P52.29/P52.31/P52.38–P52.45/P52.47–P52.48/P52.62) — `61b5e0e` — gate 1,607 (1,602 passed).
- Batch E/F: connectors + extension compile fixes — azure/httpfs/duckdb/postgres/iceberg/neo4j (P52.30/P52.32–P52.37/P52.66 + P51.19/P52.33/P52.34) — `7c601a5` — gate **1,619 (1,614 passed)**.

## [0.1.5] - 2026-08-11

### Changed

- Docs: mark release v0.1.5 — `a4380d3` (gate 1,594 hijau).

### Fixed

- CI: test `ice disk path` dibuat platform-independent — `0304a3c`.

## [0.1.4] - 2026-08-10

### Changed

- Baseline final (worktree committed) — gate **1,594 hijau**.
- Rilis v0.1.4: sync backlog crates.io → HEAD bottom-up, 15 crate naik versi — `e158dd3`.

### Fixed

- Batch C: connection txn safety + EXPORT/IMPORT + remote drain (P52.12–P52.15/P52.19/P52.23–P52.28) — `e3c7b5d` — gate 1,581 hijau.
- Batch D: storage correctness (P52.16/P52.17/P52.20/P52.21/P52.22) — `2498794` — gate 1,588 hijau.

## [0.1.3] - 2026-08-10

### Fixed

- Batch 2: MERGE/CREATE multi-pattern + parser + binder cleanup (P51.7/P51.8/P51.37/P51.38) — `a2210e2` — gate 1,548 hijau.
- Batch 3: FFI panic containment + query-result destroy + server bounds (P51.11/P51.12/P51.33/P51.34) — `aca6520` — gate 1,551 hijau.
- Batch 4: connectors — injection, temp-file, all-rows, chunk-fill, DRY (P51.14/P51.20/P51.21/P51.22/P51.39) — `8a7f2df` — gate 1,569 hijau.
- Batch A: execution-engine silent-wrong-result (P52.1/P52.3/P52.8/P52.9/P52.10/P52.11) — `d62c55b` — gate 1,569 hijau.
- Batch B: optimizer passes; 4 → NO-OP terdokumentasi (P52.2/P52.4/P52.5/P52.6/P52.7) — `8564faa` — gate 1,578 hijau.

## [0.1.2] - 2026-08-09

### Changed

- Rilis v0.1.2 (4 crate) — `fa52bf2`.

### Fixed

- Batch 1: correctness SQL inti (P51.1–P51.6) — `9c173da` — gate 1,543 hijau.

---

**Status publish (2026-08-10):** semua 31 crate publishable live di crates.io — akar-storage & akar-main @ 0.1.3, akar-binder/planner/processor/postgres @ 0.1.2, akar-optimizer @ 0.1.2, akar-common/parser @ 0.1.1, akar-server/httpfs/duckdb/sqlite/azure/delta/iceberg/unity-catalog @ 0.1.1, sisanya @ 0.1.0 (rilis v0.1.4 sync backlog crates.io → HEAD, bottom-up, 2026-08-10); akar-c `publish=false`; repo GitHub **public**; tag **v0.1.1** + **v0.1.2** + **v0.1.3** + **v0.1.4** + **v0.1.5** + GitHub Releases live.

[Unreleased]: https://github.com/mlengse/akar/compare/v0.1.5...HEAD
[0.1.5]: https://github.com/mlengse/akar/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/mlengse/akar/compare/v0.1.3...v0.1.4
[0.1.3]: https://github.com/mlengse/akar/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/mlengse/akar/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/mlengse/akar/releases/tag/v0.1.1
